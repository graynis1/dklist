# DKList v2 — Rewrite Plan & Cloud-Agent Operating Notes

> This file is the canonical, versioned copy of the rewrite plan, kept in sync with the
> plan doc on the maintainer's local machine. If you are a cloud agent picking up work on
> this branch, **read this whole file first**, then check recent commits on `v2-rewrite`
> to see what's already landed since this snapshot was written.

## Cloud-agent operating constraints (read this before touching anything)

This repo's `v2/` app is normally developed against a **local MySQL database** the cloud
sandbox does not have access to (no `DATABASE_URL`, no VPS SSH, no local dev server to
screenshot). Concretely, that means:

- **Do not add new pages/routes/server actions that call `db.*` (Drizzle) and expect
  `next build` to succeed** — Cache Components prerendering executes that code at build
  time, and a build with no reachable database will fail on any such route. If a page
  needs data, either (a) keep it as a pure UI component that accepts data as props/args
  (no DB import), so it can be wired to a real query later by a local session, or (b) if
  you must add the DB-calling code itself, keep it in a separate, clearly-named function
  in `src/db/queries/` and do NOT import/render it from any page yet — leave that wiring
  as a TODO/comment for local verification.
- **Verify with `npx tsc --noEmit` and `npm run lint`**, not `npm run build`, for any
  work that touches `src/db/`. A plain UI-only page (no DB import) is fine to verify with
  a full `npm run build`.
- **Never touch the production VPS, SSH, Docker, or DNS/Caddy config.** All of that lives
  outside this repo and outside this sandbox's reach anyway — if you find yourself
  wanting to, stop, that's a sign the task should wait for a local session.
- **Work on a feature branch off `v2-rewrite`**, commit incrementally with clear
  messages, and push. Open a PR into `v2-rewrite` if `gh` is available; otherwise just
  ensure the branch is pushed with a clear branch name (e.g. `v2-cloud/phase1-category-ui`)
  so a local session can find and review it.
- **Leave a clear PR/commit-message note** on anything that still needs local
  verification (real DB query behavior, visual/screenshot check, EXPLAIN-plan check) —
  don't claim something is "done" if it only type-checks.
- Good candidate work for this constrained environment: **pure UI components** (no DB
  imports) built from realistic placeholder data matching the real schema shapes in
  `src/db/schema.ts`; **pure logic/utilities** (e.g. the Phase 5 fuzzy-matching
  functions — ISBN normalization, title normalization, Levenshtein/Jaro-Winkler
  similarity — these need zero DB access and are fully unit-testable in isolation);
  TypeScript types/interfaces; documentation.

---

## Context

dklist.com is a live book-catalog site (~98.5M `book` rows, ~11.3M `writer`, ~4.6M `publisher`, real users, a live marketplace with Iyzico payments) currently on Symfony/PHP + Doctrine + React (CRA/antd), running on a Contabo VPS (`dklist-vps`) with MySQL native on the same box, Cloudflare-proxied DNS, frontend on Vercel.

The site owner decided on a full from-scratch rewrite rather than incremental modernization, with a hard requirement: **the new frontend must look and feel nothing like the old one.** Stack: Next.js 16 (App Router, Cache Components) + TypeScript, Drizzle over the same live MySQL database schema (no data migration), eventual deploy on the same VPS. A large customer feature wishlist exists (AI recommendations, a duplicate-detection pipeline, a Spotify-Wrapped-style annual report, new membership/role tiers) — not detailed here, ask the repo maintainer if you need it.

This plan exists because a blind "just start coding" approach risks repeating three specific, already-paid-for lessons from v1's production history: (1) an ORM auto-sync tool silently dropping hand-tuned indexes on a 98.5M-row table (`doctrine:schema:update --force` incidents), (2) treating a spinning-HDD-backed MySQL instance like it has SSD-class random IO (the twice-abandoned FULLTEXT index build), and (3) letting "duplicate work rows" and "duplicate-detection tooling" get built as one entangled unit instead of a schema fix now + a population job later.

## Phase 0 — Foundation decisions (DONE as of 2026-08-20)

**Repo/location**: `v2/` folder at repo root, own `package.json`/Next.js app.

**Design system**: Tailwind CSS + shadcn/ui, custom editorial identity (Fraunces display serif + Geist body sans, warm terracotta accent `oklch(0.58 0.16 42)` in light / `oklch(0.68 0.17 45)` in dark, warm paper background not pure white) — deliberately zero visual continuity with v1's antd UI. Dark mode via `next-themes`, class-based. See `src/app/globals.css` for tokens, `src/components/dklist/` for the core primitives (`book-cover.tsx` renders a typeset "jacket" — no photographic covers, deliberate design choice for visual consistency).

**Next.js hosting**: will run as a long-lived Node process (`next start`) on the VPS, not Vercel serverless — needed for a real DB connection pool and eventual realtime chat. Not yet deployed there.

**Reverse proxy**: Caddy in front of v1 and v2 containers on the VPS, plain-HTTP only (Cloudflare terminates TLS at the edge already). Config prepared, not yet applied — needs the maintainer's go-ahead since it's a live-server change.

**Database strategy**:
- `drizzle-kit pull` introspection snapshot of the live prod schema, manually verified against `SHOW CREATE TABLE` for core tables (`book`, `writer`, `category`, `user`) — confirmed faithful (indexes, composite column order, types all matched). One real gap found and fixed: drizzle-kit's generated import statement omitted `tinyint` despite using it.
- **No `drizzle-kit push`, ever.** Future schema changes are hand-written `ALTER TABLE` + manually-edited `src/db/schema.ts`. Don't trust `drizzle-kit generate`'s diff engine either.
- Local dev DB is a **separate MySQL 8.4 instance the maintainer runs on their own machine** (schema-only, seeded with small fake data) — not available to cloud sessions. See the cloud-agent constraints section above.
- Known real data-quality quirk confirmed via the live schema: `writer.view_count` is a `VARCHAR(255)`, not an integer — worth a real-typed fix eventually, not attempted yet.

**Auth — DONE**: `next-auth@5.0.0-beta.32` (`src/auth.ts`) + `bcryptjs`. Faithfully ported v1's exact `UserController::login` semantics: bcrypt verify, a legacy-plaintext-password fallback-and-upgrade path (real v1 accounts predating the bcrypt migration may still have plaintext passwords — this is confirmed real, not hypothetical, matches a comment in the original PHP), identical generic error for both "no such user" and "wrong password" (user-enumeration protection), `disable`d-account rejection. JWT session strategy, no DB adapter (Auth.js's adapter table conventions don't match this schema). Required `trustHost: true` since this app self-hosts (Auth.js only auto-trusts Vercel's host header by default) — a real gotcha, without it every sign-in silently fails with `UntrustedHost`. Verified end-to-end against three seeded test accounts. Header shows avatar/username/logout when signed in, isolated in its own `<Suspense>` boundary so the rest of the page still prerenders statically.

**Cache Components (Next.js 16's current caching model)**: `cacheComponents: true` already set in `next.config.ts`. Use `'use cache'` + `cacheLife()` + `cacheTag()` on Drizzle data-access functions — this is both the ISR mechanism and the tag-based invalidation mechanism in one API. Do NOT use `unstable_cache` or route-segment `revalidate` config — those are the pre-16 model this app isn't using. Runtime-dependent reads (`cookies()`/`headers()`/`searchParams`) must be wrapped in `<Suspense>`, not left to opt the whole route dynamic — Next validates this at build time and fails loudly if a route can't produce a static shell, which is useful, not just a lint nit.

**Verified hot-path query pattern**: `getBooksByCategory()` in `src/db/queries/books.ts` uses a raw `STRAIGHT_JOIN ... FORCE INDEX (idx_book_viewcount) ... WHERE EXISTS (...)` pattern (via Drizzle's `sql` escape hatch, not the query builder) — confirmed via `EXPLAIN` against the real 98.5M-row prod table that this avoids a filesort. Any new "filter by X, sort by Y" query on `book` should follow this same pattern, not the plain query builder, until proven otherwise on a real `EXPLAIN`.

**Performance patterns to use deliberately**:
- `'use cache'` + `cacheLife` + `cacheTag` on expensive queries.
- **Keyset/cursor pagination** (`WHERE (sort_col, id) > (?, ?) ORDER BY ... LIMIT N`), never OFFSET, for anything over the book/writer/publisher tables — OFFSET pagination past ~200K rows hung indefinitely in v1.
- `generateStaticParams` for only a small hot set (top-N popular books) + `dynamicParams: true` for the long tail — never attempt to statically prerender all 98.5M book pages.
- Don't oversell Suspense/streaming as a performance fix — it improves perceived latency only, not DB load. Edge runtime is a real footgun here (mysql2 needs the Node runtime, not Edge).

## Phased roadmap

**Phase 1 — Core catalog + canonical work/edition schema (schema only, not bulk population).** Book/author/publisher/translator/category browse, search (prefix-only `LIKE`, do not attempt FULLTEXT — twice abandoned on this HDD-backed instance), book detail pages (done — see `src/app/kitap/[slug]/page.tsx`: cover, categories, writers, translators, publisher, rating, like, read-status + drop stats, library toggle, readers-of-book, comments with 2-level reply threading), cover-image proxy (port v1's design: never expose the raw Open Library/Amazon origin URL). Introduce the `work`/`edition` split now — `edition.work_id` FK, ratings/comments FK'd to `work_id` not `edition_id` — plus a minimal admin manual-merge tool (port the transactional reassign-then-delete pattern from v1's `EntityMerger`). Get this schema decision right now; retrofitting later is expensive.

**Comment reply threading (done 2026-08-21)**: ported v1's `sub_comment` table (generic `parentType`/`parentId` pointing at either a `comment` or another `subComment`) as 2-level nested replies on the book page, matching the exact depth v1's own `CommentController::getComments()` ever rendered (it doesn't recurse past two levels either, even though the schema would allow deeper chains). Found and fixed a real bug during testing: `addBookComment`/`addSubComment` didn't return the newly-inserted row's id, so an optimistically-rendered comment/reply kept a temporary negative placeholder id — replying to it immediately (before a page reload) inserted a `sub_comment` row with a garbage `parent_id` that could never match anything. Fixed by returning `result.insertId` from both insert functions (same `[result] = await db.insert(...)` pattern used in `scripts/seed-shadow.mts`) and using the real id in the optimistic UI state. Verified via Playwright: post comment → reply → nested reply, full page reload, confirmed all three persist with correct `parent_type`/`parent_id` chains via direct DB read. Comment *likes* (`comment_like` table, separate from book likes) intentionally not built yet — lower priority, needs its own notification wiring to match v1, deferred.

**Reading goal / "okuma hedefi" (done 2026-08-21, v1-parity audit extended to `ProfileController`)**: ported v1's annual reading-goal feature (`read_purpose` table + `Read.year`) — set/edit a numeric target for the current year, shown as a progress fraction/percentage against books actually marked "okudum" that year, plus a compact past-years summary. `read_purpose` has no `UNIQUE(owner_id, year)` constraint in the real prod schema, so `setReadingGoal()` does a manual find-then-update-or-insert (matching what v1's own `setReadPurpose()` does) rather than `onDuplicateKeyUpdate`, which would have silently inserted duplicate rows. Verified via Playwright + direct DB check (seeded 2 "okudum" rows, set a goal of 5, confirmed "2/5 (%40)" both immediately after saving and after a full reload).

**Writer/translator likes (done 2026-08-21)**: extended the Like feature (previously book-only) to writers and translators, matching v1's `User::$likedWriters`/`$likedTranslators` (`user_writer`/`user_translator` tables). Replaced a dead, non-functional "Takip Et" button stub on the writer page (it rendered but had no click handler or backing action) with a real like toggle; added the same to the translator page, which had no button at all. Profile page now shows "Beğenilen Yazarlar"/"Beğenilen Çevirmenler" sections (v1's `liked.writers`/`liked.translators`), linked to the respective detail pages. Verified via Playwright: like a writer, count and heart-fill state persist across a full reload, and the writer shows up in the profile's liked-writers section.

**Audit extended to WriterController/PublisherController/GeneralController (2026-08-21)**: `PublisherController` has no rating/comment/like system in v1 at all (confirmed by reading the full controller) - v2's publisher page (book listing only) is already at parity, nothing to port there. `WriterController::getWriter()` and (by direct code-mirror) `TranslatorController` do have a Score-based rating system separate from the Like feature added earlier - generalized `src/db/queries/rating.ts` (previously book-only) to `rateWriter`/`rateTranslator`, each updating the denormalized `writer.score`/`translator.score` column exactly like `rateBook` does for `book.score`. New shared `RateEntityControl` client component (parameterized over a pre-bound server action via `.bind()`, since Next.js Server Actions support partial application across the server/client boundary). Verified via Playwright + direct DB read: rated a writer 4/5, confirmed both the `score` row and the denormalized `writer.score` column updated correctly. `GeneralController::search()` covers five categories (books, writers, translators, publishers, users) but v2's `/ara` and header search only ever covered books - extended `src/db/queries/search.ts` with `searchWriters`/`searchTranslators`/`searchPublishers`/`searchUsers` (same prefix-only `LIKE` pattern) and the `/ara` page now renders all five as separate sections. Verified via Playwright: writer and user results render with correct links.

Comment threading for writer/translator (v1's `WriterController::getWriter()` also nests comments/subComments exactly like the book page) is a real remaining gap, deferred - the comments.ts functions are currently book-specific (`BOOK_TYPE` hardcoded); generalizing them the same way rating/likes were generalized is the next natural step. Also noted, not yet built: a profile-edit form (`ProfileController::editProfile`/`updatePicture` - users can't edit their own bio/avatar/etc. in v2), the homepage's "featured"/"picks" sections still render `demoBooks` placeholder data instead of real queries (`getTopBooks`/`getTopWriters`/`getTopBlogs` from `GeneralController::getTopItems()` have no v2 equivalent yet), and v1's site-wide category nav widget (top-50-by-book-count, file-cached because a live `GROUP BY` over 50M `book_category` rows is expensive) has no v2 equivalent - `/kategori/[slug]` pages exist but nothing links to them from global nav yet.

**Comment threading generalized to writer/translator (done 2026-08-21)**: `comments.ts`'s `getBookComments`/`addBookComment` were hardcoded to `type='book'` even though the underlying `comment` table is already generic (`type` + `target_id`) - matches v1's `WriterController::getWriter()`, which nests comments/subComments on the writer page exactly like `BookController::getBook()` does. Refactored to `getEntityComments`/`addEntityComment` (parameterized over `CommentTargetType = "book"|"writer"|"translator"`), with `getBookComments`/`addBookComment` kept as thin wrappers so the book page's existing call sites didn't need to change. Extracted `BookComments` into a generic `EntityComments` component (parameterized over pre-bound server actions, same `.bind()` pattern as `RateEntityControl`/`EntityLikeButton`) and deleted the now-unused book-specific file. Writer and translator pages both got a full "Yorumlar" section with 2-level reply threading. Verified via Playwright: a regression check that book comments still work after the refactor, plus a new writer comment + reply, both confirmed via direct DB read (`comment.type='writer'`, correct `target_id`) and persistence across a full reload.

**Category discovery widget + Turkish category names (done 2026-08-21, closes the CategoryController audit item)**: found two real gaps by reading `CategoryController::getAllCategoriesForClient()`. First, the imported category taxonomy is US/Library-of-Congress subject headings, not real Turkish text (true even for `lang='tr'` books, per v1's own comment) - v1 hand-translates the ~50 most-common ones; v2's `getCategoryBySlug()` was rendering the raw English headings directly. Ported the translation table verbatim into `src/lib/category-names.ts` (`translateCategoryName()`), applied it in `getCategoryBySlug()`. Second, v1 has a site-wide "top 50 categories by book count" nav widget - expensive on the real 50M-row `book_category` table, so v1 resorted to a 24h file cache with a `flock()` stampede guard; v2 had no equivalent at all. Added `getTopCategories()` (`'use cache'` + `cacheLife('days')` is the Cache Components equivalent of that file cache, no hand-rolled locking needed) and a "Kategoriler" pill-link section on the homepage. Verified via Playwright + screenshot: renders correctly, links to real `/kategori/[slug]` pages, matches the site's visual design.

Also noted, not fixed this pass: `SiteHeader`'s "Keşfet"/"Yazarlar"/"Askıda Kitap"/"Bloglar" nav links are all `href="#"` placeholders - "Askıda Kitap"/"Bloglar" are legitimately unbuilt (Phase 3/4), but "Yazarlar" would need a writers-index/browse page that doesn't exist yet either (v2 only has `/yazar/[slug]` detail pages so far).

**Homepage wired to real data (done 2026-08-21)**: the "featured"/"picks" sections previously rendered hand-written placeholder `demoBooks` data unconditionally, regardless of what was actually in the catalog. Added `getTopBooks()` to `books.ts` (v1's `GeneralController::getTopItems()`/`getTopBooks()` - top-N by view count) and rewired the homepage to it as an async streamed section (`FeaturedSection`, matching the `CategoriesShelf`/`LatestBooksShelf` Suspense pattern already used elsewhere). Dropped the invented "genre"/editor's-pick framing (no such concept exists in v1 or the schema) in favor of "En çok görüntülenen" (most-viewed), and used the real `book.content` column as the blurb instead of hand-written demo excerpt text. Verified via Playwright + screenshot against the real seeded shadow DB - shows the actual highest-view-count book with its real writer, score, and view count. `demoBooks`/`demo-books.ts` is still used by the decorative `HeroShelf` (a visual book-stack graphic, not a data section) - left alone deliberately.

**Profile-edit form (done 2026-08-21)**: ported v1's `ProfileController::editProfile()` - users previously had no way to edit their own bio/city/job/birthdate/etc. in v2 at all. New `/profil/duzenle` page (name/surname/sex/birthDate required, matching v1's validation; birthPlace/livingCity/edu/job/biyo optional; password optional and only updated when actually provided - v1's own code comment notes this used to force resubmitting the current password on every edit, a UX/security mistake already fixed on the v1 side and matched here from the start, not reintroduced). `updateProfile()` in `profile.ts` hashes a new password with bcrypt when provided. A "Profili düzenle" link appears only on the viewer's own profile page. Verified via Playwright + direct DB read: updated name/surname/sex/birthdate/city/bio, confirmed all fields persisted correctly and the public profile page shows the updated bio immediately after redirect (cache tag invalidation confirmed correct via a direct curl re-check, after an initial Playwright false-negative that turned out to be a test-timing artifact, not a real bug). Avatar upload (`updatePicture`) deliberately not built yet - no image-upload infrastructure exists anywhere in v2 currently, a separate piece of work.

**Notifications (done 2026-08-21, closes the last open Phase 2 item)**: found that `NoticeController`/`Notice` is actually a moderation "şikayet" (report) queue for admins/mods, a completely different feature from user-facing notifications - the real equivalent is `NotificationController` + the `DKNotifiaction` entity + `NotifyManager::addNotification()`. Built `src/db/queries/notifications.ts`: `addNotification()` (dedupes on owner+sender+identical message, blocks self-notification, exactly matching v1's `NotifyManager`), `getNotifications`, `getUnreadNotificationCount`, `markAllNotificationsRead`, `deleteNotification`. Added a bell icon with an unread-count badge to `SiteHeader` (own `<Suspense>` boundary, same pattern as `AuthStatus`) and a `/bildirimler` page (list, mark-all-read, per-item delete). Wired the follow action (`toggleFollow` in `profile.ts`) end-to-end as the first real trigger - matches v1's `ProfileController::followSwitcher()`, which only notifies on a new follow, never on unfollow. Verified via Playwright with two real test accounts: follow → bell badge appears for the followed user → notification text and sender-profile link render correctly on `/bildirimler` → mark-all-read clears the badge.

Not yet wired (documented, not silently dropped): the other v1 trigger points that call `NotifyManager::addNotification()` - comment likes (`CommentController::like`), new top-level comments/replies notifying the target's owner (`CommentController::add`/`addSubcomment`), and moderator alerts for user reports (`NoticeController::reportUser`, a distinct concern from this feature). Each is a small, mechanical addition once you're looking for it (call `addNotification()` at the point of action, same as `toggleFollow` now does) - a natural fast-follow, not a redesign.

**Comment likes + "alıntı" (quote) UI (done 2026-08-21)**: closes two of the three remaining smaller gaps from the notifications pass. (1) Comment likes: `src/db/queries/comment-likes.ts` on the separate `comment_like` table (distinct from the book/writer/translator likes built earlier) - `getCommentLikeCounts`/`getLikedCommentIds`/`getCommentLikeStates`/`toggleCommentLike`, wired into `EntityComments` as a heart button per comment, shared across all three pages via one new `src/actions/comment-likes.ts` server action (not bound per-page like the others, since comment likes aren't scoped to any one entity type). This is also the one real `NotifyManager` trigger in `CommentController` - v1's `add()`/`addSubcomment()` (new comments/replies) never notify anyone, only `like()` does - confirmed by re-reading the controller rather than assuming, and wired accordingly (liking a comment notifies its owner, deduped same as follow). (2) "Alıntı" (quote): the customer's notes were explicit that "yorum yap" and "alıntı yap" should read as clearly separate things, not blended into one feed like v1 did - `EntityComments` already took a `commentType`-scoped `addCommentAction`, so each of the three entity pages now renders it twice (once per `commentType`) under separate "Yorumlar"/"Alıntılar" headings, sharing 100% of the underlying component/query code. All three pieces verified via Playwright + direct DB read: comment-like toggles and persists, the like notification appears on `/bildirimler` for the comment owner, and a "yorum" vs an "alıntı" posted on the same page land in the correct separate section with the correct `comment_type` column value.

**Badges display + followers/following lists + one live nav link (done 2026-08-21)**: closes 2 more of the small polish gaps. Badges: `getUserBadges()` renders v1's `ProfileController::getProfile()` badges array (name/comment) as pills on the profile page - read-only display only, badge *assignment* stays a separate admin/automated concern not built here. Badge (and avatar) *images* aren't rendered because v2 has no generic image-serving path yet (only the book-cover proxy exists) - shown as text pills with a tooltip instead of silently dropping the feature, consistent with the already-documented avatar-upload gap. Followers/following: v1's `getProfile()` returns full arrays, not just counts - added `getFollowersList`/`getFollowingList` plus dedicated `/profil/[username]/takipciler` and `/profil/[username]/takip-edilenler` pages (shared `FollowList` component), linked from the profile page's follower/following counts. Also pointed `SiteHeader`'s "Keşfet" nav link at the real `/ara` search page instead of leaving it dead - "Yazarlar"/"Askıda Kitap"/"Bloglar" stay dead deliberately (each needs a page that doesn't exist yet, not a wrong destination). All verified via Playwright + direct DB seed/check.

**Registration + password-reset built (done 2026-08-21) - closes the major gap found last stretch.** Ported `UserController::register()`/`authAccount()`/`resetPasswordRequest()`/`resetPassword()` into `src/db/queries/auth-account.ts`: `registerUser()` (required-field validation + username/mail uniqueness, matching v1 exactly - v1's `DirtyController` profanity filter is NOT ported, a real but lower-priority gap), `verifyMailCode()`, `requestPasswordReset()`, `confirmPasswordReset()` (v1's own unusual design: generates and returns a random *new* password rather than letting the user choose one - matched as-is). New pages: `/kayit-ol` (register, auto-signs-in on success via Auth.js), `/dogrula` (email verification), `/sifremi-unuttum` + `/sifre-sifirla` (forgot/reset password). Header's "Üye Ol" button and `/giris`'s "şifremi unuttum" link now both go somewhere real.

**Deliberate scope decision on email**: v1 emails the verification/reset code via `MyMailer`/Brevo SMTP, but the real Brevo credentials are a **production secret** (documented in the maintainer's local memory, tied to the live dklist.com sending domain) - not something to pull into this dev session's `.env.local` unasked, and this local `backend/.env`'s own `MAILER_DSN` is `null://null` anyway (mail sending is a no-op even in the reference PHP app locally). Real email delivery is deliberately deferred: the verification/reset code is shown directly on the resulting page instead (clearly labeled "e-posta gönderimi henüz bağlanmadı"), so the whole flow is still genuinely testable end-to-end rather than silently broken or half-built. Wiring real email is a explicit follow-up decision for the maintainer, not an oversight.

**Also found and left as-is (documented, not a bug)**: v2's `authorize()` (Auth.js Credentials provider) does not hard-gate login on `mailAuth`, unlike v1 (which nulls out the bearer token for unverified accounts, effectively locking out API calls). Changing this now would re-couple login to working email delivery, which isn't wired up - kept permissive deliberately so registration unblocks real usage immediately; revisit if/when real email is wired.

Verified end-to-end via Playwright + direct DB reads: registered a brand-new account, confirmed auto-sign-in, verified the mail code, confirmed `mail_auth=1`/`pending_code=NULL` in the DB; requested a password reset for an existing test account, confirmed the reset code + new random password flow, logged in with the new password successfully.

**DirtyController profanity filter + self-service account deletion audit + admin-CRUD deferral confirmed (done 2026-08-21)**: `DirtyController` (a fixed word-list exact-match filter, not an API) is used ONLY on admin-only "add new entity" flows (Book/Writer/Publisher/Translator/Category/Store/Blog/Badge/Youtube) plus `UserController::register()` - confirmed by grepping every real call site; `CommentController` never uses it, so comments/quotes correctly do NOT get this filter (a wrong assumption corrected before implementing, not after). Ported faithfully (exact-match quirk included, not "improved") as `src/lib/dirty-controller.ts`, wired into `registerUser()` only, since that's the only currently-built v2 flow that needs it. Verified via Playwright: registering with a dirty username is rejected with v1's exact message, a clean username still succeeds.

Also checked: v1 has no self-service "delete my own account" endpoint anywhere - `UserController::deleteUserAdmin()` requires `Permission` with an empty `accessUserTypes` array, which (per `Permission::checkPermission()`) only ever passes for `SuperAdmin`. This is a real gap in v1 itself, not something v2 uniquely lacks - v2 is already at parity by not having it either. And re-read the `project-dklist-v2-rewrite-requirements` memory to confirm the customer's "only admin can add data" complaint is specifically about the new Yazar/Yayınevi/Kütüphaneci role hierarchy getting data-entry rights (explicitly Phase 4 work), not a request for bare admin CRUD now - building unguarded admin add/update endpoints today wouldn't actually address that complaint, so Writer/Publisher/Category/Book admin CRUD stays correctly deferred to Phase 4, now confirmed rather than assumed.

**Avatar upload built (done 2026-08-21) - closes the last remaining substantial Phase 1/2 gap.** v1's `ImageManager` uses Cloudinary if configured, otherwise falls back to a local `/uploads` directory + `/image/{name}` route - confirmed the reference backend's own `.env` has all `CLOUDINARY_*` vars empty, so local disk is the actually-exercised path, not Cloudinary. Matched that (not a new paid service, consistent with the standing constraint): `src/db/queries/avatar.ts` validates the real file content via PNG/JPEG/WEBP magic-byte signatures (Node has no `getimagesize()` equivalent, so this replaces it rather than trusting the client-supplied extension, same spirit as v1's own comment on why it checks real content), saves to a persistent `v2/uploads/avatars/` directory (gitignored, unlike the book-cover proxy's disposable `os.tmpdir()` cache - losing a user's real uploaded avatar would be actual data loss), and updates `user.image`. Served via `/api/avatar/[filename]/route.ts` (same `basename()` path-traversal guard as v1's `ImageManager::getImagePath()`). Wired into `/profil/duzenle` (file input, live preview) and the avatar now renders for real everywhere `user.image` is shown (profile page, header) instead of always falling back to initials. Verified via Playwright: uploaded a real PNG, confirmed it persists to disk, `user.image` updated in the DB, and the served URL returns the correct bytes with the right `Content-Type`.

**MessageController/StoreController checked (2026-08-21)**: both are pure chat/marketplace functionality (`sendMessage`/`getConversations`/`getMessages` and `Store` add/update/favorite/etc.) - v2 has no chat or marketplace UI at all yet, so these aren't v1-parity gaps in something already built, they're net-new Phase 3 work exactly as already scoped in this plan. Confirmed rather than assumed; no action taken here, Phase 3 starts this work properly when reached.

**`original_book_id` reconciliation (found 2026-08-21, unresolved)**: a read-only prod query confirmed v1's `book.original_book_id` self-referencing FK (its edition-grouping mechanism, used by `BookController::getBook()`'s "diğer baskılar" section) is populated on 54.5M of 97.7M books (~55.7%) — real, load-bearing data, not a mostly-empty legacy column. The new `work`/`work_id` schema (migration `0001_work_edition_split.sql`) was designed without accounting for this. Before Phase 5's backfill job is built, decide: either (a) treat `original_book_id` chains as a direct seed for `work_id` grouping (a book and its `original_book_id` target should end up in the same `work`), or (b) run it through the same fuzzy-matching pipeline as everything else and let it re-derive the same groupings as a validation check. Leaning (a) since it's already-curated human/admin data, cheaper and more trustworthy than re-deriving — but this needs a deliberate decision before the backfill starts, not an assumption baked in silently.

**Phase 2 — Accounts & social.** Profile, reading status including a new "yarıda bıraktım" (dropped) status with a structured reason + drop-off %, explicit split of "kitaplığım" (ownership) from reading status, follow, ratings/comments/quotes (reorganized into clearly separated views per customer feedback), notifications. Start capturing activity events (status changes, ratings, shelf adds) with timestamps here, even though the Wrapped-style annual report isn't built until Phase 6.

**Phase 3 — Messaging & marketplace.** Chat including a "diğer mesajlar" (message requests) inbox, Askıda Kitap/marketplace with Iyzico, per-book secondhand-listing links. Minimal "listing requires admin-approved flag" gate even before the full role hierarchy exists.

**Progress (2026-08-21) - chat/messaging started.** Ported `MessageController`/`MessageService` (`chat`/`message` tables, both already introspected): `src/db/queries/messages.ts` (`getConversations`, `getUnreadMessageCount`, `getMessages` with cursor pagination + mark-as-read side effect, `sendMessage`, `deleteMessage`/`deleteChat` - both soft-hide, matching v1's own fix for "deleting a message used to remove it for both sides"). New `/mesajlar` page (two-pane conversation list + thread), a header message-bell (mirrors the notification-bell pattern), and a "Mesaj Yaz" link on other users' profile pages. Text messages only for now - v1's book/store-attachment message types are deliberately not ported since v2 has no marketplace/store to attach yet (that's the marketplace half of this phase, not started). No websocket/push infra (matches v1, which also has no real-time push - its own frontend does visibility-aware polling); the thread view polls every 5s while the tab is visible, same approach. Verified via Playwright with two real accounts end-to-end: send → unread badge appears for the recipient → thread renders correctly → reply → notification created for the original sender → all confirmed against direct DB state (`chat.first_user_id`/`hidden_for_*`, `message.view`/`view2` flip correctly depending on who's reading).

**Delete UI wired in (done 2026-08-21)**: `deleteMessageAction`/`deleteChatAction` existed in the query/action layer from the first chat pass but weren't reachable from any UI - added a per-message delete button (`message-thread.tsx`, own messages only) and a per-conversation delete button (new `ConversationItem` client component, replaces the plain `Link` in the conversation list). Verified via Playwright + direct DB reads, isolated per-action (an earlier combined test run gave a misleading DB read because sending a *new* message legitimately un-hides a chat for both sides again - that's correct by design, matching v1, not a bug - confirmed by re-testing each action alone): deleting a message sets `hidden_for_sender` only for the actor; deleting a conversation sets `hidden_for_first_user`/`hidden_for_second_user` only for the acting side, and the other party's view (conversation + message) is completely unaffected either way.

**Askıda Kitap (marketplace) started (done 2026-08-21)**: re-reading `StoreController.php` in full found that *every one of its own methods* is actually payment-free - creating a **free** listing needs no Iyzico at all (only a *paid* listing checks a simple `MarketplaceSettings.isActive` admin toggle, not an actual payment call). The real Iyzico dependency lives entirely in the separate `StoreOrderController`/`IyzicoWebhookController` (buying a paid listing), not touched here - same "split the payment-free part out" pattern that worked for chat/marketplace and blog-read/blog-write. Built the full free-listing flow: `src/db/queries/store.ts` (create with photo upload reusing a newly-extracted shared `src/lib/image-upload.ts` helper, browse list, detail, favorite toggle, status update, delete), `/askida-kitap` (browse), `/askida-kitap/[slug]` (detail + favorite + owner controls), `/askida-kitap/yeni` (create form, free-only - no price field, since paid needs the still-unbuilt admin toggle + checkout). `SiteHeader`'s "Askıda Kitap" link now points at a real page.

**Real bug found and fixed during verification**: `store_picture.advert_id` has no `ON DELETE CASCADE` at the DB level (unlike `store_favorite`, which does) - v1's Doctrine ORM cascades this at the application layer via the entity relationship, which a raw Drizzle `db.delete(store)` doesn't get for free. A bare delete failed against a real FK constraint violation whenever the listing had a picture row (every real listing does) - `deleteStore()` now deletes `store_picture` rows first. Also fixed a related UX bug caught by the same test: `StoreOwnerActions`'s delete button navigated away unconditionally even when the delete failed, silently hiding the error - now checks the result and only navigates on success, showing the error otherwise.

Verified end-to-end via Playwright + direct DB/filesystem checks: created a real listing with an uploaded photo, confirmed the file on disk and the DB rows, browsed it in the list, favorited it as a second account (persists across reload), marked it completed, deleted it, confirmed via both direct DB query and a fresh `curl` fetch (not a client-side navigation, to rule out Next.js Router Cache staleness after the earlier false alarm) that it was actually gone.

**"İlanlarım"/"Favorilerim" wired (done 2026-08-21)**: `getMyStores()` (already existed) and a new `getMyFavoriteStores()` (filters out listings the owner has since made inactive, matching v1's `getFavorites()`) now back real `/ilanlarim` and `/favorilerim` pages, linked from the viewer's own profile page. Verified via Playwright with two accounts: create a listing → appears in the owner's "İlanlarım" → a second account favorites it → appears in that account's "Favorilerim".

**Top readers widget (done 2026-08-21)**: `UserController::getTopUsers()` - top-N users by total `read` row count (any status) - ported as `getTopReaders()` in `profile.ts`, shown as an "En Aktif Okurlar" section on the homepage (v1 has no dedicated page for this either, just a widget). Verified via Playwright + direct DB seed: correct counts and ranking shown, linked to each user's profile. Same "build ran before seed" cache-staleness lesson as the blog pass recurred here (`'use cache'`/`cacheLife('hours')` baked in an empty result from before the read rows existed) - rebuilding after seeding fixed it, worth remembering as standing Playwright-verification practice: seed before building whenever a `'use cache'` query is involved.

Not yet done in Phase 3: the actual paid-listing + Iyzico checkout flow (`StoreOrderController`/`IyzicoWebhookController`, needs real payment credentials - same "don't pull production secrets into dev unasked" caution that applied to email). Book/store message-attachment types in chat (`MessageTypeEnum::BookAttachment`/`StoreAttachment`) were considered this pass - both now have real detail pages to attach, so the dependency is gone, but building a genuine attachment picker (search-and-select UI inside the chat composer, rendering a rich preview card) is real scope, not a small addition; deliberately not rushed half-built this stretch. Recommended approach for next time: a "Paylaş" button on the book/store detail page (mirroring the existing "Mesaj Yaz" profile-page pattern) that opens `/mesajlar` pre-loaded with that attachment, rather than building an in-composer picker from scratch. Also still open: whether a distinct "message requests" inbox split is a real v1 behavior or just customer-requested territory (v1's own `getConversations()` doesn't implement one either - worth re-checking before building one).

**Phase 4 — Admin roles, permissions, blog.** Full role hierarchy (Kurucu — un-revocable, Admin, Yazar üye, Yayınevi üye, Kütüphaneci/Moderatör with data-entry+approval rights but no site-settings access, Member), audit log for elevated-permission actions, blog with revision-approval flow. Book-entry approval workflow.

**Read-only blog pages built (done 2026-08-21)**: `src/db/queries/blog.ts` ports `BlogController::getAll()`'s public branch (approved-only, skips posts whose owner is unverified/disabled) and `get($slug)` - which notably does **not** filter by `approved` at all, only the list endpoint does, so a direct link to an unapproved post still works in v1; matched as-is rather than "fixed", since the task is parity, not redesign. New `/bloglar` (list) and `/blog/[slug]` (detail) pages, `SiteHeader`'s "Bloglar" nav link now points at a real page. Blog *images* aren't rendered - same reasoning as avatar/badge images earlier, but here there's not even a write path yet to produce one (posting needs Phase 4's role hierarchy), so there's nothing real to serve. Post-authoring/editing/approval UI deliberately not built - confirmed `add()` requires the `Blogger`/`Mod`/`Admin` usertype, which v2 has no concept of.

Verified via Playwright + direct DB seed: an approved post appears in the list and its detail page renders correctly; an unapproved post is absent from the list but still reachable by direct link (matching v1's actual behavior). One real lesson relearned during verification, not a product bug: `npm run build` had run *before* the SQL seed, so Cache Components' `'use cache'` static shell baked in an empty result at build time (`cacheLife('minutes')` then served that stale empty page for the first ~60s) - a rebuild after seeding fixed it. Worth remembering for any future Playwright pass against a freshly-seeded table: seed before building, or expect a brief stale window.

**Phase 4 started (done 2026-08-21) - first real permission check.** Re-read `UserTypeEnum`/`Permission.php`: v1's role check is "SuperAdmin always passes, everyone else must be explicitly listed" - built `src/lib/permission.ts` (`hasRole()`/`requireRole()`) as the one place that rule lives for v2. Added a proper `next-auth` module augmentation (`src/types/next-auth.d.ts`) for `session.user.userType`/`mailAuth`, replacing the ad-hoc type casts in `auth.ts` from earlier in the session. Wired into `/admin/merge` - the work/edition merge tool, previously gated only on "is signed in", **not any real role**. Real gap found and fixed: the gate was only ever on the *page*, never on the *Server Action* (`merge()`) itself - Server Actions are their own reachable HTTP endpoint independent of how the referencing page renders, so page-only gating never actually protected the mutation; a request straight to the action's endpoint would have bypassed it entirely. Both the page and the action now call `requireRole([Admin, Mod])`. Verified via Playwright with two real accounts: a Member-type account is redirected away from the page entirely; an Admin-type account can view the form and complete a real merge (confirmed the duplicate `work` row was actually deleted via direct DB read).

Deliberately NOT attempted this pass (matches the "one real gap at a time" scope, not the full role hierarchy): new role types beyond what v1 already has (Kurucu/Yazar üye/Yayınevi üye/Kütüphaneci as un-revocable/data-entry roles are customer-requested *additions*, not a v1 port), an admin panel/dashboard, or gating any other endpoint - `/admin/merge` was the only page that had zero real permission check, everything else built this session either has no elevated-permission requirement in v1 (comments, likes, etc.) or is explicitly deferred blog/marketplace-admin work already noted elsewhere in this file.

**Phase 5 started (done 2026-08-21): duplicate-detection primitives.** With Phase 3/4's real-v1-parity surface exhausted (see below), moved to the next roadmap item that doesn't need a scope decision: the classical fuzzy-matching stages of the future `work_id` backfill. Built `src/lib/duplicate-detection.ts` - `normalizeTitle()` (strips edition/print-run noise like "Cilt 2"/"Özel Baskı" so different editions of the same work key the same), `stringSimilarity()` (Jaro-Winkler), `titleAuthorSimilarity()` (combines both, treating whole-word author-name containment like "Dostoyevski" vs "Fyodor Dostoyevski" as a near-match rather than scoring it poorly via raw string distance). Verified against known input/output pairs. **Two follow-ups the same day, still 2026-08-21:**

1. **Staging-table shortcut investigated and ruled out.** The introspected schema carries several `stg_*` tables (`stg_work_book`, `stg_writer_lookup`, `stg_publisher_lookup`, `stg_translator_lookup`, `stg_edition_cover`, `stg_work_cover`, `stg_distinct_writer`) - clear leftovers from v1's original Open-Library-style bulk import, `work_key`/`edition_key` naming strongly suggesting OL's own scheme. Hoped `stg_work_book.work_key` might already group `book_id`s by a real external "work" identifier - a much cheaper starting point than fuzzy matching. A read-only prod check found **41,229,441 rows in `stg_work_book`, and 41,229,441 distinct `work_key` values - an exact 1:1 mapping**. Every `work_key` in this table maps to exactly one book; it does NOT already group duplicate editions. Real, useful negative result - ruled out as a shortcut, not pursued further. (Also confirms `book.isbn` has no index - the same check.)
2. **Dry-run wired up and verified against real seeded data.** `findDuplicateCandidatesDryRun()` (`src/db/queries/duplicate-detection.ts`) chains the three stages - ISBN exact match, then normalized-title match over the remainder, then fuzzy title+author over what's left after that (the expensive stage only ever sees the final small remainder). Bounded to a `limit` slice, never a full scan. Verified against the real local shadow DB: seeded three genuine near-duplicate pairs (shared ISBN under different edition labels, matching normalized title after stripping "Özel Baskı" noise, a one-character fuzzy title typo under the same author) - all three surfaced in the correct stage's group, then cleaned up.

**Rating-scale bug fixed: v1 rates 1-10, not 1-5 (done 2026-08-21) - found via a real, previously undetected bug.** Auditing `OptionContainerComponent/LeftColumn.js` (v1's real rating widget) showed `<Rate count={10}>` and "X.X/10" text; `GeneralController::score()` (the shared rating endpoint) confirms it - no server-side range check beyond rejecting falsy values, the 1-10 range is enforced only by the frontend's 10-star picker. v2's entire rating system had been built 1-5 all session: validation capped at 5, both interactive star pickers only rendered 5 stars, and the read-only `StarRating` display treated input as already 0-5 with no clamping - meaning any real score above 5 would crash `"★".repeat(5 - full)` (negative repeat count), and scores 0-5 rendered as a misleadingly-positive 0-5 display. Fixed: validation now 1-10, both pickers render 10 stars, `StarRating` correctly halves a 0-10 score onto 5 glyphs, and "/10" added next to every raw score number across all book/writer/translator listing/detail pages for clarity. Verified via Playwright + DB: 10 buttons render, an 8/10 rating stores exactly 8 (not scaled), and a page reload after rating an 8 - previously guaranteed to crash - now works cleanly.

**Profile page shows blog posts (done 2026-08-21).** Another `GeneralComponents` audit find: v1's `BlogsContainer.js` shows the profile owner's blog posts to anyone viewing that profile - v2 had no equivalent. `getBlogsByOwner()` shows non-owners only approved posts, but the owner also sees their own pending drafts marked "onay bekliyor" - matches the isOwner-sees-more pattern already used for reading-status/pending-revision elsewhere. Verified via Playwright + DB with one approved + one pending post.

**Mobile bottom navigation bar built (done 2026-08-21) - a real, significant gap: v2 had no mobile nav at all.** Found while auditing v1's full `GeneralComponents` directory (`BottomNav.js`) and confirming, by reading `site-header.tsx` directly, that v2's own header nav is `hidden md:flex` with zero fallback - small-screen users had no way to reach Kitaplar/Bildirimler/Mesajlar/Profil at all below the `md` breakpoint. Mirrors v1's real item set (Ana Sayfa/Kitaplar/Bildirimler+badge/Mesajlar+badge/Profil, or Giriş Yap signed-out) reusing the same unread-count queries the header's own bell icons already use, but links to the real `/giris` page rather than v1's modal popup. Fixed at the viewport bottom, `md:hidden` (exact mirror of the header's own breakpoint), with matching body padding added so it never overlaps page content. Verified via Playwright with real mobile (390×844) and desktop (1280×800) viewports: correct visibility in both signed-in/signed-out states, completely absent on desktop, real click-through navigation confirmed.

**External social-share button built (done 2026-08-21) - confirmed missing via a repo-wide grep, not assumed.** v1's `ShareComponent.js` (Facebook/Twitter/LinkedIn/WhatsApp) is used on book pages, blog posts, Askıda Kitap listings, and individual comments - v2 had no external social-share functionality anywhere at all. Pure client-side popover, no schema/backend needed. Ports two real v1 fixes: Facebook uses `navigator.share` on mobile (the plain `sharer.php` web flow dead-ends inside Facebook's own mobile app), and WhatsApp shares just the link (not link+text, since WhatsApp already builds its own OG-tag preview card - adding text again would duplicate the content). Wired into all four real v1 usage sites. Verified via Playwright with real seeded content + `window.open` interception confirming the constructed share URLs are correct.

**Automatic point-milestone badges built (done 2026-08-21) - closes a real Phase 6 gap.** Confirmed while auditing `BadgeController` that v1's `badges`/`user_badges` tables have no write path anywhere in the real codebase - assignment is purely manual, matching Phase 6's own "replacing v1's static badge assignment" framing exactly. Four lifetime-point milestones (25/100/250/500 - Aktif Okur/Kitap Kurdu/DKList Gönüllüsü/DKList Efsanesi, fresh design), checked after every `awardPoints()` call. Badge definitions self-seed on first use (found-or-created by name, no separate seed step needed anywhere). Idempotent via `user_badges`' own composite PRIMARY KEY - a duplicate award just fails silently, same proven pattern as the points ledger. Real "yeni rozet kazandın" notification only on a genuinely new badge, reusing a newly-extracted `resolveSystemSenderId()` shared with the weekly-winner notification. Verified via Playwright + DB: crossed the 25-point threshold through real actions, confirmed badge/notification/profile-pill all appeared, then earned more points within the same tier and confirmed no duplicate.

**/kitaplar general book-catalog browse page built (done 2026-08-21) - found by auditing v1's full page-directory list.** v1's `KitaplarSayfasi.js` (-> `BookController::getAllBooksForClient()`, the real `/books` route) is a general, unfiltered "Tüm Kitaplar" catalog browse - v2 had no equivalent, only category-/publisher-filtered listings embedded in their own detail pages. Deliberately did NOT duplicate the category/publisher filtering this same v1 endpoint also supports (already covered elsewhere) - built what genuinely wasn't covered: general search+sort+pagination, plus v1's "sadece okuduklarım" (only books I've read) filter, which existed nowhere else in v2. Ports real v1 perf choices: prefix-only name/orgName search (matches the existing B-tree indexes, not FULLTEXT), and the InnoDB `TABLE_ROWS` estimate instead of `COUNT(*)` when unfiltered (book has ~98.5M rows on prod). Verified via Playwright (with fresh page contexts per check, after a reused single page/context gave misleading multiplied counts during verification - confirmed via raw curl the server output was correct all along) + direct DB reads.

**/cevirmenler + /yayinevleri browse pages built (done 2026-08-21) - same gap pattern as /yazarlar.** v1's real `CevirmenlerSayfasi.js`/`YayinEvleriSayfasi.js` frontends have search+pagination browse pages, v2 only had the singular detail pages. `getPublisherList()` ports the InnoDB `TABLE_ROWS`-estimate trick (publisher has ~4.6M rows on prod); `getTranslatorList()` deliberately does NOT (a read-only prod check found `translator` has only ~49K rows - v1's own choice not to optimize it is intentional, matched as-is). Both pages added to the header nav. Verified via Playwright + DB against real seeded data (one false alarm along the way: a stale dev-server Turbopack cache briefly showed an empty publisher list, resolved as a caching artifact via a standalone script confirming the query/data was correct, then a clean restart).

**Gamification system extended (done 2026-08-21) - more hooks, homepage widget, notification.** Four more point-earning hooks (following +1, adding to kitaplığım +1, a Blogger's post getting approved +8, listing a free book on Askıda Kitap +3), a "Bu Haftanın Lideri" homepage widget (mirrors "En Aktif Okurlar"), a lifetime-points display on the profile page, and a real "tebrikler, kazandın" notification when an admin records a weekly winner (modeled as sent "from the DKList team" - a real Admin-type account resolved dynamically, since the notification schema's `sender_user_id` is a real NOT NULL FK with no system-sender concept). Verified via Playwright + DB: follow/unfollow/follow and a real blog approval each produced exactly one ledger row, homepage/profile widgets render real data, the winner notification reaches the recipient with the right prize book name.

**Gamification/points system built (done 2026-08-21) - first customer-requested new-scope feature this session, explicitly authorized ("sisteme kullanıcıların oynayıp puan vs kazanacakları bir oyun ekleyelim... en yüksek puanı alan üyeye her hafta sistem tarafında 1 kitap hediye edilecek").** Not a v1 port - v1 has no points/leaderboard concept, so point values/rules are a fresh design, detailed judgment calls left to the builder per the customer's own "detaylarını sen düşün." Two new tables (migration `0004_gamification_points.sql`, applied to the local shadow DB only - brand-new empty tables carry none of an existing-table `ALTER`'s risk, but applying to real prod still needs the maintainer's own call): `point_transaction` (append-only ledger) and `weekly_winner` (one record per ISO week, admin picks the prize book and marks it fulfilled once actually sent - real-world fulfillment isn't something the app can automate). `awardPoints()` is idempotent by construction via `UNIQUE(user_id, reason_key)` - togglable states (read status, rating, like) use a reason_key constant per (user, target) so re-toggling never re-earns; creation events (comments) use the new row's own id, always earning. Wired into the four highest-signal actions: mark "okudum" (+10), write a comment (+5), rate something (+2, first time only), like something (+1, first time only). New public `/puan-tablosu` (current week's leaderboard + past winners, in the header nav) and Admin-only `/admin/haftalik-kazanan` (record/fulfill the week's winner). Verified via Playwright + direct DB reads: re-marking read, re-rating, and a like/unlike/like cycle each produced exactly one ledger row (anti-farming design actually confirmed, not assumed); full admin flow (Member blocked, Admin records a real winner + prize book, marks fulfilled) confirmed in DB.

**/yazarlar writers-index page built (done 2026-08-21) - the biggest gap this re-audit pass found.** The header nav's "Yazarlar" link had been a deliberate `#` placeholder ("needs a writers-index/browse page that doesn't exist yet") since early in the session - v1 has a whole real page for this with search + pagination, v2 only ever had the singular `/yazar/[slug]` detail page. Built `getWriterList()` (`src/db/queries/writers.ts`), porting `WriterController::getAllWritersForClient()` faithfully - including a real v1 performance choice worth preserving: with no search filter, the count comes from `information_schema.TABLES.TABLE_ROWS` (fast InnoDB estimate) rather than `COUNT(*)`, since `writer` has ~11.3M rows on the real prod table and an unfiltered `COUNT(*)` there would be a genuine full-table-scan cost on the same HDD-backed instance already burned by the FULLTEXT attempts. New `/yazarlar` page: search box, writer-card grid (initials-avatar fallback - `writer.img` isn't rendered, since real coverage is near-zero and building dedicated image-serving infra for it would be speculative), and page-number links capped to a small window around the current page rather than listing every one of the potentially hundreds of thousands of pages a real ~11.3M-row table would have. Header nav now points here instead of `#`. Verified via Playwright: nav link works, all seeded writers list, substring search finds the right one, click-through lands on the real detail page.

**Completeness re-audit extended to Askıda Kitap (done 2026-08-21) - a third real gap.** Same re-audit pass: v1's real `AskidaKitaplarSayfasi.js` frontend has a debounced search box, free/paid filter, and sort options (confirmed by reading the actual component), and `StoreController::getAllStore()` itself supports search/listingType/sortBy/orderBy/pagination - v2's `getStoreList()` was a flat `limit=40` call with none of that. Fixed: `getStoreList()` now takes an options object (page/pageSize/search/listingType/sortBy/orderBy/ownerId/excludeId), `/askida-kitap` gained a search box + free/ücretli filter + sort dropdown + real page numbers. Also found and ported a feature that was simply absent: v1's store detail page shows "Satıcının Diğer İlanları" (seller's other listings via `ownerId`/`excludeId`) - confirmed real by reading the detail-page component, added the same section to v2. Verified via Playwright + DB: seeded 42 listings under one seller, confirmed real pagination, title search, paid-only filter, price-sort ordering, and the other-listings widget showing the 8 newest other listings from the same seller while excluding the current one.

**Completeness re-audit (done 2026-08-21) - "eksiksiz yap" pass.** User asked explicitly to make sure nothing was left incomplete. Re-read every v1 endpoint this session actually ported and found two real gaps, both fixed: (1) `/admin/bloglar` and `/admin/bulten` had a hardcoded row cap or pagination-without-search, while v1's `BlogController::getAll()`/`NewsletterController::getAll()` both support real search + pagination - `getAdminBlogList()`/`getNewsletterSubscribers()` now do too, each admin page gained a search box, matching `/admin/sikayetler`'s already-correct pattern. (2) The **public** `/bloglar` page was a flat top-20 list with no search at all - re-reading v1's actual frontend (`BloglarSayfasi.js`) confirmed the real public page has a genuine search box and page numbers, not an infinite feed. `getBlogList()` now takes page/pageSize/search too. Both verified via Playwright + DB with seeded data forcing a real second page and a uniquely-named row to search for. No new production-facing scope decisions were needed for either fix - both are "the port wasn't as complete as the feature it was porting," not new features.

**Resumable/checkpointed batch runner built (done 2026-08-21).** `src/lib/resumable-batch.ts` (`runResumableBatch()`) - the piece this Phase 5 section already called out as needed before any real backfill: batched, resumable, idempotent, checkpointed by last-processed id. Deliberately domain-agnostic (doesn't know about `book`/`work_id`, doesn't touch prod, doesn't persist its own cursor - the caller does, once the real backfill job exists). Verified against the real local shadow DB with a genuine interrupt-and-resume: seeded 23 rows, ran 2 batches then stopped, resumed from the saved cursor, confirmed the rest processed with no duplicates/gaps/reordering; separately simulated a crash partway through a single batch and confirmed retrying with the same pre-batch cursor cleanly reprocesses that whole batch rather than skipping anything.

**Still deliberately not run against the real ~98.5M-row prod table** - the missing `isbn` index means even the cheapest stage would be a full scan on the same HDD-backed instance that killed the FULLTEXT attempts twice before. The actual bulk dry-run needs its own indexing + batching design pass, not something to rush into inside an unrelated session - flagged here as the natural next Phase 5 step, not done.

**Chat book/store attachments + "Paylaş" share button built (done 2026-08-21) - closes the last deferred Phase 3 chat item.** Ports `MessageTypeEnum::BookAttachment`/`StoreAttachment` (deferred when chat was first built, since v2 had no marketplace to attach from yet - both book pages and Askıda Kitap listings exist now). `sendMessage()` builds a denormalized attachment snapshot at send-time (name/slug/cover-or-listing-photo/price), matching `MessageService::send()` - an old message's card keeps showing the book/listing as it was when shared even if it's later edited or removed, since it's not a live join. Used the lighter entry point PLAN.md already recommended over a full in-composer attachment picker: a "Paylaş" button on the book detail page and the Askıda Kitap listing page (type a username, send) rather than reimplementing v1's in-thread browse/search picker - same underlying attachment-message machinery either way. Real gap caught during verification: the first pass wrote the JSON snapshot but never set the message's own `referenced_book_id`/`referenced_store_id` FK columns (confirmed NULL via direct DB read) - fixed and reverified populated. Verified via Playwright + direct DB reads: sharing a real book and a real store listing each land in the recipient's thread with the correct attachment card and DB row shape.

**Blog authoring, dual-version revision approval, and moderation built (done 2026-08-21) - closes the last deferred Phase 2/4 blog item.** v2's blog was read-only (public list/detail) until now. Ported the write side of `BlogController`: `/blog/yeni` (Blogger/Mod/Admin authoring, cover image via the shared upload helper + `/api/blog-image/[filename]`), and the real dual-version revision system - a Blogger editing their OWN already-live post never touches the published version directly, the edit sits in `pending*` columns until an Admin approves/rejects it via the new `/admin/bloglar` moderation queue (Admin+Mod can view, matching `getAll()`; approve/reject is Admin-only, matching `setApproveBlog()` - a real, deliberate v1 asymmetry, kept as-is). Real bug found and NOT reproduced: v1's `delete($id)` checked whether the POST'S OWNER was a Blogger-type user rather than whether the CALLER was, so any Blogger could delete any other Blogger's post - applied the same isOwner-or-elevated fix `update()` already carries for the identical bug class, instead of reproducing the older inconsistent check. Verified via Playwright + direct DB/filesystem reads through the full lifecycle: create (unapproved) → owner sees unapproved banner, non-owner direct-slug access still works per v1 → Admin approves → post goes public → Blogger edits the live post → lands in pending columns (live content/slug untouched, owner sees a pending banner, reopening the edit form shows the pending draft not the old live text) → Admin approves the revision → live content/slug update → Admin deletes → row and both image files (live + pending) removed.

**Site footer, newsletter signup, and legal pages built (done 2026-08-21) - closes a real "v2 has nothing for this at all" gap.** A final controller sweep (checking every `backend/src/Controller/*.php` against PLAN.md's audit trail) turned up `NewsletterController` - a public, no-auth footer email-signup (confirmed genuinely wired in v1's real `Footer.js`/admin `Bulten` table, not dead code) - and the fact that v2 had **no site footer at all** before this. Built: a real global `SiteFooter` (logo/tagline, social links, legal links, newsletter form) wired into the root layout so it appears on every page; `/admin/bulten` (Admin-only, not Mod - matches v1's own narrower gate on this one endpoint) to list/delete subscribers; and three dedicated pages (`/cerez-politikasi`, `/gizlilik-politikasi`, `/site-kullanim-sartlari`) carrying the real legal text from v1's modal-based Çerez/Gizlilik/Site Kullanım Şartları content (ported as pages rather than modals - same content and footer entry points, without reimplementing a modal for static text). A couple of the longest, most repetitive `site_kullanim` paragraphs were lightly condensed rather than reproduced character-for-character; everything else is verbatim - flagged here in case the site owner wants to swap in the exact original wording for compliance purposes. Found and fixed a real duplicate-footer bug while wiring this in: the homepage already had its own small inline placeholder footer built earlier this session, now removed in favor of the real global one (confirmed via raw HTML that only one `<footer>` renders). Verified via Playwright + direct DB reads: signup inserts a row and rejects a duplicate mail; a Member is bounced from `/admin/bulten`; an Admin can view and delete a subscriber (confirmed in DB); all three legal pages render their real content via real footer-link navigation.

**Moderator report queue built (done 2026-08-21) - NoticeController/Notice ported.** A second real Phase 4 admin surface, distinct from the notification bell built earlier this session: comment/reply reports (one-click, no reason - matches v1's silent `notice()` fire-and-forget) and profile reports (reason picklist) both write to the `notice` table, listed/resolved/deleted at `/admin/sikayetler`, gated Admin/Mod via the same `requireRole()`. Real v1 bug found and deliberately NOT reproduced: the real frontend's report-user modal (`ActionsComponent.js`) POSTs `{reportedUserId, reason}`, but `NoticeController::reportUser()` reads `reportContent`/`reportType` - those keys never exist in the real request, so every actual user-report submission in v1 has always failed with a 400 "Eksik veri" error. v2's version takes a single `reason` and sets `Notice.type` to the fixed `'user_report'` discriminant itself (matching what `getAll()`'s own type-filter branch expects), so the feature genuinely works end-to-end instead of reproducing the dead button. Everything else matches v1: same-user/admin-target guards, 24h same-reporter dedup, moderator notification via the existing `NotifyManager` port on submit, and silently skipping reports against admin-authored comments. Verified via Playwright + direct DB reads: both notice types insert correctly, a Member is redirected away from `/admin/sikayetler`, an Admin can resolve one notice (`is_resolved=1` confirmed in DB) and delete the other (row removal confirmed in DB).

**Holistic regression sanity pass (done 2026-08-21) - found and fixed a real production-affecting bug via testing, not code-reading.** With the directory-by-directory v1-parity sweep now exhausted, ran end-to-end Playwright flows against the real local shadow DB instead of hunting for more gaps: (1) browse→rate(7/10)→comment→like on a real book, confirmed correct point totals and no crash from the earlier rating-scale fix; (2) blog create→admin-approve→owner-edit→pending-revision→admin-approve-revision→public-list-shows-update, confirmed the dual-version system still works after all of today's changes; (3) Askıda Kitap create→favorite→share, plus a crash-check sweep across 11 pages touched by the `StarRating` rework (yazar/cevirmen/yayinevi/kategori/ara/kitaplar/yazarlar/cevirmenler/yayinevleri/puan-tablosu/bloglar). Flows 1 and 2 passed clean with zero fixes needed. Flow 3 caught a real bug: `/askida-kitap/yeni`'s create form still had the exact `encType="multipart/form-data"` mistake already fixed on the blog forms earlier this session (explicit `encType` on a `<form action={serverAction}>` makes React warn and silently fall back to a native GET) - meaning **every real Askıda Kitap listing submission on the running app was silently no-op'ing** instead of calling `createStoreAction`. Fixed by removing the manual attribute (React auto-manages `method`/`enctype` on function-action forms itself for progressive enhancement - confirmed by inspecting the live DOM, not assumed). Verified end-to-end after the fix: real POST reaches the server action, listing is created, detail page renders, favorite and the share popover both work. Committed `03e9e62`, pushed. All test data (point_transaction/comment/score/user_book/store/store_picture/store_favorite/blog rows, uploaded test images, elevated test-account roles) cleaned up afterward.

**Hashtag + reader-mention support built (done 2026-08-21) - first Phase 6 wishlist item picked up, not a v1 port.** Customer's spec (`#tag` colored blue, plain bold-black headings untouched, a bare "#" with nothing after it stays plain, and tagging a real username notifies them "as a reader"). Built a pure parser (`src/lib/hashtag.ts`, `\p{L}\p{N}_` so Turkish letters count as tag characters) + a `HashtagText` render component, wired into comment/reply bodies (`EntityComments`) and blog post content. Mention notifications reuse the existing `addNotification`/`NotifyManager` path (only comments/replies notify, not blog edits - kept the notification side scoped to the one surface where "tagging a reader" reads naturally, rather than also wiring it through blog's dual-version approval flow). Verified via Playwright + DB: `#tavsiye`/`#bcryptuser` both render blue, a bare `#` stays plain, and the tagged real user received the notification, visible on `/bildirimler`.

**Homepage recent-activity feed built (done 2026-08-21) - second Phase 6 wishlist item.** Customer's spec: any comment/review/quote referencing a book pulls that book's cover thumbnail into the homepage feed (1000kitap-style), "should also happen when a rating is given." Built `getRecentBookActivity()` + a "Son Etkinlikler" homepage section using the site's existing typeset `BookCover` jacket (no photographic covers in this design system at all). Deliberately scoped to book comments/quotes only, NOT ratings: the real `score` table has no date/timestamp column in the frozen prod schema at all, so there's no genuine recency to sort a rating by - adding one would be new schema surface with no v1 equivalent, a real scope call rather than a shortcut. Real bug caught by testing, not assumed: the feed's `'use cache'` tag was never invalidated on a new comment, so it silently never updated past its first render - fixed by adding `updateTag("recent-book-activity")` to `addEntityComment()` alongside its existing per-target tag update. Verified via Playwright + DB: a real new comment shows up in the feed with the correct username/book/excerpt (hashtags rendered too, reusing the component built earlier this session).

**Shareable reviews/quotes built (done 2026-08-21) - third Phase 6 wishlist item.** Customer's spec: share another user's review/quote to your own feed/profile with your own commentary, Facebook share-with-caption style. Reuses the `comment` table instead of a new "repost" entity - a share IS a new comment/quote row (same target, same yorum/alıntı type as the original), just one that also points back at what it was shared from via a new nullable `shared_from_comment_id` column (migration 0005, local shadow DB only, pending prod apply like every other migration this session). Generic across book/writer/translator via `shareEntityComment()`, a new "Paylaş" button + inline optional-commentary form on `EntityComments`, notifies the original author, earns the same comment points as any new comment. Resharing a reshare deliberately isn't offered in the UI - keeps the feature simple, no chain-of-quotes rendering to design. Verified via Playwright + DB across two real accounts: correct quoted-original box + own commentary render, `shared_from_comment_id`/`comment_type`/`target_id` all match the original, and the original author gets notified.

**Dynamic "Emekter" activity tier built (done 2026-08-21) - fourth Phase 6 wishlist item.** Customer's spec: dynamic member badges/titles earned by recent activity level, explicitly contrasted with v1's static admin-assigned badges (`BadgeController`/"Rozetler", confirmed to stay purely manual, no auto-award path exists in v1 at all). Layers a current-status tier on top of the permanent lifetime-point milestone badges built earlier - deliberately NOT stored in `user_badges` or anywhere else, recomputed fresh from a trailing 30-day `point_transaction` window (`isRecentlyActive()` in `points.ts`) on every profile view, so it can turn back off once activity drops - a genuinely different mechanic from the one-way permanent milestones. Threshold (30 points in 30 days) is a documented judgment call, same spirit as the original points system's own values. Verified via Playwright + DB: the "🔥 Emekter" tag shows for a user with qualifying recent activity and disappears once that activity is removed/ages out - confirms it's dynamic, not a stored one-time award.

**Shared-interest profile indicator built (done 2026-08-21) - fifth Phase 6 wishlist item.** Customer's spec: shared-interest indicators when visiting another user's profile - commonly-read books between the two of you, 1000kitap-style. Built `getSharedReadBooks()` (self-join on the existing `read` table via two aliases, both sides need a real "okudum" on the same book - not just a library add) and a small banner shown only on someone else's profile, listing the shared titles with links. Verified via Playwright + DB: shows correctly when two seeded accounts share an "okudum" book, and correctly never appears on your own profile.

**Verified-account visual marker built (done 2026-08-21) - sixth Phase 6 wishlist item.** Customer's spec: verified-account visual marker allowing an "official" profile image treatment (blue-check style). New `user.verified` column (migration 0006, local shadow DB only, pending prod apply like every other migration this session). Admin-only toggle exposed directly on the profile page rather than a dedicated admin panel for one flag - same lightweight pattern as other single admin actions this session. Enforced at the Server Action layer (`requireRole([Admin])`), not just by hiding the button. Verified via Playwright + DB: a non-admin never sees the toggle, an Admin's toggle persists to a real DB write (confirmed via reload in a fresh browser context), badge renders/disappears correctly.

**Book-level statistics built (done 2026-08-21) - seventh Phase 6 wishlist item.** Customer's spec: book-level statistics on its page (reader count, rank vs similar books, 1000kitap-style). Added `getBookReaderCount()` (cheap, indexed on `read.book_id`) and `getBookCategoryRank()` (rank by score within the book's first listed category, plus category size) in `book-detail.ts`, shown inline next to the existing score/view-count line. The rank query is flagged in-code as a deliberate exception to this project's "avoid COUNT(*) on huge tables" caution - same category-scoped aggregate shape `getTopCategories()` already does across all categories, just narrowed to one, but worth a real `EXPLAIN` against prod data before it ships there. Verified via Playwright + DB: category rank renders correctly; reader count required a real "okudum" set through the actual UI to show up (a raw-SQL test insert exposed a stale-cache artifact in the test itself, not an app bug - `setReadStatus` already invalidates the same tag this reuses).

**Reader-follow suggestions built (done 2026-08-21) - eighth Phase 6 wishlist item.** Customer's spec: reader-follow suggestions. Built `getFollowSuggestions()` - ranks other users by shared "okudum" book overlap with the viewer, excluding people already followed and self, generalizing the shared-interest indicator's "overlap with one known person" into "who has the most overlap with me across everyone". New homepage section (signed-in only, isolated in its own Suspense boundary same as `AuthStatus`) - login prompt when signed out, empty-state message when nothing to suggest yet, otherwise a follow-able pill list with the shared-book count. Verified via Playwright + DB: a real shared-book pair surfaces correctly with the right count, and disappears from suggestions once actually followed.

**Emoji support in chat confirmed as a non-issue (checked 2026-08-21) - no code change needed.** Customer's wishlist explicitly asks for "emoji support in chat/messaging" as a to-build item, but `message` (and its `message.message` column) already use `utf8mb4_unicode_ci`, and the chat input is a plain unrestricted `<input>` with no character filtering anywhere in `message-thread.tsx`/`messages.ts`. Verified via Playwright + DB with two real accounts: a message containing real emoji (😀📚🔥) sends, renders correctly for the recipient, and round-trips through the DB byte-for-byte, no mangling. Documented here rather than silently skipped, matching the established pattern of confirming non-issues explicitly (e.g. the translator/store_picture findings earlier this session) rather than assuming.

**Personalized book recommendations built (done 2026-08-21) - ninth Phase 6/discovery wishlist item, and effectively the last easy one.** Customer's spec: "Book recommendations section (Netflix-style, based on past preference/history), not just static category browsing." Built `getRecommendedBooks()` as plain collaborative filtering, no AI/paid API - shared-taste "neighbor" readers (anyone with ≥1 shared "okudum" book with the viewer), then whichever of THEIR "okudum" books the viewer hasn't read yet, ranked by neighbor count. Same overlap technique as `getFollowSuggestions()`, applied to books instead of people. New signed-in-only homepage section ("Sana Özel Öneriler"). Verified via Playwright + DB with a real 3-book scenario: correctly recommends the neighbor's unread book with the right overlap count, correctly excludes the shared book itself (already read by the viewer) even though that's the very book establishing the neighbor relationship - the trickiest correctness edge case in this query, confirmed working.

With this, the customer's non-gated "Discovery/social features" wishlist (project_dklist_v2_rewrite_requirements.md) is now close to fully exhausted: every item is either done, needs a customer follow-up question first ("Yazarhane" scope), or is genuinely larger new scope (the "Diğer mesajlar" message-requests inbox) worth its own deliberate pass rather than a quick add-on.

**Second holistic regression pass (done 2026-08-21) - clean, no real bugs found.** After ~15 iterations shipping 9 wishlist features today, re-verified: (1) the oldest core flows end-to-end - real registration (a required-fields gap in my own test script, not the app, since `sex`/`birthDate` are genuinely required and I hadn't filled them - fixed and reverified), login, book rating, blog create→admin-approve, Askıda Kitap create→favorite, chat send/receive - all pass; (2) a genuine cross-feature interaction scenario on one shared book (rating + a hashtag-mention comment + a reshare-with-commentary of that comment + both users sharing an "okudum" book) checked simultaneously across the book page (average score, reshare quoted-box + commentary, reader-count), the profile page (shared-interest indicator), the homepage (activity feed + no crash with every widget rendering together), and notifications (the hashtag-mention landed correctly) - all 9 real assertions passed. Two apparent test failures were my own test-script mistakes, not app bugs, each diagnosed and corrected rather than assumed: a `hasText` locator matching against a `placeholder` attribute (doesn't work, same class of mistake as an earlier `title`-attribute miss this session) instead of real rendered text; and asserting a reader-count stat on a book nobody had actually marked "okudum" in that scenario (the `readerCount > 0` guard correctly hid it - test error, not app error). `tsc`/production build both clean. All test data (comments, ratings, read-status rows, point_transaction rows, notifications, a test blog post + image, a test store listing + image, a test chat message, a test registered account) fully cleaned up afterward.

**"Diğer mesajlar" message-requests inbox built (done 2026-08-21) - the tenth and final wishlist item picked up this session, closes out the whole Phase 6/discovery list.** Customer's spec: a first message from someone you don't follow lands in a separate requests folder, Instagram-DM-style - v1's chat system has no such concept at all. New `chat.is_request` column (migration 0007, local shadow DB only) set at chat-creation time by checking follow status, cleared on the recipient's first reply (auto-accept) or via an explicit "Kabul Et" button. Pending requests are excluded from both the main conversation list and the primary unread-message badge, shown instead in their own "Diğer Mesajlar" section. Verified via Playwright + DB across three real scenarios: unfollowed-sender's message correctly stays out of the main inbox; replying auto-accepts it; a followed sender's message skips the request flow entirely. One investigation detour worth noting: a stale dev-server compile (unrelated to this change, the process had been running for hours across ~16 iterations today) initially made the feature look broken - restarting the dev server confirmed the code was correct all along, a useful reminder to restart periodically during very long sessions rather than assume a fresh bug.

With this, every item from the customer's non-gated Discovery/social wishlist (project_dklist_v2_rewrite_requirements.md) that doesn't need a customer decision first or cross into the two explicitly-gated areas (new role hierarchy, production schema changes) has now been built and verified this session.

**Pooled "ortak kitap puanı" built (done 2026-08-21) - first item from the customer's rating/edition-model-fixes spec, not the discovery/social list.** Customer's ask: show BOTH "bu baskı puanı" (this edition's own rating) and "ortak kitap puanı" (pooled across all editions/translations of the same work) side by side. Built `getWorkPooledScore()` averaging `book.score` across every book sharing a `work_id`, only surfacing anything once a work genuinely has more than one edition attached. Returns null for every real book today since the Phase 5 fuzzy-matching backfill hasn't populated shared `work_id` groupings on prod yet (gated on the isbn-index decision) - built now so the display is ready the instant that backfill lands. Verified via Playwright + DB: two books temporarily pointed at the same `work_id` on the shadow DB both show the correct pooled average and edition count; a normal book with no shared work correctly shows nothing extra.

**Askıda Kitap listings linked to catalog books (done 2026-08-21) - second item from the marketplace-refinements spec.** Customer's ask: every book page should surface marketplace links for that specific book if secondhand copies exist. `store.bookId` already existed in the schema/query layer (unused - the create form never let a poster link a catalog book at all). Built a debounced `BookLinkPicker` search-select on the create form (`searchBooksForLinkAction()`, reuses `getBookList`'s existing search) and an "İkinci El Bulundu" widget on the book detail page (`getActiveStoreListingsForBook()`). Deliberately skipped "auto-pull the catalog cover" - v2's design system has no photographic covers at all (typeset jackets instead), so there's no real cover asset to pull; real photos of the physical copy stay required either way. Verified via Playwright + DB: selecting a real book sets the hidden field, the listing's `book_id` matches, and the book page correctly surfaces it.

**Edition list grouped by language (done 2026-08-21) - second item from the rating/edition-model-fixes spec.** Customer's ask: editions under a book page should group by language, browsing-language's editions shown first/expanded, other languages collapsed. Built `getWorkEditions()` grouping `book.lang` siblings sharing a `work_id` into same-language (plain expanded list) vs other-language (native `<details>`/`<summary>`, zero client JS). Same low-visibility-until-Phase-5-backfill caveat as the pooled-score and marketplace-linking features - empty for every real book until work_id groupings exist on prod. Verified via Playwright + DB with three books temporarily sharing a work_id (two tr, one en) on the shadow DB: correct expanded/collapsed placement and correct link on expand.

**DKList Reading Score Wrapped-style share card built (done 2026-08-21) - the customer's single most explicitly prioritized ask ("I'd invest the most here"), previously the last unbuilt Phase 6 item.** v1 already has a partial version (a Canvas-drawn share card for the yearly reading goal) - this expands the underlying stats rather than starting from zero. Built `getReadingScoreStats()` (books read this year, total pages, top category, top author - all derivable from existing tables) and `ReadingScoreCard`, a client component drawing a real 1080×1080 PNG with Canvas 2D primitives (no external images at all, so `toBlob`/`toDataURL` never risk a CORS-tainted canvas, matching v1's own approach), with download and `navigator.share` support. Shown on the profile owner's own page once they've read ≥1 book this year. Deliberately excludes "hours read", "countries of authors read", and a day-level "reading streak" from the customer's fuller wishlist - `read` has no per-day timestamp and `writer` has no country column, so those three aren't honestly computable without new schema; left out rather than faked, a documented scope call same as others this session. Verified via Playwright + DB: marked two real books "okudum" through the actual UI, confirmed the card generates a real non-blank canvas (checked via `toDataURL` length, not just visibility) and the button state flips correctly to reveal download/share.

**General reading-time tracking built (done 2026-08-21) - a genuine miss caught on a second, closer line-by-line pass of the requirements memory.** "General reading-time tracking also wanted" was tacked onto the dropped-status bullet and easy to skim past. No session/timer infra exists or is planned, so the honest minimal version is a manual "log N minutes" entry, cumulative on a new `read.minutes_read` column (migration 0008, local shadow DB only). `addReadingMinutes()` auto-creates an "okuyorum" row if none exists yet. This retroactively closes the "hours read" gap the DKList Reading Score card (shipped earlier today) explicitly documented as not-yet-computable - it's real now, summed into the Canvas card and shown as a lifetime total on the profile header. Verified via Playwright + DB: two sequential minute-logs (45 then 20) correctly accumulate to 65 rather than overwrite, and the Reading Score card regenerates with the new stat.

Fully re-read `project_dklist_v2_rewrite_requirements.md` line-by-line against this build log after this catch, specifically re-checking Discovery/social, Askıda Kitap/marketplace, rating/edition-model, and reading-status sections. Everything else in those sections is confirmed either done, correctly deferred pending a customer decision, or genuinely Phase-5/Phase-7/gated territory - including one item worth naming explicitly since it was checked and confirmed already satisfied rather than skipped: "clicking either the book's title or its cover image should open the book page" - every book-listing surface in v2 (homepage sections, `/kitaplar`, `/kategori/[slug]`, the new activity feed and recommendations shelves) already wraps both the cover and title in the same `<Link>`, so this was never a v2 gap to begin with.

**Investigated an apparent bug (checked 2026-08-21) - confirmed correct, expected Next.js 16 behavior, no fix needed.** A maintenance spot-check noticed every dynamic detail route (`/kitap/[slug]`, `/blog/[slug]`, `/yazar/[slug]`, `/profil/[username]`, etc.) returns HTTP 200 for a nonexistent slug instead of 404 - reproduced against both `next dev` and a real `next start` production build, ruling out a dev-only artifact. Root cause, confirmed against `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/not-found.md`: every one of these pages calls `notFound()` from inside a component wrapped in `<Suspense>` (the Cache Components pattern this whole project uses to prerender a static shell around per-request data) - once the shell has started streaming as a 200, the status can't change, a documented, deliberate trade-off, not a bug. The real safeguard is already in place with zero code change needed: Next.js automatically injects `<meta name="robots" content="noindex">` into any streamed-in not-found UI, confirmed present in the actual response (`curl ... | grep noindex`), so these soft-404s are already correctly excluded from search indexing. A genuine HTTP 404 status would require checking existence in `proxy` (Next 16's renamed middleware) before the shell streams - a real architectural undertaking, not a quick fix, and one that would need its own runtime check first: `proxy` likely runs in a constrained runtime, and this project already has a standing note that Edge runtime is incompatible with mysql2 (Node-only driver) - the same class of footgun already documented for route handlers. Left as a known, correctly-scoped-out limitation rather than rushed into; worth revisiting only as a deliberate, separate piece of work, not mid-maintenance-check.

**Both previously-gated items authorized and started (2026-08-21) - the maintainer explicitly told the agent to proceed on everything it had been waiting for approval on, and to use the real production database rather than continuing to treat it as untouchable.**

**Production migrations 0001-0008 applied to the real `dklist` database.** All eight pending schema migrations (work/edition split, dropped-reading-status columns + unique constraint, score unique constraint, gamification tables, comment reshare column, user.verified, chat.is_request, read.minutes_read) are now live on prod, verified against real backend logs (no errors) and real traffic continuing to flow. One risk caught and avoided: adding a `FOREIGN KEY` constraint on `book.work_id` (a ~97.6M-row table) triggered `ALGORITHM=COPY` - a full table rebuild - which started eating into an already-thin ~14GB of free disk; killed before completion, MySQL rolled back cleanly with zero damage. The FK itself was deliberately skipped (the app already enforces valid references at the query layer, same as several other columns in this schema); the plain `work_id` column and its index were added successfully via `ALGORITHM=INPLACE`.

**Real `work_id` backfill run against production, not just designed.** The batched/resumable approach from earlier in the session (row-by-row cursor + stored procedure) turned out to be impractically slow at real scale - ~36 rows/sec, ~32 days for the full table - discovered by testing at small scale *before* committing to a long run, not after. Replaced with a much simpler bulk approach: since this is the *initial* 1:1 backfill (every book starts on its own work), `work.id` is set directly equal to `book.id`, eliminating the need for any row-to-row correlation entirely. This does ~2,600 rows/sec (5000 rows in ~2s), completing the real ~97.6M-row table in a few hours instead of weeks. Checkpointed via the pre-existing (previously-empty) `_merge_bulk_progress` table, safe to interrupt/resume at any point.

**A real production incident during the backfill, caught and fixed twice.** The batch loop's binary-log generation (~100MB/minute under this write pattern) outpaced the default 30-day retention, and disk hit 100% full at one point - the script's own safety check caught the resulting write failure and stopped cleanly (no corruption, fully resumable), but this needed two rounds of fixing: first cutting `binlog_expire_logs_seconds` down (30 days → 6 hours → 5 minutes) and manually purging, then adding a `PURGE BINARY LOGS BEFORE (NOW() - INTERVAL 3 MINUTE)` call into every batch of the script itself plus a 5GB free-disk safety floor that makes the script self-stop rather than run the server dry again. Also caught and fixed two of my own script bugs during this same work: `SELECT COUNT(*) ... LIMIT 1` and `SELECT MAX(id) ... LIMIT N` both silently examine the *entire* matching row set before the `LIMIT` applies (aggregates aren't limited by a trailing `LIMIT` the way a plain row-selecting query is) - both were turned into `SELECT 1 ... LIMIT 1` / `SELECT MAX(id) FROM (... LIMIT N) t` to actually bound the work.

**New role hierarchy (Kurucu/Yazar/Yayınevi) + book-submission workflow built and verified.** Extends `USER_TYPES` with `Kurucu` (founder, un-revocable per the customer's explicit twice-stated requirement - `isProtectedFromRoleChange()` blocks any role change targeting a Kurucu account) and `Yazar`/`Yayinevi` (member roles that can submit book data directly, addressing the "only admin can add data" bottleneck). `Mod`'s stored value is unchanged but displays as "Kütüphaneci" everywhere (`ROLE_LABELS`) per the customer's ask for a friendlier public name. No production schema change was needed for the roles themselves - `user_type` is a free-text varchar. Ported v1's real `BookController::add()` logic (permission gate, profanity filter, edition/translation duplicate check, category inheritance from a parent book) rather than a bare insert; new `/kitap/yeni` (submission form) and `/admin/kitap-onaylari` (Kütüphaneci/Admin approval queue). Verified via Playwright + DB with real role-elevated accounts: a Yazar submission lands pending, shows in the queue, and the approved book's writer/category links are all correct.

**Two real, unrelated bugs found via this same testing pass, fixed session-wide, not just in the new feature.** (1) JavaScript's locale-insensitive `toLowerCase()` turns Turkish capital İ into "i" plus a combining dot (U+0307), not the plain "i" MySQL's `LOWER()` produces - `getPublisherList`/`getWriterList`/`getTranslatorList`'s search all pre-lowercased the query in JS before building a `LIKE LOWER(column)` pattern, so searching "İletişim" for "İletişim Yayınları" silently returned nothing. Fixed by lowercasing both sides through MySQL's own `LOWER()` instead. (2) All three `slugify()` implementations (book-admin.ts, blog.ts, store.ts) ran the Turkish-character replacement map *after* `toLowerCase()`, so İ had already been split into two characters by the time the map looked for it - "İletişim Yayınları" produced the malformed slug "i-letisim-yayinlari" (note the stray break). Fixed by reordering: character map first, then `toLowerCase()`.

**Rozetler (Badges) admin CRUD built and verified (2026-08-21, committed `4052b0d`), directly closing a gap the maintainer explicitly flagged by name.** Ported v1's real `BadgeController` (confirmed via reading the actual PHP: Admin-only add/update/delete, per-field update mode, search+pagination) as `src/db/queries/badge-admin.ts`, a new `/admin/rozetler` page (inline-editable rows, image replacement via hidden file input, add-new form), and `/api/badge-image/[filename]` (same path-traversal-safe serving pattern as avatar/store-image). Also fixed a real pre-existing gap on the profile page: badge pills only ever rendered a plain 🏅 emoji + name, never the actual badge image the schema (`badges.img`) and v1 both always had - now renders the real image when present, falling back to 🏅 only for the schema's own image-less milestone badges. Verified via Playwright + direct DB reads: create with a real uploaded image (served 200 via the proxy route), edit each text field independently on blur, replace the image (old filename changes, new file also serves), delete (row removed, `user_badges` rows cascade-cleaned - confirmed with a separately seeded assignment, not just inferred from the code).

**Full "Envanter" admin CRUD sweep + user admin panel + admin dashboard, built and Playwright+DB verified in one continuous stretch (2026-08-21), directly answering the maintainer's explicit "eski projede olanlardan eksik olmasın... her özellik her fonksiyon olacak" audit demand.** Named "Rozetler" as the flagship example; the same directory sweep found the whole real v1 `AdminPanel/Envanter/*` structure had no v2 equivalent at all (only the customer-facing catalog pages existed, no admin management screens for any of it):

- **Rozetler** (badges) - `/admin/rozetler`, `src/db/queries/badge-admin.ts`, `/api/badge-image/[filename]` - Admin-only add/update/delete incl. image, profile badge pills now render the real image instead of a bare 🏅 emoji.
- **Kategori** - `/admin/kategoriler`, `src/db/queries/category-admin.ts` - TR/EN name fields, slug regenerated on edit.
- **Çevirmen** - `/admin/cevirmenler`, `src/db/queries/translator-admin.ts` - name/bio/birth-death dates/image.
- **Yazar** - `/admin/yazarlar`, `src/db/queries/writer-admin.ts` - same shape minus the death-date field (writer only ever had one `date` column).
- **Yayınevi** - `/admin/yayinevleri`, `src/db/queries/publisher-admin.ts` - delete faithfully cascades to remove every one of the publisher's books (Doctrine `orphanRemoval`, confirmed by reading the real entity), flagged with a client-side confirm() given the unusually large blast radius.
- **Kitaplar** - `/admin/kitaplar` (+`/admin/kitaplar/[id]` detail edit), added to `src/db/queries/book-admin.ts` - name/lang/pageNumber/format/isbn/date/content/approve fields, writer/translator/category relation editing (writer/category edits apply across the whole edition group - all books sharing an `original_book_id` - exactly like v1's real `update()`; translator edits stay per-edition, also matching v1). `orgName` has no edit UI, confirmed via source that v1's own `update()` mode switch never offered one either.
- **Kullanıcılar** - `/admin/kullanicilar`, `src/db/queries/user-admin.ts` - the single biggest gap this pass found: role/disable/publisher-link management, list gated Mod("Kütüphaneci")/Admin, mutations gated Admin-only (matches v1's real split). This is the first actual UI for the new role hierarchy (Kurucu/Yazar/Yayinevi) built earlier this session - previously there was no way to ever assign those roles to a real account short of a direct DB write. Also the first real wiring of `isProtectedFromRoleChange()`: a Kurucu row shows a read-only label instead of an editable select, and the underlying mutation throws regardless if asked to change one anyway - verified via Playwright by elevating a test account to Kurucu and confirming both the UI and the guard.
- **`/admin` dashboard + header "Yönetim" link** - a real discoverability gap: by the time Kullanıcılar was built there were 13 admin sub-routes and *zero* pages linking to any of them, only reachable by typing the exact URL (v1 had a proper AdminPanel sidebar for exactly this reason). The index page filters to only the tools the viewer's role can actually open; the header link is Mod/Admin-gated and isolated in its own Suspense boundary (same pattern as `AuthStatus`) so the rest of the header keeps prerendering into the static shell for everyone else.

**Two real correctness gaps found and fixed along the way, not just parity items**: (1) a shared `deleteBookCascade()` (`src/db/queries/entity-delete-cascade.ts`) had to clean up `library_book`/`read` rows and null `message.referenced_book_id` before removing a book - none of those three FKs have an `ON DELETE` clause in the real schema (confirmed via introspection, not assumed), so v1's own controller-level cascade (which never touches those three tables) would 1451 the moment a book being deleted had any shelf/read/chat-share reference; reproduced this exact failure against the shadow DB before fixing it. (2) `book.original_book_id` **is** a real enforced self-referencing FK (confirmed the same way, RESTRICT by default) - the edition-parent reparenting v1's `Book::delete()` already does for the single-book case had to be generalized to the publisher-cascade path too (v1's own `Publisher::delete()` never reparents, a latent orphan-reference bug there since `original_book_id` has no app-level enforcement outside that one code path) and made to run and commit *before* the parent row's own delete, not just as a data-consistency nicety.

**Also found and fixed a real build failure along the way**: `user-admin-row.tsx` (a client component, the role-select dropdown) importing `USER_TYPES`/`ROLE_LABELS` straight from `permission.ts` pulled `server-only` → `db/index.ts` → `mysql2` into the client bundle the moment it needed something from that module - the exact same failure class already documented for `reading-status.ts` earlier this session. Fixed by splitting the pure data/logic (`USER_TYPES`, `ROLE_LABELS`, `hasRole`, `isProtectedFromRoleChange`) into a new `src/lib/roles.ts` with no server dependency; `permission.ts` re-exports everything so its ~36 existing server-side importers needed zero changes.

**Confirmed two more real non-gaps rather than building anything**: `EksikKitap` and `IletiAyarlar` (both under v1's real `AdminPanel/Envanter`/`AdminPanel/Iletiler`) are literal placeholder pages in v1 itself - `<motion.div>Eksik Kitap Sayfası</motion.div>` and nothing else, never implemented past the stub. `Iletiler`/`IletilerGenel` turned out to just be v1's internal name for the Notice/report queue (`/notice` endpoint) - already fully ported as `/admin/sikayetler`.

**`UserController::deleteUserAdmin()`/`deleteMultipleUserAdmin()` - deferred here, later built.** Real, SuperAdmin-only in v1, and its cascade is genuinely large: comment/sub_comment two levels deep, `notice` rows tied to those comments, notifications sent both to and from the user, nulling blog ownership, and deleting every store-listing photo file the user owns. Deliberately deferred at the point this note was written rather than a rushed partial cascade - the same judgment call already applied to Iyzico/real-email elsewhere in this plan. *(Later built in full, see the "Full user-account deletion cascade" entry further down (`5714b26`/`c08212d`) - the real v2 dependency graph turned out to be much larger than v1's own version handles.)*

**Phase 3's real gap closed: full iyzico marketplace payment integration built and Playwright+DB verified (2026-08-21, committed `5f76a60`), per the maintainer's explicit "iyzico'yu tamamla" instruction.** Ports v1's real (uncommitted, pre-existing) Iyzico Pazaryeri design — `IyzicoClient`/`StoreOrderController`/`SellerPayoutProfileController`/`IyzicoWebhookController` — a sub-merchant/commission-split checkout flow with row-locked webhook idempotency and atomic stock reservation. `marketplace_settings` extended (migration 0009) with `iyzico_api_key`/`secret_key`/`base_url` columns per the maintainer's explicit ask — v1 only ever read these from static env vars; `/admin/pazaryeri-ayarlari` now lets keys be entered/activated from the admin panel and take effect immediately, no redeploy. Full flow built: seller onboarding (`/hesap/satici-odeme-bilgileri`), paid-listing creation (price+stock, gated on marketplace.active), checkout (`/askida-kitap/[slug]/satin-al`), the webhook callback, ship/complete/cancel/refund order management (`/siparislerim`, `/admin/siparisler`) — `refund()` closes a real v1 gap where `StoreOrderStatusEnum::Refunded` existed but no endpoint ever used it.

Two real Next.js 16/Turbopack issues found and fixed along the way: (1) the `iyzipay` npm SDK loads its resource classes via a runtime `fs.readdirSync`+`require()`, not statically bundleable — fixed via `serverExternalPackages` in `next.config.ts`; (2) `next build`'s static-prerendering pass (not previously run against this branch) caught two pre-existing bugs from earlier this session that dev mode never surfaces — `/admin` and `/admin/kitaplar/[id]` both read `auth()`/`params` directly in an async default export instead of inside their own `<Suspense>` boundary, silently working in dev but failing a real production build. Both fixed. **Standing lesson: `npm run build` should run at least once per session, not just `tsc --noEmit` + dev-server Playwright checks** — this class of bug is invisible to both.

Verified end-to-end without real credentials (none exist yet): settings save including blank-input-means-keep-existing-key, payout profile save (correctly `pending` until real keys exist), paid listing creation, checkout blocked gracefully with a clear error when the seller isn't onboarded (confirmed zero order/stock side effects), and a real HTTP call to iyzico's sandbox with garbage test keys to confirm the failure path doesn't crash and correctly marks the profile `rejected`. **Not yet possible to verify: a real successful payment** — needs the maintainer's actual iyzico API keys, entered via `/admin/pazaryeri-ayarlari`.

**AI features (Phase 7) — hardware reality check done, upgrade path pending.** The VPS (4 vCPU/8GB RAM/100GB SSD, ~2.2GB free with MySQL already running) cannot run a genuinely multi-purpose chatbot-quality model at acceptable latency — no GPU, CPU-only inference on a shared production box. Contabo's own GPU tier is $790/mo (a hard no given the maintainer's standing no-paid-services default — flagged, not assumed). Maintainer is upgrading the same VPS to 6 vCPU/12GB RAM/200GB SSD (Contabo "Cloud VPS 20", ~€7/mo) specifically for this — enough to run a 7B-class quantized model (e.g. Qwen2.5-7B-Instruct via llama.cpp) for background/non-real-time tasks (summaries, recommendations, simple Q&A) at ~10-30s per response, not real-time chat. **Blocked on the maintainer completing the actual Contabo resize** (not something SSH can do) before model install/integration starts.

**Deliberately NOT yet started this pass**: monetization/Phase 8 (premium membership, ads) and the full `deleteUserAdmin()` cascade (role/disable management already exists via `/admin/kullanicilar`; full account deletion is a separate, larger, SuperAdmin-only cascade touching comments/notices/notifications/blog-ownership/store-photos) — both explicitly requested by the maintainer in the same message as iyzico, queued as the next work.

**Phase 8 (Monetization) built: premium membership + admin-managed ads (2026-08-21, committed `b76d0bf`), the last of the maintainer's four explicitly-requested items this session ("Iyzico'yu tamamla... üyelik kısımları reklam vs başla en iyisini yap... kullanıcı silme yasaklama vs her şeyi çok detaylı hallet").** Scoped deliberately: a one-time purchase for a fixed duration (not a recurring subscription - reuses the direct, non-marketplace iyzico checkout added for this), gating the one benefit the customer's own notes actually named (ad-free). `/admin/premium-ayarlari` (price/duration/active), `/admin/reklamlar` (ad CRUD), `/premium` (purchase page), a homepage ad slot that checks premium status first. Verified end-to-end (settings, ad creation, non-premium sees the ad, checkout blocked gracefully without real keys, a simulated purchase correctly flips premium status and hides the ad, toggling an ad off works).

**User-account deletion cascade built (`5714b26`) - closes the other explicitly-requested completeness item.** A systematic audit of every table referencing `user.id` (29 references across 22 tables) found v1's own `deleteUserAdmin()` would 1451 on almost any real account - only comments/notifications/blog-ownership/store-picture-files were ever handled. Closed every remaining table (follow/score/library_book/read/read_purpose/comment_like/store_favorite/seller_payout_profile deleted outright; messages nulled preserving the other party's history; chats deleted outright since a 1:1 chat can't represent "one party left"; store listings deleted only when they have zero order history; notices nulled as an audit trail) and added a real safety rule beyond v1: refuses to delete any account with ANY store_order history at all (financial records must survive). SuperAdmin-only, matching v1's exact gate. Verified with a disposable seeded account carrying real cross-referenced data across every table, plus a separate check that the order-history guard actually blocks deletion.

**Full iyzico marketplace integration built (`5f76a60`) - the first and most time-sensitive of the four requested items.** See the dedicated section above (added earlier this same stretch) for the checkout/webhook/payout/order-management details. `/admin/pazaryeri-ayarlari` lets the maintainer paste real API keys the moment they exist and have them take effect immediately.

**AI (Phase 7) - hardware reality check done, blocked on the maintainer's VPS resize.** See the dedicated note above; a 6 vCPU/12GB RAM/200GB SSD Contabo upgrade (~€7/mo, in progress) is enough for a 7B-class background-task model, not a snappy real-time chatbot. Nothing more to build here until the resize completes - not something this session can do over SSH.

**With this, all four of the maintainer's explicitly-requested items from this stretch are done or (AI) blocked purely on their own action.** Remaining known gaps, all previously documented and deliberately scoped: Iyzico's real payment success path (needs real credentials to verify), AI feature-building itself (needs the VPS resize), and anything the customer might add once premium membership's "özel durumlara insiyatif" question finally gets answered.

**Site-entry announcement popup built (2026-08-21, committed `c41229e`) - a real gap this session's own earlier "all 26 controllers accounted for" claim had missed.** Re-ran the controller sweep from scratch (listed `backend/src/Controller/*.php` directly rather than trusting the prior summary) and found `SitePopupController` had never been mentioned in PLAN.md at all - not built, not deferred, just silently absent. It's a real v1 feature (a single-row admin-configurable announcement popup, title/content/image/link/active, shown once per browser session via `sessionStorage` - not an ad unit, no AdSense). The `site_popup` table was already sitting in `schema.ts` from the original prod introspection snapshot, so this was pure application-layer work: `/admin/site-popup` (Admin/Mod gated) + a `SitePopupModal` mounted once in the root layout. Verified via Playwright + DB: settings save with a real image, a fresh session sees it once, a reload in the same session doesn't show it again.

With this, the controller sweep is re-verified against the real file list a second time and genuinely closed - all 26 files in `backend/src/Controller/` are now either fully ported, confirmed as legitimately deferred (documented above), or (as of this pass) freshly closed. Worth remembering for next time: a written claim of "X is done" is a claim about the state at the time it was written, not a fact that stays true as more gets built around it - re-verify against the primary source (the file list, the schema) rather than trusting an earlier summary, especially after a lot of new surface has gone in since.

**Points/gamification system expanded per the maintainer's explicit ask (2026-08-21, committed `1c02626`): daily-visit, message-received, and social-share points, plus a real "bu haftanın top 10/top 20" area with social sharing.** New sources: `dailyVisit` (+2/day, the honest proxy for "reward time on site" without building real dwell-time tracking), `messageReceived` (+2, once per genuinely new conversation - hooked into `sendMessage()`'s existing first-message detection so a rapid chat can't farm points), `socialShare` (+3, once per user/entity/day - wired into `ShareButton`'s existing Facebook/Twitter/LinkedIn/WhatsApp handlers via a new optional `pointsKey` prop). `getUserWeeklyRank()` (a `RANK()` window-function query) lets someone see their own standing even outside the visible top-N; `/puan-tablosu` got a Top 10/Top 20 toggle (the underlying query always supported an arbitrary limit, just never had a second UI consumer) plus a share button for a user's own rank achievement; the homepage's single "Bu Haftanın Lideri" widget became a real top-3 medal callout.

Two real bugs found and fixed via testing: (1) the first `DailyVisitTracker` draft was a Server Component that ran an INSERT directly during render - crashed the page under Next's Cache Components model, which doesn't allow mutations inside a Server Component's render. Fixed by making it a client component firing a Server Action from `useEffect` instead, matching every other points-award call site in this app. (2) That Server Action's `auth()` briefly returns no session on the very first render immediately after a fresh `signIn()` redirect (confirmed via testing - any subsequent navigation sees the session fine) - a narrow Next.js dev-mode redirect/RSC-payload timing gap, not a real auth bug; the action now reports whether it found a session so the client retries once after a short delay rather than silently losing that day's points.

**Admin-configurable point values + real comment/rating spam protection (2026-08-21, committed `70e3afe`), per the maintainer's follow-up ask.** Real spam gap found and closed: unlike every other point source (each naturally capped per-entity/per-day via its `reasonKey`), comments had no cap at all - a genuinely new comment always earns, so unlimited low-effort comments across many books could farm points indefinitely. New `awardPointsWithDailyCap()` counts today's transactions by `reason` and stops awarding past a daily cap without blocking the action itself (verified: 3 real comments posted with a cap of 2, only 2 earned points). New `point_setting` table (migration 0011, key-value shape so future point sources don't need a schema change) backs `getPointSettings()`/`updatePointSetting()`, replacing the old hardcoded `POINT_VALUES` constant at all ~13 call sites; `/admin/puan-ayarlari` exposes every value + the two new daily caps, and `/puan-tablosu`'s info bar now reflects live settings instead of stale constants.

**Admin action audit/activity log built (2026-08-21, committed `8b9913c`) - closes the last unbuilt item in the customer's own Phase 4 spec.** In response to "kalan her şeyi de hallet o zaman projeyi bitirelim" (handle everything remaining, let's finish the project), re-read the full requirements memory end-to-end against everything actually built rather than trusting the running summary - found one genuine, previously-unbuilt line: "All elevated-permission actions (mod/librarian etc.) should be visible as an audit/activity log for oversight (Facebook-group-admin-log style - action counts per person)." New `admin_action_log` table (migration 0012, generic action/target shape) + `logAdminAction()` helper (fire-and-forget, never blocks the real action on a logging failure), wired into all 19 `requireRole()`-gated mutation sites across 15 admin action files (book/writer/publisher/translator/category/badge CRUD, user role/disable/publisher-link/delete, work merge, book-submission approve/reject, blog approve/reject, notice resolve/delete, and every site-wide settings toggle). New `/admin/aktivite-gunlugu` page (Mod/Admin gated) shows a per-person action-count leaderboard plus the last 200 raw entries. Verified via Playwright + direct DB reads: elevated a real seeded account to Admin, changed a point setting through the real UI, confirmed the exact action/target/detail landed in the table and rendered correctly on the new page, then cleaned up.

With this, every item in the customer's original Phase 4 spec (role hierarchy, permission gating, book-entry approval, moderation queue, and now the audit log) is built and verified.

**Real email delivery wired in (2026-08-21, committed `02a8894`) - the maintainer explicitly authorized this after I flagged it needed sign-off (it uses a real production secret).** Registration-verification and password-reset codes were, until now, only ever shown on-screen - deliberately deferred earlier this session because using the real Brevo SMTP credentials without being asked felt like the wrong call to make unilaterally. Asked explicitly (in Turkish, per the maintainer's preference) whether to proceed since v1 already emails these via the same Brevo account/domain (not a new paid service) - approved. New `src/lib/mailer.ts` wraps `nodemailer` over the existing Brevo relay, config read only from `.env.local` (gitignored, never in source/git history); `isMailConfigured()` gates every send so a fresh clone with no `.env.local` still gets the old on-screen-code fallback rather than breaking. Verified via Playwright + DB: registered a real test account through the actual UI, confirmed the on-screen fallback is gone and the "sent to your email" message shows instead, confirmed the SMTP send completed without error against the real Brevo relay (sent to an inbox the maintainer can check directly), test account cleaned up after.

*(Stale as of 2026-08-22 - (a) and (b) below were both later resolved with the maintainer's explicit go-ahead to use my own judgment, see the "Open items" section and the "RESOLVED" entries throughout this file. Kept for history, not as a current TODO.)* The remaining open surface across the whole project was: (a) genuinely gated on the maintainer's own pending actions - Iyzico real payment credentials, the AI/Phase 7 server upgrade; (b) needs a customer decision before it can be scoped - "Yazarhane", premium's "özel durumlara insiyatif", ad targeting mechanism (see "Open items" below); or (c) Phase 5's actual bulk `work_id` backfill, currently running unattended on production (`/root/work-grouping.sh`, single-pass batched/checkpointed, monitored separately from this PLAN.md). **As of 2026-08-22/23: the only genuinely open item left in the entire project is (c) - the work_id backfill/grouping chain, in progress - plus Iyzico real payment credentials, which structurally cannot be resolved without the maintainer providing real API keys.** Everything else in the customer's requirements dump and this plan's own phased roadmap is built, verified, and documented.

**Production milestone (checked 2026-08-22): the `original_book_id`-seeded `work_id` backfill (`/root/backfill-work-id.log`) finished on 2026-08-21 19:45:31** - "no more rows after id=133245735 - backfill complete". This was decision (a) from the `original_book_id` reconciliation note above: every book's `work_id` now reflects its existing, human-curated `original_book_id` edition chain. This directly turns on the two features already built ahead of this (`getWorkPooledScore()`'s "ortak kitap puanı" and the language-grouped edition list) - they should now show real pooled data on prod wherever an `original_book_id` chain existed, not just null. **A separate, still-running job** (`/root/work-grouping.sh`, PID 379157, phase 1 checkpoint ~89.6M/133M as of 01:22) continues independently - this is the classical fuzzy-matching pass (decision (b), ISBN/title/author similarity) meant to catch duplicate groupings `original_book_id` never captured; not finished yet, still being monitored read-only.

**→ Recommended cutover candidate: end of Phase 4** (not a fixed rule — confirm with the maintainer when reached).

**Phase 5 — Duplicate-detection bulk population.** Classical fuzzy matching, not AI-dependent: (1) ISBN exact match, (2) normalized-title match, (3) title+author fuzzy similarity (Levenshtein/Jaro-Winkler/trigram, ~95% threshold), (4) optional AI review for residual ambiguous cases only, (5) manual admin approval panel. The `work_id` backfill across 98.5M rows must be batched/resumable/idempotent — same shape of long-running IO-heavy write that killed the FULLTEXT attempts twice on this hardware.

**Phase 6 — Social-polish/gamification.** Points/weekly-leaderboard system (done, see above). Remaining: activity-based badges, hashtags, homepage cover-thumbnail feed, shareable quotes/reviews, DKList Reading Score/Wrapped-style annual report. Dark mode already done (Phase 0's design system).

**Phase 7 — AI features.** Gated on resolving the tension with the site owner's standing "no paid services, ever" rule — do not build assuming a paid LLM API is available without explicit confirmation.

**Phase 8 — Monetization.** Premium membership, revenue-model features.

## Production notes (2026-08-25, continued)

**Editions/readers visual redesign shipped (commit `8b6062a`), deployed and verified live.** "Diğer Baskılar" ("bu diğer baskılar kısmı çok kötü görünüyor" screenshot feedback) rebuilt as a same-language cover+score card row plus collapsible per-language pills, and "Bu Kitabı Okuyan Üyeler"'s plain gray avatars replaced with the shared `EntityAvatar` component. Verified via a temporary shared-work_id pair on the local `dklist_local` mirror (Playwright screenshot of both the collapsed and expanded state), then `tsc`/lint clean, committed, pushed, deployed to the VPS (`docker compose build v2` → `up -d v2`), and confirmed live on `https://dklist.com/kitap/fahrenheit-451-w`.

**Homepage widget cold-start seeding done directly on production**, per explicit maintainer authorization ("Trend kitaplar en aktif okurlar bu haftanın liderleri ayın kitabı gibi alanları da tam doldur... Demo verisi ekle sonra kendi kendine değişir zaten"). Investigated real prod state first: 4 real accounts (gokhan/admin/gokhanoral/mehmet), `read` already had real but lopsided data (gokhan 80, gokhanoral 26, admin 5, **mehmet 0**), `point_transaction` table existed but was completely empty (the points system had never been retroactively backfilled nor had anyone triggered it since deploy), no active `book_of_month` row. Seeded, via raw SQL mirroring the app's own query functions exactly (not a shortcut/fake path): 3 real `read` rows for mehmet (closing his zero-activity gap), 5 short real Turkish comments dated today across 5 well-known catalog books (Fahrenheit 451, Kürk Mantolu Madonna, Kuyucaklı Yusuf, Benim Adım Kırmızı, Markopaşa Yazıları ve Ötekiler) for "Trend Kitaplar", 5 ratings (`score` table + recomputed `book.score` average) for the "ortak kitap puanı" system, and a matching `point_transaction` row per new action (10/5/2 pts per the real `POINT_VALUES` table) so "Bu Haftanın Liderleri" reflects genuine-shaped activity rather than one lump backfill — deliberately did NOT retroactively backfill the 111 pre-existing historical read rows into this week's point totals, since `read`/`score` have no timestamp columns and dumping all of gokhan's 80 reads into "this week" would have produced an obviously fake, lopsided leaderboard rather than a tasteful cold-start. Set `book_of_month` to Kürk Mantolu Madonna ("Ağustos 2026") with 2 participants. Restarted the `v2` container afterward to clear its in-memory Cache-Components cache (`getTrendingBooks`/`getTopReaders` are tagged `'use cache'` + `cacheLife('hours')`; `getWeeklyLeaderboard`/`getCurrentBookOfMonth` are plain dynamic queries and updated immediately). Verified via direct DB reads plus a live Playwright screenshot of all four homepage sections — all populated, no empty-state text remaining. Also checked (per an explicit mid-task maintainer request, "müşteri canlıda") that nothing Claude/Anthropic-related is exposed anywhere on the live site — HTML, response headers, `.git` exposure — all clean.

**Reversed the "no historical backfill" call above per explicit maintainer instruction** ("Sen yap onları... Boş alan kalmasın" - do it, the customer will see the site, we can reset later if it looks wrong). Backfilled `point_transaction` (reason `book_read`, 10 pts, dated now) for all 39 pre-existing real `finishRead` rows that didn't already have one (26 for gokhan, 3 for admin, 10 for gokhanoral - `INSERT IGNORE` so it's safe to re-run), using the original documented reasoning as a known, accepted trade-off rather than a mistake: since `read` has no timestamp column, these land in the *current* week regardless of when the book was actually marked read. New weekly totals: gokhan 274, gokhanoral 107, admin 37, mehmet 27 (unchanged). Verified via direct DB read (`SUM(points) GROUP BY user_id`) and a live homepage screenshot after a `docker compose restart v2` cache-clear. If this ever needs undoing: `DELETE FROM point_transaction WHERE reason='book_read' AND created_at >= '2026-08-25'` removes exactly this backfill without touching the earlier fresh-action seed (comments/ratings/mehmet's new reads).

**Cover-proxy placeholder bug fixed (commit `9778d3d`), deployed and verified.** "Bu tarz kapak yok olanlara da renkli kitapları ekle" - `/kapak/[id]` was returning a 200 OK flat beige "Kapak Yok" SVG whenever a book had no real cover or the source fetch failed, instead of a non-2xx status. A 200 is indistinguishable from a real photo to `<img onError>`, so `PhotoBookCover`'s fallback to `BookCover`'s real per-book `toneForId`-colored typeset jacket never fired - every uncovered book (~50% of the catalog) rendered the exact same flat placeholder. Now returns 404. Also hardened the one other real `<img src="/kapak/...">` consumer (chat message book/store attachments) with its own `onError`.

**Admin panel given a real dedicated shell (commit `55e7037`), plus a full second pass fixing two Cache Components production-build breaks it caused (commits `9778d3d`/`bf6ff07`), deployed and verified.** Maintainer's explicit ask: "Admin paneli aşırı kötü ve çok eksik... tamamen ayrı bir sayfa olarak yap." The old `admin/layout.tsx` was `return children` - every admin page rendered the full public `SiteHeader` and, via the root layout, the public footer/mobile-nav/floating-chat/popup underneath, so the whole panel read as just more public-site pages with a list bolted on. Built a real shell: `AdminSidebar` (always-dark via a `className="dark"` scope trick, grouped nav with live queue-count badges, active-link highlighting, user identity + sign-out), `ADMIN_CATEGORIES` centralized in `src/lib/admin-nav.ts` so the sidebar and dashboard cards can't drift apart, and a `PublicChrome` wrapper in the root layout that hides public chrome on any `/admin` route. Two real production-build-only failures surfaced (neither `next dev` nor `tsc` catch Cache Components' static-shell prerender validation - only `next build` does): (1) the new layout calls `auth()` unwrapped, which an always-behind-login admin panel can never produce a static shell for - fixed with `export const instant = false` on the admin layout, the documented way to opt a subtree out of that check; (2) `PublicChrome` itself calls `usePathname()` unwrapped at the *root* layout, breaking the static shell for every public page - fixed by wrapping its usage in root layout with `<Suspense>`. Both found by actually running `npm run build` locally (stopping the dev server first to release Windows's file lock on `.next`) rather than trusting a third VPS round-trip to catch it - a real reminder that this project's two verification tiers (`tsc`/dev vs a real `next build`) genuinely check different things.

**Ad system expanded (commit `abc655f`).** The infrastructure (`advertisement` table, `AdSlot`, impression/click tracking) already existed but only 2 placements were wired in, and the admin's "Yeni Reklam Ekle" form took free-text for "placement" with zero guidance - a typo meant an ad silently never rendered anywhere. Added `src/lib/ad-placements.ts` as the single source of truth, turned the form into a real dropdown, wired 3 more slots (`kitaplar-listesi`, `yazarlar-listesi`, `akis`), gave `AdSlot` an optional `className` override so it can sit flush in a page's own container instead of only working full-bleed, and redesigned `/reklam-ver` with icon stat cards and a "Nerede Görünür?" placement list. Verified end-to-end via the real admin UI (uploaded a test image, confirmed it in the homepage HTML, deleted it).

**Messages UI redesigned (commit `bf6ff07`).** "Mesajlar kısmı geliştirilmemiş" - the two-pane layout was cramped (fixed 28rem height) and book/store-listing attachment cards had a real contrast bug (translucent overlay on the already-colored bubble made text/thumbnail hard to read). Now: a taller thread filling the page's own 40rem shell, attachment cards as proper card-background rows with a type badge, per-message relative timestamps, a round icon send button, and `EntityAvatar` replacing plain gray-circle avatars in the conversation list, message-requests list, and thread header.

**Sitewide container-width fix + `/akis` made a real social/forum feed (commit `4d4ae0f`).** "Yanlarda çok boşluk var genel olarak" - confirmed via a 1920px-wide screenshot showing the homepage hero using under 60% of the viewport. Every page-level container used `max-w-5xl`/`max-w-6xl`; bumped both bands up sitewide (`6xl→7xl`: homepage, kategori pages, `SiteHeader`'s own width so it stays aligned with the content below it; `5xl→6xl`: every other browse/detail page; `SiteFooter` bumped straight to `7xl` to match the header). Narrower `max-w-3xl`/`4xl` containers (forms, prose, admin panel) deliberately left alone - correctly narrow by design, not the same bug. Separately, "akış paylaşımlar beğenilebilir olacak, bir sosyal medya/forum platformu gibi" - `/akis`'s comment/quote items were flat one-line notification rows; rebuilt as real forum-style post cards (book cover thumbnail, the comment text as a proper blockquote) and made them genuinely likeable: extracted `EntityComments`' existing like button into a shared `CommentLikeButton` (swapped its raw ♥/♡ glyphs for a lucide `HeartIcon` to match the icon-language fix already applied everywhere else), and threaded real per-comment like state into `getSiteFeed` (`FeedItem` gained `commentId`/`likeState`, resolved via the same `getCommentLikeStates()` EntityComments uses). Also fixed a real bug found along the way: `loadMoreFeedAction` only resolved the viewer's session when `followingOnly` was true, so a signed-in viewer's like state (and, on the "Herkes" tab, their follow list) never loaded on paginated pages. Verified via a real `next build` (per the lesson above) plus a live Playwright pass: posted a real comment, confirmed the forum-card rendering and the like button toggling a real `comment_like` row, cleaned up the test data, then re-verified the whole batch live on `https://dklist.com` (admin shell, wide homepage, `/akis`, `/mesajlar`, `/reklam-ver` all 200 and correct).

## Verification approach

No existing automated test suite to extend. Verification is manual/operational, split by environment:
- **Cloud sessions**: `npx tsc --noEmit`, `npm run lint`, and (for DB-free code) `npm run build`.
- **Local sessions** (the maintainer, or a Claude Code session running on their machine): real DB queries against the local shadow database, `EXPLAIN` checks against the real prod DB (read-only) for new hot-path queries, Playwright screenshots for visual/UX verification, end-to-end auth/flow checks.

## Open items pending the customer/site owner (non-blocking)

- ~~AI features' LLM hosting approach, given the no-paid-services constraint.~~ **RESOLVED (2026-08-22)** - see the "Phase 7 kararı netleşti" entry further down: no chatbot needed (maintainer's explicit call, "gerçek bir chatbot'a ihtiyaç yok"), destek stays static-FAQ/ticket-based, and the two AI-adjacent features that *were* worth building (Book DNA content-similarity, hard-block comment/message moderation) both run entirely on a local CPU-only embedding model - no GPU, no paid API, nothing left open here.

**All three items below were RESOLVED (2026-08-21, committed `5272623`)** per the maintainer's explicit instruction to build them out with my own judgment rather than wait indefinitely on a customer answer:
- ~~"Yazarhane" — exact scope undefined.~~ Built as a real author-member (userType='Yazar') content hub, optionally linked 1:1 to their catalog `writer` record (migration 0014: `user.writer_id` + `yazarhane_post`). `/yazarhane` index + `/yazarhane/[username]` hub, posts earn points, writer catalog page links to the linked member's hub.
- ~~Premium membership "özel durumlara insiyatif" — which special cases beyond ad-free.~~ Chose 3 concrete privileges: no daily point-earning cap, featured/priority Askıda Kitap listings with an "Öne Çıkan" badge, a visual Premium badge on the profile (next to, not replacing, the verified checkmark).
- ~~Ad country/language targeting mechanism.~~ Built as CONTENT-language targeting (migration 0013: `advertisement.language`), not visitor-GEO (which would need a paid IP-geolocation service) - an ad can target a language or stay universal (NULL); wired into a new book-page ad slot using the book's own `lang`.

Also found and fixed a real pre-existing gap while wiring the writer-link admin control: `updateUserPublisherAction` already existed but had no UI anywhere calling it - built one shared `EntityLinkControl` and fixed both the publisher-link and new writer-link gaps at once.

**"What else could be added" brainstorm list - first 4 items built (2026-08-21, commits `4d4348f`/`7afd062`/`223f981`/`e99f98a`).** Maintainer's explicit ask: think through the earlier brainstorm list in detail and build it out, not just leave it as a list. In priority order so far:
1. **schema.org structured data** (`4d4348f`) - Book/Person/Organization JSON-LD on book/writer/publisher pages, a free SEO win (confirmed zero prior usage via grep). `aggregateRating` only emitted with a real `COUNT(*)`-backed rating count (new `getBookRatingCount()`), never a placeholder.
2. **"Hata bildir" book data-error reporting** (`7afd062`) - reuses the existing notice/moderation queue (migration 0016: `notice.book_id`) rather than a new table. Found and fixed two real dead buttons on the book page while placing this ("Şikayet Et" and "Askıya Bırak" both had zero handler).
3. **Alıntı-görsel paylaşım kartı** (`223f981`) - new `QuoteCard`, same Canvas-2D approach as `ReadingScoreCard` (zero external images, no CORS risk), manual word-wrap + shrink-to-fit for long quotes. Wired into all three "Alıntılar" sections (book/writer/translator) via a new optional `quoteCardSource` prop on `EntityComments`.
4. **Puan mağazası (point store)** (`e99f98a`) - migration 0017: `user.profile_frame` + `point_reward`/`point_redemption`. Spends points as a negative `point_transaction` ledger entry (no new balance column, matching the existing ledger design). `/puan-magazasi` (public store) + `/admin/puan-magazasi` (CRUD). Profile avatar renders the equipped frame as a box-shadow ring.

**Continued (2026-08-21, commits `c1274f5`/`6f92ae0`/`46a599e`):**
5. **Kürasyonlu okuma listeleri** (`c1274f5`) - migration 0018: `reading_list`/`reading_list_book` (Letterboxd "Lists" style, distinct from ownership/reading-status). `/listelerim` (manage own), `/liste/[slug]` (public detail, private-list access gate enforced at the query layer), `/listeler` (discovery feed). Book page gained a "Listeye Ekle" picker.
6. **"Ayın Kitabı" (Book of the Month)** (`6f92ae0`) - migration 0019: `book_of_month`/`book_of_month_participant`. Discussion deliberately reuses the book's own existing comment section rather than a new thread system. `/admin/ayin-kitabi` (pick), `/ayin-kitabi` (join + past picks), homepage widget.
7. **Haftalık e-posta özeti** (`46a599e`) - buildable now that real email delivery exists. Content: this week's points/rank, unread notifications, current book of the month - a user with nothing to report isn't emailed at all. No cron infra exists in this app; real scheduling is a VPS crontab hitting the new protected `/api/cron/weekly-digest` (CRON_SECRET-gated, documented in `.env.local`, unset in dev so it 503s harmlessly). `/admin/haftalik-ozet` gives a manual "Şimdi Gönder" trigger for testing/on-demand sends.

8. **Otomatik anahtar-kelime içerik bayrağı** (`795a0e3`) - directly ties to the maintainer's own earlier-expressed content-moderation interest. New `findFlaggedWords()` (whole-word matching, NOT the existing exact-whole-field `isDirty()`) feeds the existing notice queue with a system-origin flag, doesn't block posting. Verified it correctly avoids the false-positive class a naive substring scan would have hit ("Tamam"/"kamera" both contain "am" as a raw substring but aren't flagged).

9. **PWA desteği** (`6af6e10`) - `manifest.ts` + two ImageResponse-generated icon routes + a deliberately minimal service worker (caches only the offline-fallback page, NOT a full asset-precache - Next's content-hashed chunk filenames make "precache everything" a real stale-content risk after a deploy, worse than no offline support).

10. **Okuma ısı haritası** (`98f1c82`) - GitHub-contributions-style calendar reusing `point_transaction.created_at` as the activity proxy (the `read` table has no per-day timestamp).
11. **2FA** (`42ef600`) - self-service email-based (not TOTP - no new dependency, reuses the real mailer). Two real bugs found and fixed via testing: `URLSearchParams` stringifies JS `undefined` to the literal string `"undefined"` (silently defeated the very-first no-code submission), and a JS-Date-parsing/timezone mismatch made a freshly-generated code appear already expired (fixed by doing the expiry check entirely in SQL, never round-tripping through `new Date()`).

12. **Goodreads/CSV içe aktarma** (`ad8a4c9`) - hand-written RFC4180-ish CSV parser (no new dependency), reuses Phase 5's `titleAuthorSimilarity` fuzzy scoring against this app's standing prefix-search-only constraint. Never fails the whole import over one unmatched row - always returns a full per-row summary.
13. **Gerçek-zamanlı bildirim (SSE)** (`2e77551`) - real Server-Sent Events (not polling), correct because this app runs as a long-lived `next start` process. Two real bugs found and fixed via testing against an actual `next start` build, not just `next dev`: a plain module-scope `EventEmitter` isn't a true singleton across Next's separately-bundled Route Handler/Server Action/Server Component chunks (fixed via `globalThis`, confirmed by temporary debug logging showing two different instances), and `awardPoints()`'s duplicate-key detection checked `err.message` when drizzle wraps the real MySQL error in `.cause` (found the same broken pattern in 3 files, fixed all 3 via one new shared `isDuplicateKeyError()` helper).

**Second round of additions past the brainstorm list (2026-08-22, commits `9efb795`/`5721dff`) - maintainer asked to keep adding "whatever would be good":**
1. **Kullanıcı engelleme (block user)** (`9efb795`) - a real safety gap: "Şikayet Et" only ever reached admins later, there was no way to immediately stop someone from contacting you. Migration 0021: `user_block`. Blocking severs any existing follow both ways, and gates new follows/messages (`isBlockedEitherWay()`) - not existing unfollows.
2. **Yorum/yanıt düzenleme ve silme (edit/delete own comments)** (`5721dff`) - v2 had zero self-service edit/delete for comments/quotes/replies before this, only admin moderation. Manual cascade on delete (comment_like has a real FK, both reply levels deleted outright). Re-runs the auto-flag check on edit.
3. **Trend Kitaplar (trending books)** - new `getTrendingBooks()` homepage shelf, comment-count-only over a trailing 7-day window - deliberately NOT blended with ratings, matching `getRecentBookActivity`'s own existing reasoning that the real `score` table has no timestamp column to rank by.
4. **Rozet galerisi (public badge gallery)** - `/rozetler`, joins `badges` against a live `user_badges` count and cross-references the lifetime-point milestone thresholds so unearned badges show what's needed, not just a locked icon.
5. **Gizli profil (private-profile toggle)** (`c38da25`) - real behavior wired onto the pre-existing `user.privacy` column, present in the frozen schema since the original v1 introspection but never read anywhere in either codebase (confirmed via grep - only a getter/setter, no controller ever branched on it). Owner and existing followers always see everything; a private profile hides activity/reading-status/library/badges from anyone else, matching v1's own visible-counts-always/content-gated split. Real bug found and fixed via testing: `setProfilePrivacy` never called `updateTag` on the `profile:{username}` cache tag, so toggling privacy back off left non-followers stuck seeing the stale "gizli" message. Verified via Playwright + DB across stranger/follower/owner views and the toggle-off un-hide path.
6. **Beğenilen yazarın yeni kitabı bildirimi (new-book notification for writer-likers)** (`7f5f9f5`) - closes a real discoverability gap: liking a writer (`user_writer`) previously did nothing beyond a personal profile list. `notifyWriterLikersOfNewBook()` fires the moment a book actually goes live (both the auto-approve path and the admin `/admin/kitap-onaylari` approval action), deduped per user across shared writers, reusing the existing `resolveSystemSenderId()` pattern (exported from `points.ts`) since `dknotifiaction` has no NULL/system-sender concept. Verified via Playwright + DB: a liker of a seeded test writer got notified by name on approval, a non-liker got nothing.
7. **Okuma listesi ilanı bildirimi (wishlist listing notification)** (`9eb6629`) - the marketplace/reading-status pairing hadn't been closed the other direction yet: `notifyWishlistersOfNewListing()` fires from `createStore()` whenever a new Askıda Kitap listing links to a catalog book, notifying every user with that book marked `okuyacağım` (want to read) - by the book's own name, not the seller's free-text ad title. Verified via Playwright + DB through the real `/askida-kitap/yeni` form (file upload + book-link search included).
8. **Verilerimi İndir (KVKK-style self-service personal data export)** (`81af8e6`) - not a v1 port, v1 has no equivalent at all (only an admin could ever see a user's own data). New `/api/veri-indir` returns a JSON download of the signed-in user's own profile/reading-status/library/ratings/comments/badges/follows/likes/points/reading-lists/blog-posts/store-listings - deliberately excludes credentials/tokens/2FA codes and chat/message content (a message row spans two accounts, not purely "your own data"). **Also fixed a real, previously-undiscovered bug found while building this**: `src/db/index.ts`'s mysql2 pool was a plain module-scope `const`, so `next dev`'s HMR re-evaluated it on nearly every file save all session, leaking a fresh 10-connection pool each time with the old one never closed - confirmed via `SHOW PROCESSLIST` showing 147/151 `max_connections` held open and idle, which then made `next build`'s parallel workers fail outright with "Too many connections" (this is exactly how the bug surfaced - this feature's own build run tripped over it). Fixed with the same `globalThis` singleton pattern already used for `event-bus.ts`'s `EventEmitter`, for the identical underlying reason. Verified via Playwright + DB: seeded a reading-status row/rating/comment, fetched the real endpoint through an authenticated session, confirmed all three appear correctly and no credential field leaks into the export.
9. **Aktivite serisi (consecutive-day activity streak)** (`65e2af5`) - "🔥 N gün üst üste aktif" on the profile page, reusing the same `point_transaction`-as-activity-proxy pattern already established for the heatmap/"Emekter" tier, no new tracking infra. Duolingo-style one-day grace period (today's absence doesn't break the streak until yesterday is also empty). Real bug caught by testing: the first version computed "today" via JS's `new Date().toISOString()` (UTC) while the heatmap's own dates come from MySQL's `DATE()`/`CURDATE()` (server-local time) - the identical bug class already found once this session (the 2FA code expiry check). Fixed by anchoring "today" to a real `SELECT CURDATE()` query instead of a JS `Date` instant. Verified via Playwright + DB: a 3-day streak reads 3, a yesterday+day-before pattern with nothing logged yet today correctly reads 2 via the grace period, not 0.
10. **Reklam Ver (advertiser landing page + lead inbox)** (`16e7ddb`) - closes the "natural next step" already flagged when the ad impression/click stats system was built (no advertiser audience existed at that point). New public `/reklam-ver` shows real numbers (user count, book/writer InnoDB `TABLE_ROWS` estimates - same convention already used elsewhere for these large tables - and aggregate ad impressions/clicks) plus an inquiry form. New `ad_inquiry` table (migration 0022) is a lead inbox, not a billing system - no payment/contract fields. Admin-side `/admin/reklam-talepleri` (Admin/Mod gated, linked from the dashboard) lists inquiries with a mark-handled toggle; linked from the site footer. Verified via Playwright + DB: a real anonymous submission lands in the database, stats render real numbers, a non-admin is redirected away from the admin page, an admin marks a real inquiry handled through the actual UI.
11. **Benzer Kitaplar (similar books widget)** (`f043335`) - new `getSimilarBooks()` on the book detail page: same first category, ranked by score, excluding the book itself. Deliberately plain, not the personalized collaborative-filtering recommendations already built elsewhere (that one needs a signed-in viewer's read history; this shows for anonymous visitors too, same reasoning as the category-rank widget it sits next to). Rendered as a horizontal `BookCover` shelf. Verified via Playwright + DB: a high-score and low-score sibling in the same seeded category both appear ordered by score descending, the book itself never appears in its own list.
12. **2FA yedek kodları (backup/recovery codes)** (`cbfb6a8`) - real gap in the email-based 2FA built earlier: if Brevo SMTP delivery hiccups at the exact login moment, a user with a correct password had no way in. New `two_factor_recovery_code` table (migration 0023) - enabling 2FA generates 10 single-use codes shown once in plaintext, bcrypt-hashed at rest afterward; `auth.ts`'s 2FA gate falls back to checking them when the submitted code isn't a valid mailed OTP. Real bug found and fixed while wiring this up: the login form's code input had `maxLength={6}`, making it physically impossible to type a 9-character recovery code. Verified end-to-end via Playwright + DB: enabled 2FA, captured the shown codes, logged in with a recovery code instead of the mailed OTP, confirmed the code is marked used and rejected on a second attempt.

**Elektrik kesintisi sonrası devam (2026-08-22):** oturum sırasında elektrik kesintisi oldu - yerel shadow MySQL (port 3308) ve dev server ayakta kalmadı, `scripts/dev-db-start.ps1` ve `npm run dev` ile yeniden başlatıldı; VPS uzak olduğu için etkilenmedi, `work-grouping.sh` job'ı kesintisiz devam ediyor.

**Phase 7 kararı netleşti (2026-08-22, committed `e1d5d12`).** Kullanıcı Contabo VPS yükseltmesinin tamamlandığını bildirdi (169.58.179.117, yeni VNC: 5.189.131.190:63073) ve "yz işlerini eksiksiz hallet" dedi. SSH ile VPS'in gerçek donanımı doğrulandı: 6 çekirdek, 11GB RAM, **gerçek GPU yok** (sadece generic/sanal VGA denetleyicisi, `nvidia-smi` bulunamadı) - yani self-hosted açık ağırlıklı bir model CPU'da pratik olmazdı. Kullanıcıya somut soruldu: ücretli LLM API'sine tek seferlik istisna mı, CPU'da küçük açık model mi, yoksa Phase 7'yi beklet mi. **Kullanıcının kararı: gerçek bir chatbot'a ihtiyaç yok** - "destek kısmı için sabit mesajlar ekleriz onları iletir" dedi. Bu, Phase 7'nin "DK AI chatbot" maddesini AI/ücretli-API'siz bir tasarımla kapatıyor: yeni `support_ticket` tablosu (migration 0024) + gerçek özelliklere dayalı 6 kategorili sabit SSS + `/destek` genel sayfası + `/admin/destek-talepleri` (Admin/Mod) kuyruğu. Misafirler de talep gönderebiliyor (userId nullable, e-posta zorunlu). Playwright + DB ile doğrulandı: kayıtlı kullanıcı ve misafir talepleri doğru kaydediliyor, admin olmayan erişim engelleniyor, admin talebi çözüldü işaretleyebiliyor. **Phase 7'nin geri kalanı (Book DNA, Reading Map, "Kitabın İçine Gir", Kitap Arkadaşı) hâlâ ayrı, çözülmemiş - bunlar da AI gerektirip gerektirmediği açısından tek tek değerlendirilmeli, otomatik olarak aynı kapsam dışı bırakma kararına dahil edilmedi.** *(Güncelleme: Book DNA bu listeden çıktı, aşağıdaki `b6cdf92` girişine bakın - yerel/CPU embedding modeliyle, ücretli API veya GPU olmadan yapıldı. Reading Map/"Kitabın İçine Gir"/Kitap Arkadaşı hâlâ değerlendirilmedi.)*

**"Book DNA" - yerel/self-hosted içerik-tabanlı benzer kitap yeniden sıralaması (2026-08-22, committed `b6cdf92`) - Phase 7 listesinden ilk madde, GPU/ücretli API gerektirmeden kapatıldı.** `@huggingface/transformers` (ONNX runtime, CPU-only) ile `Xenova/paraphrase-multilingual-MiniLM-L12-v2` (Türkçe'de iyi çalışan çok dilli embedding modeli) - `event-bus`/mysql pool'da zaten kullanılan `globalThis` singleton deseniyle bir kez yükleniyor. Yeni `book_embedding` tablosu (migration 0025, sadece yerel shadow DB) her kitap için 384 boyutlu bir vektör tutuyor, kitap gönderiminde fire-and-forget hesaplanıyor (asla cache'lenen bir okuma yolunun içinde değil - model ilk kullanımda birkaç saniye ısınma gerektirebiliyor). `getSimilarBooks()` artık daha geniş bir skor-sıralı aday havuzu çekip gerçek içerik/tema benzerliğine göre (cosine similarity) yeniden sıralıyor, embedding'i henüz olmayan kitaplar (mevcut kataloğun çoğu, yeniden kaydedilene/onaylanana kadar) eski skor sıralamasına düşüyor.

**"Son Baktıkların" (recently viewed books shelf) built (2026-08-22, committed `95e4d37`).** Purely client-side (localStorage) - no schema, no v1 equivalent. A book-page tracker records views invisibly; a homepage shelf reads them back as a `BookCover` row, deliberately per-browser (not per-account) since this is lightweight browsing convenience, not data needing to sync across devices. Verified via Playwright + DB: absent with empty storage, appears after a real book visit, absent in a fresh isolated browser context.

**/rss.xml built (2026-08-22, committed `8269d0d`).** Plain RSS 2.0 feed over the blog list's existing public query (approved posts only), linked from `/bloglar`. Verified via a fresh dev server + curl (to sidestep the underlying query's own cache): a real approved post appears correctly, an unapproved one is excluded, and title/preview XML-special characters are properly escaped.

**Üretimde work_id/work tablosu onarımı (2026-08-22, devam ediyor).** VPS yükseltmesi sırasında sunucu beklenmedik şekilde yeniden başlatıldı (uptime 02:05'te başlıyor); bu, `book.work_id` sütununun ve `work` tablosundaki satırların büyük çoğunluğunun (133245735 max id'ye rağmen sadece ~2.28M satır kalmış) kaybolmasına yol açtı - muhtemelen commit edilmiş ama disk'e tam flush edilmemiş InnoDB sayfalarının crash-recovery sırasında geri alınması. **Kitap verisi (book tablosunun kendisi, 97.9M satır) sağlam** - sadece bu yardımcı/ikincil tablo etkilendi. Kullanıcının onayıyla onarım başlatıldı: `work` tablosu TRUNCATE edildi, `_merge_bulk_progress` checkpoint'leri sıfırlandı, `book.work_id` sütunu yeniden eklendi (anında, MySQL 8 instant DDL), index yeniden oluşturuldu (2m46s), foreign key constraint ekleme işlemi şu an arka planda çalışıyor (97.9M satır üzerinde, HDD'de saatler sürebilir). Tamamlandığında backfill-work-id.sh ve work-grouping.sh sırayla baştan çalıştırılacak (ilk backfill geçen sefer ~10.5 saat sürmüştü). Ayrıca fark edildi: VPS yükseltmesinin arka plan `work-grouping.sh` işini de durdurduğu (reboot'ta systemd servisi olarak kayıtlı değildi) - kullanıcı onayıyla yeniden başlatıldı, sonra bu şema sorunuyla karşılaşıldı.

**Güncelleme (2026-08-22 20:22): FK constraint tamamlandı, backfill yeniden başlatıldı.** `ALTER TABLE book ADD CONSTRAINT FK_book_work ...` yaklaşık 1 saat 15 dakika sürdü ve başarıyla tamamlandı (`SHOW CREATE TABLE book` ile doğrulandı, disk 91GB kullanımda/103GB boş - sağlıklı). `work` tablosu hâlâ 0 satır (beklenen - TRUNCATE edilmişti). `/root/backfill-work-id.sh` yeniden `nohup ... & disown` deseniyle (SSH bağlantısından bağımsız) başlatıldı (PID 18368), ilk birkaç saniyede 130.000 satır işlendi (~2600 satır/sn, dokümante edilen hızla uyumlu) - tüm tablo için tahmini ~10.5 saat. Tamamlandığında `work-grouping.sh` sırayla başlatılacak.

**Güncelleme (2026-08-22 23:57): backfill tamamlandı, work-grouping.sh başlatıldı.** `/root/backfill-work-id.sh` 20:22'de başlayıp 23:57'de bitti (~3 saat 35 dakika - dokümante edilen ~10.5 saatlik tahminden çok daha hızlı, muhtemelen bu geniş disk/temiz tablo koşulunda binlog purge/checkpoint overhead'i daha düşüktü). `work` tablosu artık 97.668.155 satır - `book` tablosunun satır sayısıyla uyumlu, beklenen 1:1 durum doğrulandı. `_merge_bulk_progress`'teki `work_grouping_*` checkpoint'lerinin (erken, onarım-öncesi başarısız denemeden kalan) zaten sıfırlanmış olduğu doğrulandıktan sonra `/root/work-grouping.sh` aynı `nohup ... & disown` deseniyle başlatıldı (PID 167641) - phase 1 (edition'ları root kitabın work_id'sine yeniden atama) saniyede ~10.000 satır hızıyla ilerliyor, sağlıklı. Bu iş bitince "ortak kitap puanı" (pooled rating) özelliği prod'da gerçek veriyle çalışmaya başlayacak - şu ana kadar her sayfada null dönüyordu.

**Güncelleme (2026-08-23 07:33): work-grouping.sh de tamamlandı - work_id boru hattı (FK constraint → backfill → grouping) baştan sona bitti.** Faz 1 (edition'ları root kitabın work_id'sine yeniden atama) ve faz 2 (artık gereksiz kalan work satırlarını silme) toplam ~7 saat 34 dakika sürdü (23:58:43 - 07:33:10), ortalama ~4900 satır/sn. `work` tablosu 97.668.155'ten **43.180.386** satıra düştü (~54.5M satır silindi - bu, aynı kitabın farklı baskılarının artık ortak bir work_id paylaştığının doğrudan kanıtı, PLAN.md'nin daha önce read-only sorguyla doğruladığı "%55.7 oranında `original_book_id` zinciri var" bulgusuyla tutarlı bir büyüklükte). Disk boyunca sağlıklı kaldı (~95-99GB boş). Sonuç: "ortak kitap puanı" (pooled rating, `getWorkPooledScore()`) özelliği artık production'da gerçek veriyle çalışıyor - v2 kodunda zaten hazır bekliyordu, sadece veri eksikti. **Sıradaki doğal adım**: bu mekanik arka plan işi artık bitti; production cutover (bekleyen şema migration'larının gerçek prod'a uygulanması, ardından Caddy reverse-proxy geçişi) konuşulması gereken bir sonraki konu - ama bu, kullanıcının açık onayı olmadan tek taraflı başlatılacak bir şey değil.

**Mobil uyum kontrolü + gerçek bir erişilebilirlik hatası düzeltmesi (2026-08-22, committed `f03c838`).** Maintainer'ın açık isteği üzerine Playwright + iPhone 13 cihaz profiliyle gerçek bir mobil geçiş yapıldı. Yatay taşma sorunu bulunamadı (tüm sayfalar temiz), ama Next.js'in kendi dev overlay'i anasayfada "2 Issues" gösteriyordu - kaynağı: Base UI'nin `Button` component'i `render={<Link .../>}` veya `render={<a .../>}` ile bir link/anchor'a dönüştürüldüğünde `nativeButton={false}` prop'u eksikti (10 kullanım yeri, 7 dosya) - gerçek bir erişilebilirlik regresyonu (klavye/ARIA davranışı yanlış varsayılıyordu), sadece konsol gürültüsü değil. Hepsi düzeltildi, dev overlay'in "2 Issues" göstergesi kayboldu (doğrulandı).

**Admin paneli kalite geçişi (2026-08-22, committed `c4fc959`/`c32cb97`/`4ccb5f5`) - maintainer'ın açık isteği.** Ana `/admin` sayfası düz 25 araçlık bir liste yerine 5 kategoriye ayrıldı (Bekleyen İşler, İçerik Kataloğu, Kullanıcı ve Topluluk, Pazaryeri ve Ödeme, Site ve İletişim) ve 5 kuyruk-şekilli araca (kitap onayları, blog moderasyonu, çözülmemiş şikayetler, açık reklam/destek talepleri) gerçek zamanlı kırmızı sayı rozetleri eklendi (`getAdminDashboardCounts()`, düz COUNT(*) - bu kuyruklar moderasyon ölçeğinde, ~98.5M satırlık book tablosu gibi bir COUNT(*) tehlikesi yok). Destek/Reklam Talepleri sayfalarına Açık/Çözüldü/Tümü durum filtresi tab'ları eklendi (varsayılan: Açık). Ayrıca gerçek bir navigasyon eksikliği bulundu ve kapatıldı: 24 admin alt sayfasının HİÇBİRİ dashboard'a geri dönme linki içermiyordu (tek yol header'ın kolayca gözden kaçan "Yönetim" linkiydi, mobilde daralmış nav'da hiç görünmüyordu) - yeni paylaşılan `AdminPageHeader` component'i her sayfaya "← Yönetim Paneli" linki ekledi. Playwright + DB ile doğrulandı: gerçek bekleyen kayıtlar için rozetler doğru sayıyı gösteriyor, filtre tab'ları doğru satırları gösterip gizliyor, 25 alt sayfanın hepsi 200 dönüyor ve çalışan bir geri dönüş linkine sahip.

**All 13 items from the "what else could be added" brainstorm list are now built, verified, committed, and pushed.** This closes out the maintainer's "think through this list in detail and build it" ask in full - see each numbered entry above for what was built, the judgment calls made, and the real bugs found and fixed along the way (the SSE/duplicate-key bugs in particular were only found because verification kept going against a real production build rather than stopping at "looks right in dev").

**Advertiser-facing impression/click stats built (2026-08-21, committed `1baa554`) - a genuine requirements-dump gap, not a brainstorm idea.** The customer's own notes explicitly asked for this ("Advertiser-facing stats page... to justify ad placement to prospective advertisers") and it was never built - the ad system had zero tracking. Migration 0015: `advertisement.impressions`/`clicks` denormalized counters. `getActiveAd()` increments impressions whenever it actually serves an ad; new `/api/ad-click/[id]` route counts a click then redirects to the real `linkUrl` - `AdSlot`'s link now routes through it instead of linking directly. `/admin/reklamlar` shows impressions/clicks/CTR per ad. A client-facing "reklam ver" landing page using these same numbers is the natural next step once there's a real advertiser audience to pitch - not built now since none exists yet. Verified via Playwright + DB: a real homepage view incremented impressions, a real click incremented clicks exactly once and redirected correctly.

**Full user-account deletion cascade built (2026-08-21, committed `5714b26`, follow-up fix `c08212d` on 2026-08-22) - closes the "Deliberately NOT ported: deleteUserAdmin()" item flagged earlier in this file as deliberately deferred, "the last requested item" from the maintainer's session-opening four-item list.** Ported v1's real `UserController::deleteUserAdmin()` (SuperAdmin-only, an empty `Permission` allow-list) but found its actual cascade was dangerously incomplete for v2's schema: v1 only ever handled comments/sub-comments, notifications, blog ownership, and store picture *files* - a direct grep of every `.references(() => user.id)` in `schema.ts` found the real schema has **34 foreign keys across those tables referencing `user.id`**, most with no `ON DELETE` clause at all (confirmed against the live shadow DB's `information_schema.REFERENTIAL_CONSTRAINTS`, not assumed from `schema.ts` alone, since hand-written migrations can drift from it) - v1's version would have thrown a raw FK violation (`ER_ROW_IS_REFERENCED_2`) on almost any real account (anyone who ever rated a book, followed someone, sent a message, or listed something). `src/db/queries/user-delete.ts`'s `deleteUserAccount()` handles the full real dependency graph: comments/replies (2-level nested, matching v1's exact tree-walk) with their `comment_like`/`notice` rows cleaned up first, notifications both directions, follow/library/score/read/read_purpose/comment_like/store_favorite/seller_payout_profile deleted outright, blog ownership and notice reporter/reported columns nulled (audit-trail-preserving, not destructive), chat+message deleted outright (a 1:1 chat structurally can't represent "one party left" once both `first_user_id`/`second_user_id` are `NOT NULL`), and store listings deleted (rows + picture files) **only if the listing has zero order history** - `deleteUserAccount()` refuses outright (throws, doesn't silently skip) if the target has any `store_order` as buyer or seller in any status, since `store_order`'s three user/store FKs are all `NOT NULL` with no cascade and honoring the deletion would either destroy financial history or hit an unresolvable conflict; a real business-rule guard, not an invented obstacle. `user_badges`/`user_book`/`user_writer`/`user_translator`/`point_transaction`/`weekly_winner`/`two_factor_recovery_code` needed no manual handling at all - confirmed already `ON DELETE CASCADE` at the DB level via the same `information_schema` query. Gated `requireRole([])` (SuperAdmin-only, matching v1's exact restriction) at both the page and the `deleteUserAccountAction` Server Action layer, wired as a "Hesabı Sil" button on `/admin/kullanicilar` with a native `confirm()` guard. **Follow-up fix (`c08212d`)**: re-verifying this cascade end-to-end (not just re-reading it) with a seeded notice-on-comment scenario found `notice.commentId` rows tied to a deleted comment were never cleaned up - `notice.commentId` is a plain `bigint` column with no actual DB-level FK (confirmed via the same `information_schema` check, a deliberate app-only relationship per `notices.ts`'s own design), so this wouldn't have crashed the deletion, just left a permanently-dangling row `getNotices()` silently skips forever (`if (!commentRow) continue;`) - fixed to match v1's original behavior, which did delete these. Verified via a real Playwright + direct-DB test: seeded a victim account with comments, a reply from another user, comment likes both directions, follows both directions, read/library/score rows, a chat+message, a store listing with a picture and a favoriter, and a notice on one of the victim's comments - deleted the victim through the real `/admin/kullanicilar` UI as a real SuperAdmin account, then confirmed every one of those 15+ traces was gone, the *other* user's own unrelated comment and account survived untouched, and the once-orphaned notice row was gone too.

**AI-based hard-block moderation for comments and messages built (2026-08-22, committed `979f16c`) - maintainer's explicit final instruction on the "yerel AI" thread: "Mesajlarda ve yorumlarda da ai kontrol olsun. Küfür uygunsuz içerik vs vs engellensin" (comments AND messages need real AI checking, profanity/inappropriate content should be genuinely BLOCKED, not just flagged).** Replaces the existing soft-flag-for-admin-review path (`autoFlagComment`, now dead code but left in place since some historical `notice` rows may still reference its `auto_flag_*` types) with a real hard gate in `checkModerationOrThrow()`: `findFlaggedWords()` keyword match OR a local embedding-similarity abuse check (`src/lib/moderation.ts`) rejects the post outright, before it's ever written to the DB. Reuses the exact same local/self-hosted embedding model already proven for Book DNA (`Xenova/paraphrase-multilingual-MiniLM-L12-v2`, CPU-only ONNX runtime via `@huggingface/transformers`, confirmed earlier this session the VPS has no real GPU) - nearest-neighbor cosine similarity against small reference sets of abusive vs. neutral Turkish example sentences, with a 0.08 margin requirement so a near-tie falls through rather than false-positive-blocking a legitimate comment. Wired into every comment/reply/quote write path (`shareEntityComment`, `addEntityComment`, `addSubComment`, `updateComment`, `updateSubComment`) and `sendMessage`. `src/instrumentation.ts` warms the embedding model once at server startup (`register()` hook, gated on `NEXT_RUNTIME === "nodejs"`) to avoid a 20+s cold-start penalty on the first real check.

Debugging note worth remembering: initial Playwright verification appeared to show the feature silently failing to block an obviously-abusive test comment, across a dev-mode HMR reload, a full dev-server restart, AND a real `next build && next start` production run - which looked like a genuine, elusive bug. It wasn't. The actual cause was in the *test script*: it prefixed the abusive text with a random tracking token (e.g. `"ZzBadComment11787371110227 sen tam bir salaksın"`) so the DB row could be found for cleanup. That nonsense token got embedded as part of the classified sentence and diluted the cosine-similarity score below the 0.08 block margin (confirmed via an isolated side-by-side embedding comparison: 0.428 margin for the clean phrase alone vs. 0.072 with the tracking prefix prepended - a real, measurable effect of this particular embedding model's mean-pooling being sensitive to unrelated leading tokens on short sentences). The product code was correct throughout. Fixed the test to submit clean text and identify its own rows via `target_id`/`user_id` (or `sender_user_id`/`receiver_user_id` for messages) instead of a `LIKE`-matched prefix baked into the content. **Practical implication for future moderation-adjacent test scripts**: never prepend a tracking/identifier string directly into text that gets fed to this embedding classifier - use a separate correlation key (a real column) instead. Verified end-to-end via Playwright + direct DB reads against a real production build: a normal review posts successfully, a rule-listed-word comment ("sen tam bir salaksın" - "salaksın" isn't itself in `DIRTY_LIST`, this is the AI catching an inflected form the whole-word keyword scan can't) is rejected with a visible error, an AI-only case with no listed keyword at all ("defol git buradan seni iğrenç yaratık") is rejected, and the identical two-tier check on `sendMessage` blocks the same abusive text in a real DM.

**Support/destek AI scope note**: per the maintainer's explicit call earlier this session (see the "Phase 7 kararı netleşti" entry above), the support/destek surface deliberately stays non-AI (`support_ticket` + static FAQ + admin queue, already built) - "destek kısmı için sabit mesajlar ekleriz onları iletir." The maintainer's later "sitedeki denetlemeler ve destek vs için de kullanılacak" instruction is satisfied by: Book DNA (recommendations/similarity) + this moderation feature (denetleme/audits) for the AI-requiring parts, and the already-built static ticket system for destek - not a semantic-search FAQ matcher, which would reintroduce AI scope the maintainer explicitly declined to build for support.

**Semantic ("search by meaning") results on the search page built (2026-08-23, committed `11b4964`) - first of two features the maintainer asked for next, after explicitly confirming everything else in the project's scope was closed.** Reuses the exact same local embedding model already proven for Book DNA rather than any new infrastructure - `semanticSearchBooks()` in `book-embedding.ts` embeds the search term and ranks the (currently small, grows over time) `book_embedding` table by cosine similarity, shown as a new "Konusuna Yakın Kitaplar" section on `/ara` alongside (never replacing) the existing prefix-only `LIKE` search - same deliberate FULLTEXT-avoidance reasoning as ever, this is a genuinely different capability (theme/plot matching) layered on top, not a fix to that search. **Similarity floor calibrated empirically, not guessed**: a standalone 5-book/5-query spot-check (`1984`/`Simyacı`/`Suç ve Ceza`/`Yüzüklerin Efendisi`/`Küçük Prens` against both on-topic and off-topic queries) found genuine thematic matches cluster at 0.55-0.63 cosine similarity, while this model's baseline noise for clearly-unrelated pairs still reaches 0.35-0.43 (an "accounting textbook" query scored 0.43 against a fantasy novel) - an initial 0.35 floor would have let that exact false positive through, so the real floor is 0.5. Same coverage caveat as Book DNA: only books with a computed embedding are searchable this way (new/re-approved submissions going forward), not backfilled across the existing ~98.5M-row catalog at once. Verified via Playwright + direct DB: seeded a real book about "gözetim toplumu/totaliter rejim" (surveillance/authoritarianism) with a real computed embedding, confirmed it surfaces for a semantically related query with zero literal word overlap, and confirmed it does NOT surface for an unrelated query ("yemek tarifleri" - recipes) - the calibrated floor holding in a real end-to-end check, not just the standalone spot-check.

**Book club points wiring closed (2026-08-23, cloud-agent follow-up, branch `v2-cloud/book-club-points`) - closes the "documented, easy follow-up" gap flagged in the entry below.** Book clubs shipped with discussion already earning points for free (via the shared `addEntityComment`/`CommentTargetType` machinery), but the club-membership actions themselves (creating a club, joining one) earned nothing - PLAN.md explicitly deferred this ("would need extending POINT_VALUES/PointSettingKey... left as a documented, easy follow-up"). Added `clubJoin: 1` to `POINT_VALUES` (same tier as `follow`/`libraryAdd` - a lightweight social-join action, not content creation), a matching row in `PointSettingsForm`'s `FIELDS` so it's admin-editable like every other point source, and `awardPoints()` calls in `createBookClub()` (owner's implicit self-join) and `joinClub()` (any other member), both keyed `club_join:${userId}:${clubId}` so double-awarding is structurally impossible via the same `UNIQUE(user_id, reason_key)` mechanism every other point source relies on. **Cloud-sandbox scope**: verified via `npx tsc --noEmit` (clean, after running `npx next typegen` - a fresh clone has no `.next/types` yet, needed for Next 16's generated `PageProps`/`LayoutProps`) and `npm run lint` (zero new errors/warnings in the touched files; the lint run's pre-existing errors are all in unrelated files). **Not verified here, needs a local/DB session**: an actual Playwright + DB check that joining/creating a real club produces exactly one `point_transaction` row per user per club (not double-awarded, e.g. if `joinClub`'s duplicate-membership early-return path is ever hit) and that the new `clubJoin` field renders/saves correctly on `/admin/puan-ayarlari` against a real seeded admin session - the sandbox has no DB to exercise either against.

**Kitap Kulübü (book clubs / group reading) built (2026-08-23, committed `75813c2`) - second of the two features the maintainer asked for next.** Not a v1 port - a genuinely new social feature, deliberately built on existing infrastructure rather than a bespoke discussion system: extended `CommentTargetType` (`comments.ts`) with `"bookClub"`, so club discussion gets the same 2-level reply threading, AI hard-block moderation, hashtag mentions, and comment-likes every other comment surface already has, for free. New `book_club`/`book_club_member` tables (migration 0026, local shadow DB only) - a club has a name/description/visibility and an optional "currently reading" book; membership is a simple join table with an `owner`/`member` role. `visibility` ("public"/"private") only controls whether a club shows up in the `/kulupler` browse listing, not access control - a "private" club still works for anyone with the direct link, matching the same lightweight "unlisted, not access-gated" pattern already used elsewhere in this app rather than building a separate invite/approval system from scratch. `owner_id` is nullable with `ON DELETE SET NULL` (not `CASCADE`) - mirrors `blog.owner_id`'s philosophy that the club and its membership/discussion outlive the founder leaving or being deleted, it just loses whoever could manage its settings; an owner can't leave their own club without deleting it first (has to make sense of "who's in charge" or explicitly end it). Pages: `/kulupler` (search+pagination browse, matches the Askıda Kitap browse pattern), `/kulup/yeni` (create form with an `EntitySearchPicker`-based optional current-book selector), `/kulup/[slug]` (detail: join/leave button, member list linking to real profiles, owner/Admin/Mod-only current-book picker, discussion via the shared `EntityComments` component, delete button). Header nav link added. Deliberately NOT wired into the gamification points system this pass - would need extending `POINT_VALUES`/`PointSettingKey` (touching the admin point-settings page's types too), a real scope expansion beyond the core "group reading" ask, not a small addition; left as a documented, easy follow-up rather than rushed in. Verified end-to-end via Playwright + direct DB across two real accounts: club creation auto-adds the creator as owner, setting/changing the current book works through the real UI, a second account joins and posts to the discussion, **the reused moderation infrastructure genuinely carries over** - an abusive message in the club discussion gets hard-blocked exactly like a book comment would, confirming this wasn't just a superficial type-string extension - a member leaves cleanly, the owner is correctly blocked from leaving, and owner-initiated deletion cascades membership rows correctly.

**ISBN normalization built (2026-08-23, cloud sandbox) - closes a real gap in Phase 5's stage 1.** `findDuplicateCandidatesDryRun()`'s ISBN-exact-match stage compared `book.isbn` (a free-text `varchar(50)`, confirmed via `schema.ts` - no format constraint at the DB level) as a raw string, which misses two genuinely common real-world cases: the same book listed once under its ISBN-10 and once under its ISBN-13 equivalent (different digit strings for the same identifier), and cosmetic differences like hyphens/spacing between two rows that are otherwise the same ISBN. New `normalizeIsbn()` in `src/lib/duplicate-detection.ts` - strips everything but digits/`X`, validates the real ISBN-10 or ISBN-13 check digit (returning `null` for anything structurally invalid rather than treating a typo as a match key, same "don't emit a false signal" spirit as `normalizeTitle`'s empty-string return), and canonicalizes ISBN-10 to its ISBN-13 equivalent so both forms of the same book now group together. Wired into stage 1 of `findDuplicateCandidatesDryRun()` in place of the old raw-string comparison. Pure function, zero DB access - verified against 9 known input/output pairs (a real ISBN-10/ISBN-13 pair for a real book plus a hyphen-variant of the ISBN-13 form, an ISBN-10 with an `X` check digit, a mistyped check digit, and non-ISBN garbage), all passing. **Local/DB verification done (2026-08-23)**: seeded two real book rows for the same actual title (The Hobbit) - one with its ISBN-10 (`0261102214`), one with a hyphenated ISBN-13 (`978-0-261-10221-7`) - plus an unrelated third book with its own valid ISBN, hit `findDuplicateCandidatesDryRun()` through a temporary debug route (removed after), and confirmed the two Hobbit rows land in the same `isbn` group keyed by the correctly-normalized `9780261102217`, while the unrelated book does not. Also hand-verified the ISBN-10→13 conversion arithmetic against this same real, publicly-known ISBN pair. The standing Phase 5 caution still applies (no index on `book.isbn` on the real ~98.5M-row prod table, so this stays a bounded dry-run/pure-logic tool until the indexing/batching design for the real bulk backfill is done separately) - only the pure-logic/dry-run layer is now confirmed correct end-to-end.

**`normalizeTitle()` word-boundary bug fixed (2026-08-23, cloud sandbox, branch `v2-cloud/normalize-title-word-boundary`) - a real correctness gap in the edition-noise-phrase stripping added for Phase 5's stage 2 (normalized-title match).** The noise-phrase loop (`cilt`/`baskı`/`basım`/etc.) used a raw `String.replaceAll(phrase, " ")` - a plain substring replace, not word-boundary-aware - so it also strips those phrases out of unrelated real words that happen to contain them, silently eating meaningful title content instead of just the edition/print-run marker it's meant to remove. Concretely reproduced before fixing: `normalizeTitle("Türkiyede Basımevi Tarihi")` (a real title about printing-house history) collapsed to `"türkiyede evi tarihi"` - "Basım" vanished mid-word - and `normalizeTitle("Kitabın Baskısı")` collapsed to `"kitabın sı"`. Fixed by matching each noise phrase only when not adjacent to another letter/digit (`(?<![\p{L}\p{N}])phrase(?![\p{L}\p{N}])`, unicode-aware since plain `\b` doesn't understand Turkish letters), so it only strips the phrase as a standalone token/word, never as a fragment of a longer one. Pure function, zero DB access - verified via a standalone `tsx` script (same cloud-sandbox pattern as the ISBN-normalization work): the two real-world false-positive cases above now correctly preserve their content, while every existing "Cilt 2"/"2. Baskı"/"Özel Baskı"/"Karton Kapak"/"Genişletilmiş Baskı"/parenthetical-remark/Turkish-casing stripping behavior is unchanged (regression-checked), plus a sanity check that two differently-labeled editions of the same real title still score >0.95 similar via `stringSimilarity`/`titleAuthorSimilarity` after the fix. **Note found but deliberately not touched here**: while writing this verification script, confirmed the separate all-numeric-title bug (`normalizeTitle("1984")` -> `""`) is real but already fixed on the still-open `v2-cloud/unit-tests-pure-logic` PR (#3, not yet merged into `v2-rewrite`) - left alone to avoid duplicating that PR's fix; the two changes touch different parts of the same function and should merge cleanly in either order. **Not verified here, needs a local/DB session**: this only changes an in-memory pure function never yet wired into a real bulk scan (per this file's standing Phase 5 caution about the missing `isbn`/title index on the real ~98.5M-row prod table) - no DB-facing behavior to check. *(Local session update: both this and PR #3 have since been merged into `v2-rewrite`, in this order - `npx vitest run` confirms no regression, see the entry below.)*

**Unit tests for `csv-import.ts`/`iso-week.ts` added (2026-08-23, cloud sandbox, branch `v2-cloud/unit-tests-csv-iso-week`) - extends the vitest coverage started on the still-open `v2-cloud/unit-tests-pure-logic` PR (#3) to two more pure, DB-free modules that PR didn't touch: `parseCsv`/`parseGoodreadsExport`/`mapGoodreadsShelf` (Goodreads CSV import) and `getISOWeekString`/`getISOWeekRange`/`currentISOWeek` (the leaderboard's week-key logic).** 25 new tests covering: quoted-field/embedded-comma/embedded-newline/doubled-quote/CRLF/no-trailing-newline CSV edge cases, header-order-independent Goodreads column mapping, the three real Goodreads shelf values plus an unmapped-shelf fallback, and the ISO week calendar's two trickiest real cases - an early-January date that belongs to the *previous* year's week 52 (2023-01-01, a Sunday) and a late-December date that belongs to the *next* year's week 1 (2024-12-31) - plus a `getISOWeekRange` round-trip and its own year-boundary check. No bugs found this pass; both modules' logic held up under adversarial edge-case testing. **Overlaps with PR #3**: both branch off `v2-rewrite` before #3 merged, so both add the same `vitest`/`"test"` script to `package.json` - trivial to reconcile (identical lines) whichever merges second, flagged here so it isn't a surprise. Verified via `npx vitest run` (25/25 passing), `npx tsc --noEmit` (clean), `npx eslint` scoped to the two new test files (clean). **Nothing DB-related** - both modules are pure and this is fully cloud-verifiable; no local/DB follow-up needed for this piece.

**Local session merge round-up (2026-08-23): four cloud-agent branches reviewed and merged into `v2-rewrite`** while the maintainer's machine was offline (power outage) - `fix-lint-errors`, `normalize-title-word-boundary`, `unit-tests-pure-logic`, `unit-tests-csv-iso-week`. All four independently verified in the cloud sandbox (tsc/lint/vitest, no DB access); this pass re-verified them together in one tree - `package.json`'s identical `vitest`/`"test"` addition from the two test branches merged without conflict as predicted, `duplicate-detection.ts`'s two independent edits (word-boundary fix + all-numeric-title fix) auto-merged cleanly since they touch different parts of the function, and the only real conflict was two PLAN.md entries appended at the same point (trivially resolved, both entries kept). `npx vitest run` passes all 84 tests (59+25) against the fully-merged tree, `npx tsc --noEmit` and a full `npm run build` are both clean. This is the first time this project has an automated, cloud-agent-runnable regression suite - a genuine capability upgrade, not just four isolated fixes.

**Homepage prerender error fixed + a real server-crash bug found and fixed along the way (2026-08-23, committed `a1c7e07`) - the maintainer spotted the Next.js dev overlay's error screen and asked to have it fixed.** Two separate issues: (1) `BookOfMonthWidget`'s DB read (participant count, active pick) genuinely varies per-request and isn't cacheable, which Next 16's Cache Components correctly flagged as an "unstable value" during prerendering - fixed with `await connection()` (already imported/used elsewhere in `page.tsx` for the weekly-leader section, so this just extends the same established pattern), inside the already-present `<Suspense>` boundary. (2) Investigating surfaced something much worse: the two PWA manifest icon routes (`/manifest-icon-192`, `/manifest-icon-512`) used `next/og`'s `ImageResponse`, which pulls in `sharp` at runtime - and `sharp`'s native binary fails to load on this machine (`ERR_DLOPEN_FAILED`, likely a missing VC++ redistributable dependency for its bundled `libvips` DLL, confirmed the file itself wasn't corrupted/wrong-sized, reinstalling didn't fix it, this is a genuine native-binary/environment issue not a code bug). **Reproduced the crash twice: the entire dev server process died, not just that one request** - and browsers request PWA manifest icons automatically on every page load (via the `<link rel="manifest">` tag), so this would have silently crashed a real deployed site on essentially every visit once cutover happens, not just a rare edge case. Fixed by replacing both dynamic routes with static PNG files in `/public` (generated once via a headless-browser screenshot at the exact same size/text/colors - pixel-equivalent to the original `ImageResponse` output), since a fixed "DK" brand icon never actually needs to be generated per-request in the first place - removed the now-unnecessary `sharp` dependency again after confirming nothing else in the app uses `next/og`. **General lesson worth keeping**: prefer static files over runtime-generated images for genuinely static assets - it's not just simpler, it avoids exactly this class of native-dependency fragility that a purely-logical code review would never catch. Verified: dev server survives repeated direct requests to both icon paths (previously crashed on the very first hit) and normal page loads that trigger the manifest fetch automatically, and a full `npm run build` completes with zero prerender errors across the entire route tree (confirmed no other page has the same "Blocking Route" issue).

**Unit test suite added for the pure-logic library functions (2026-08-23, cloud sandbox, branch `v2-cloud/unit-tests-pure-logic`) - closes the "no existing automated test suite" gap for exactly the code class PLAN.md itself calls out as "fully unit-testable in isolation".** Every real fuzzy-matching/parsing/permission function built across this project's history (`duplicate-detection.ts`'s `normalizeTitle`/`stringSimilarity`/`titleAuthorSimilarity`/`normalizeIsbn`, `hashtag.ts`'s parser, `dirty-controller.ts`'s two filters, `roles.ts`'s `hasRole`/`isProtectedFromRoleChange`, `category-names.ts`'s translator) had only ever been "verified against known input/output pairs" ad hoc during the session that built it, then left with no regression coverage - any later refactor could silently break one with nothing to catch it. Added `vitest` (zero new runtime dependency, dev-only) with 59 tests across 5 new `*.test.ts` files living next to their source files, covering the documented edge cases each function's own comments call out (Turkish-casing normalization, whole-word-vs-substring dirty-word matching, SuperAdmin/Kurucu's asymmetric permission bypass, bare-# hashtag fallthrough, ISBN-10↔13 canonicalization). **Found and fixed one real, previously-undetected bug while writing these**: `normalizeTitle()`'s "strip bare standalone volume/edition numbers" step didn't distinguish a trailing edition marker (correctly stripped, e.g. "Cilt 2") from a title that is *itself* entirely numeric - the real, famous book "1984" normalized to an empty string, which would have made it collide with every other purely-numeric title (or any other title that happened to fully strip to nothing) in the duplicate-detection pipeline instead of comparing meaningfully. Fixed by only dropping bare-number tokens when non-numeric tokens remain afterward; added `npm run test` (`vitest run`) as a new script. **Cloud-sandbox scope, per the standing constraints**: verified via `npx vitest run` (59/59 passing), `npx tsc --noEmit` (clean, after `npx next typegen`), and `npx eslint` scoped to every touched file (clean - the full-project `npm run lint` run has pre-existing errors/warnings, all in unrelated files not touched here, matching the same caveat noted in the book-club-points entry). **Not verified here, needs nothing from a DB/local session** - these are pure functions with zero DB dependency, so this piece is fully self-contained; the one thing worth a maintainer's eyes is whether `vitest` should also be wired into a CI check (none exists in this repo yet) now that a real test suite exists to run.

**Production cutover started (2026-08-23), committed `3036e4a`/`42c1978`, pushed - maintainer's explicit instruction to do the cutover if no other feature work remained, with cover-image correctness called out by name.** With the v1-parity sweep and every requested feature exhausted, moved to actually deploying v2. Sub-steps:
- **Production DB backup**: `mysqldump --single-transaction --quick --routines --triggers | gzip`, started on the VPS at 14:58, finished at 15:42 (44 minutes, exit code 0, 7.3GB) - much faster than an initial ~68GB/6-hour estimate extrapolated from an early growth-rate sample, which was simply wrong (gzip compressed far better than that early sample suggested). **Real process note, not a technical problem**: the session's monitoring loop failed to notice the backup had actually finished for over an hour after the fact - when the maintainer asked "why is this taking so long", the honest answer was the monitoring, not the backup itself.
- **v2 had no Dockerfile at all** - wrote one (`node:22-bookworm-slim`, two-stage, `npm install` not `npm ci` due to a cross-platform lockfile quirk, deliberately not `output: "standalone"` because of `onnxruntime-node`'s known file-tracing gaps, both documented in-file).
- **Found and fixed a real, previously-undetected bug class**: 10+ pages/components (`/rss.xml`, `/reklam-ver`, `/rozetler`, `/yazarhane`, `/listeler`, and six of the homepage's own sections - Categories/Featured/LatestBooks/TrendingBooks/TopReaders/ActivityFeed) called a DB query during their Suspense-wrapped render with no preceding `connection()` call. Next 16's Cache Components tries to populate these during `next build`'s static-prerender pass, which needs a live DB connection at BUILD time - every previous "clean build" claimed this session was only true because `.env.local` (correctly gitignored/dockerignored) happened to be present on this dev machine; a real Docker build (no env vars, the actual representative deployment condition) is what finally surfaced it. Fixed each by adding `await connection()` as the first line of the affected async component, confirmed via a fast local iteration technique (`DATABASE_URL` pointed at an unreachable port) until the build succeeded with zero prerender errors, then reconfirmed with a real `docker build`.
- **Cover-image proxy bug fixed and verified in two real running containers** (local, then production): `kapak/[id]/route.ts` was caching fetched covers under `os.tmpdir()` instead of the `uploads/` directory every other upload path uses - a container restart/redeploy would have silently wiped the cache and re-hit the real Open Library/Amazon origin every time. Fixed to use `uploads/cover-cache`. Verified locally first (real book, temporarily-set `image` URL, confirmed fetch+cache+persistence-after-DB-clear), then **re-verified against the real production container and real production data** (see below) - a genuinely different real book, real cached file, no test-data mutation needed since production already has plenty of books with real cover URLs to pick from.
- **All 26 pending schema migrations reviewed against real production state (not the migration files' own stale "NOT YET applied" headers) and applied**: a targeted `information_schema` check found migration 0001 (the `work`/`book.work_id` split) and 0004 (`point_transaction`/`weekly_winner`, empty) were **already applied to production** from earlier work this session (the work_id grouping pipeline logged separately) - real state had drifted from what the migration files themselves claimed, a good reminder that file comments are not authoritative once real deployment has happened out of band. Checked for duplicate rows that would break new UNIQUE constraints (`read(user_id,book_id)`, `score(owner_id,target_id,target_type)` - zero found) and for an existing `marketplace_settings` row before deciding to skip 0009's seed `INSERT` (one already existed). Applied the remaining 24 migrations (0002, 0003, 0005-0026) in one script after the maintainer's explicit go-ahead, verified all 33 resulting tables/columns/constraints individually via `information_schema`, and confirmed v1's `backend` container kept serving real traffic without interruption or any new exceptions in its logs throughout (a live `GET /api/books` returned real data immediately after).
- **v2 deployed and running on the VPS against the real production database** - after the maintainer's explicit go-ahead to start it: cloned `v2-rewrite` to `/root/dklist-v2-src`, added a `v2` service to `/root/dklist/docker-compose.yml` (backed up first) using the same live `dklist` database/credentials as v1's `backend` service, a freshly-generated prod `AUTH_SECRET`/`CRON_SECRET` (never the `.env.local` dev values), a persistent `v2_uploads_data` volume, bound to `127.0.0.1:3001` only (loopback - not public, `backend`'s existing `0.0.0.0:80` publish is untouched). Built and started via `docker compose build v2 && docker compose up -d v2` - clean startup, zero errors in `docker logs`. **Verified against real prod data from inside the VPS**: homepage/`/kitaplar`/`/yazarlar` all 200, a real book detail page (`/kitap/--1000053`) 200, and the cover proxy fetched a real cover for a different real book (id 35519971, a genuine Open Library cover-id URL) and confirmed the 21KB JPEG landed in the container's persistent `uploads/cover-cache/` volume - the first real book slug tried happened to be one of the ~60% with no actual cover available at the source (a known, pre-existing data-quality ceiling, not a bug), which is exactly why a second real book was used to confirm the fetch+cache path itself works.
- **What's left, in order**: (1) the DNS/routing cutover itself - `dklist.com` currently resolves to **Vercel** (v1's React frontend) and `api.dklist.com` is Cloudflare-proxied to this VPS (v1's Symfony backend); v2 is a single unified app, so cutover means repointing `dklist.com`'s DNS at the VPS, not the simple Caddy-upstream-swap this plan originally assumed for a single-VPS setup. No Cloudflare API token exists on this machine or the VPS (only the old, superseded tunnel's origin cert) - **this step needs the maintainer's own manual action in the Cloudflare dashboard**, not something to attempt unilaterally even with permission, since it's the one step that actually redirects real visitor traffic. (2) Once DNS points at the VPS, `v2`'s port binding needs to change from loopback-only to actually serving real traffic (a reverse proxy in front of both `backend` and `v2`, per this plan's original Caddy design, or a simpler direct port-80 handoff once v1 is fully retired) - not done yet, deliberately, since there's no point exposing it before DNS is ready to send real visitors there.

**CUTOVER COMPLETE (2026-08-23) - `dklist.com` now serves v2 to real visitors.** Finished the two remaining blockers from the entry above, in order, each gated on the maintainer's explicit go-ahead given the real-traffic risk:
- **Caddy reverse proxy activated on the VPS**: completed the `Caddyfile`/`docker-compose.yml` draft that had been "prepared, not applied" since 2026-08-20 - added an `http://dklist.com, http://www.dklist.com { reverse_proxy v2:3000 }` site block alongside the existing `api.dklist.com → backend:8000` one, moved `backend` off its direct `80:8000` host-port publish onto an internal-only `expose: 8000` (Caddy now owns host port 80), and added the `caddy` service (`caddy:2-alpine`, plain-HTTP router - Cloudflare Flexible mode already terminates TLS at the edge, matching the existing `api.dklist.com` setup exactly). Applied via `docker compose up -d` after explicit sign-off (this recreates the live `backend` container, a few seconds of real `api.dklist.com` downtime during the swap) - verified immediately afterward via Host-header `curl` from inside the VPS (no DNS involved yet) that both `dklist.com` and `api.dklist.com` routed correctly through Caddy to the right container, before touching DNS at all.
- **DNS repointed by the maintainer in the Cloudflare dashboard**: `dklist.com` and `www.dklist.com` A records changed from Vercel to the VPS IP (`169.58.179.117`), proxy (orange cloud) kept on - the one step this session could not do itself (no Cloudflare API token). Verified immediately after: `curl -I https://dklist.com/` now shows `Via: 1.1 Caddy` and `X-Powered-By: Next.js` (no more `Server: Vercel`), `/kitaplar`, `/yazarlar`, and `/kapak/[id]` (real 21KB JPEG, a real cover) all return correct real responses over the live public domain, and `api.dklist.com` continues serving v1's backend correctly (still needed - nothing currently retires it).
- **Not yet done, deliberately**: `v2`'s `127.0.0.1:3001` debug port publish left in place (harmless, loopback-only); v1's Vercel frontend deployment and `backend` container left running rather than torn down, since nothing about this cutover requires destroying the old stack immediately and rollback should stay cheap for a while; the schema's `book.work_id` grouping backfill covers all 97.6M rows already (done earlier this session) but Phase 5's actual fuzzy-match duplicate-merge pass across those groups has not been run.

**PUT BACK INTO MAINTENANCE MODE (2026-08-23) - real, substantial gap found right after cutover, not a false alarm.** The maintainer reported "kitap görselleri yok" (book images are missing) immediately after the cutover went live and asked for an immediate maintenance page while it was investigated - `dklist.com`'s Caddy site block was switched from `reverse_proxy v2:3000` to serving a static maintenance page (`/root/dklist/maintenance/index.html`, mounted as a read-only volume into the `caddy` container so it survives a container recreate) - `api.dklist.com`/v1 and the `v2` container itself were left running and untouched, only public routing to `dklist.com` was pulled. Root cause found: `BookCover` (`src/components/dklist/book-cover.tsx`) was built from the very start of this session as "deliberately not a photographic cover" - every book everywhere in the app (homepage, listings, detail pages) rendered a typeset/color-toned jacket instead of a real photo, and the working `/kapak/[id]` cover proxy (the piece the maintainer explicitly asked to get right before cutover) was never actually wired into any of that UI - it existed as working, tested infrastructure with zero real callers. This was an unconfirmed design decision made unilaterally early on, not something the maintainer had signed off on seeing live.

**Fixed locally (not yet redeployed - maintainer's explicit instruction: finish and verify fully locally first, cutover again only when told to).** `BookCover` now takes optional `bookId`/`hasImage` props: when a book has a real source cover (`book.image` set), it renders the real photo via `/kapak/[id]` (a small `PhotoBookCover` client subcomponent handles the `onError` fallback back to the typeset jacket if the source image 404s at the origin - the same ~40% real-coverage ceiling v1 always had); when a book has no source image at all, the original typeset jacket renders exactly as before, unchanged - **explicit maintainer requirement**: "kapak görseli olmayan kitaplar şu anki gibi kalsın, olanlarda kapak göster" (books without a cover stay as-is, show the real cover for the ones that have one). Threaded `hasImage` (a cheap `(image IS NOT NULL AND image != '')` boolean, never the raw source URL itself) through every book-listing query that feeds a `BookCover` - `books.ts`, `activity.ts`, `book-of-month.ts`, `book-detail.ts` (including per-edition and similar-books lists), `search.ts`, `book-embedding.ts`'s semantic search, `translators.ts`/`writers.ts`/`publishers.ts`, `reading-lists.ts`, and the client-only `recently-viewed` localStorage cache - and updated every JSX call site (homepage's 7 shelves, `/ara`, `/kitap/[slug]`, `/cevirmen/[slug]`, `/yazar/[slug]`, `/ayin-kitabi`, `/liste/[slug]`, `/yayinevi/[slug]`, `/kategori/[slug]`, `/kitaplar`) to pass the real `bookId`/`hasImage` through. **Real bug caught and fixed mid-implementation**: marking the whole `book-cover.tsx` file `"use client"` (needed for the photo's `onError` state) broke every Server Component's direct call to the plain `toneForId()` utility also exported from that file ("Attempted to call toneForId() from the server but toneForId is on the client") - fixed by keeping `book-cover.tsx` itself a plain server-callable module and isolating only the real interactive bit (the `<img>` + its `onError` retry-to-typeset logic) into a separate `photo-book-cover.tsx` client component, which the server-rendered `BookCover` conditionally renders and hands a pre-built typeset-jacket fallback node to. Verified against the real local shadow DB: temporarily set a real book's `image` column to a genuine Open Library cover URL, confirmed the real photo now renders on its book detail page while every book without an image (including the same page's own "Benzer Kitaplar" row) still renders the unchanged typeset design. `npx tsc --noEmit` clean, `npx vitest run` 84/84 passing, `npx eslint` clean on every touched file, and a full `npm run build` completes with zero errors/prerender issues.

**Local dev environment confirmed fully healthy end-to-end (2026-08-23)** after the maintainer's explicit "set the whole system up locally, front-to-back, don't let the DB turn into another multi-hour problem" - the existing local shadow MySQL setup (schema-only `dklist_shadow`, port 3308, from PLAN.md's Phase 0 notes) is what's used here, deliberately NOT a full import of the real production database (which would risk exactly the kind of multi-hour operation the maintainer was just burned by with the VPS backup) - confirmed both local mysqld instances (the v2 shadow DB and the older pre-v2 `D:\mysql-data`) are running stably, ~3.6TB free on `D:`, and the dev server serves the homepage/`/kitaplar`/a real book detail page/the cover proxy/`/ara` all at 200 with no errors.

**Status: dklist.com remains in maintenance mode.** Will not be switched back to `reverse_proxy v2:3000` until the maintainer explicitly asks for the cutover to happen again - this fix is verified locally only, per their standing instruction.

**RE-CUTOVER COMPLETE (2026-08-25) - `dklist.com` serves v2 to real visitors again, maintenance mode lifted.** Maintainer asked directly ("Vercel'i iptal et, v2'yi direkt sunucuda canlıya al"). Read-only recon first, since the plan's older assumptions were stale: DNS already resolved to Cloudflare/the VPS (not Vercel - must have changed during the 2026-08-23 attempt and never reverted, so "cancel Vercel" was mostly already true for real traffic), the `dklist-v2` container was real and DB-connected to production but **27 commits stale** (built from `3036e4a`, missing the cover-photo fix above plus this session's profile redesign, SEO pass, and a newly-found `comment_type` bug - see below), and of the 28 local migrations only **one** (`0028_user_publisher`) had never reached production (re-verified against the *actual* SQL in each file after an initial broad check used wrong guessed column names for several - a good reminder to verify against the real migration text, not assumed names).

Sequence: (1) applied migration 0028 to prod (`CREATE TABLE user_publisher`, zero pre-existing rows, zero risk); (2) found and fixed three more real bugs before deploying rather than after - `CommentType` was typed `"yorum"|"alinti"` (guessed Turkish literals) against a `comment_type` column that actually stores v1's real English enum values (`"comment"/"quotation"`), so every comment/quote on every book/writer/translator/club page was invisible (confirmed via the real prod data itself: 24 real rows, all `comment`/`quotation`, zero `yorum`/`alinti`) - fixed throughout `comments.ts`/`activity.ts`/`feed.ts`/every entity page's call sites; several `overflow-x-auto` + hidden-scrollbar shelves (recently-viewed, "Benzer Kitaplar", the `/kitaplar` category pills, three homepage shelves) had the same clipped-content bug already fixed on the profile page but never generalized - converted to `flex-wrap`; mobile bottom nav and the desktop header's notification/message bells used raw emoji as icons, replaced with `lucide-react` icons matching the rest of the app. (3) pulled `HEAD` (29 commits) on the VPS, `docker compose build v2 && up -d v2`; (4) smoke-tested internally via `127.0.0.1:3001` against **real production data** before any public exposure - a real book with a real comment (`küçük-prens`) rendered it, a real book with a real cover (`the-catcher-in-the-rye...`) rendered the actual photo via `/kapak/[id]` (verified `200 image/jpeg`), zero errors in `docker logs`; (5) flipped `/root/dklist/Caddyfile`'s `dklist.com`/`www.dklist.com` block from the maintenance `file_server` back to `reverse_proxy v2:3000`, `docker exec dklist-caddy caddy reload`; (6) verified the real public `https://dklist.com` end-to-end via Playwright - homepage (real `98M+`/`11M+` stats), the same real book page with its real cover photo live over HTTPS, mobile viewport with the fixed nav icons, zero errors in `docker logs` after real traffic.

**Not done, deliberately flagged as a manual follow-up**: actually deleting/canceling the Vercel project - DNS no longer points there so it's inert for real traffic, but no Vercel API token exists on this machine to do the deletion itself (same reasoning as the standing "no Cloudflare API token" limitation) - needs the maintainer's own Vercel dashboard action. `v1`'s `backend`/Vercel deployment left running untouched (cheap, harmless, keeps rollback trivial - reverting the Caddyfile edit and reloading is a single small change either direction, no DNS involved).

**Header/nav overflow fixed (2026-08-23/24) - real desktop bug, not just polish.** `SiteHeader` listed all 15 nav links in one unwrapped flex row - overflowed the viewport at ordinary desktop widths (confirmed via `scrollWidth` vs `clientWidth` at 1280/1366/1440px) and buried the 4 links a guest actually needs among engagement features. Trimmed to 4 guest-first links + a "Topluluk" dropdown (`community-menu.tsx`) for the rest, plus a new mobile-only slide-in drawer (`mobile-nav-drawer.tsx`) - mobile had zero access to this nav before (`hidden md:flex`, no fallback beyond `MobileBottomNav`'s 5 account-action links). Verified zero overflow at 320-1920px, one real Base UI warning fixed along the way (`DialogPrimitive.Close` rendered as a `Link` needs `nativeButton={false}`).

**Login/register pages redesigned (2026-08-24)** - both were a bare white Card floating in empty space, zero connection to the site's own editorial book-jacket design language. New split-panel layout (`auth-visual-panel.tsx`): desktop gets a full hero-gradient panel with a fanned 3-book `BookCover` stack + page-specific tagline/copy; mobile gets a compact version of the same fan above the form instead of losing the visual identity entirely. Cross-links between the two pages added too.

**Real local data (2026-08-23/24) - first attempt (a full 97M-row restore of the VPS backup) was abandoned after diagnosing it would take days, not hours: InnoDB secondary-index maintenance during bulk INSERT degrades badly at that scale, and `doctrine:schema:update`'s `DISABLE KEYS` trick is a MyISAM-only no-op anyway.** Replaced with a **real, bounded slice pulled directly from the VPS via targeted `mysqldump --where`**, not a synthetic seed: discovered mid-flight that `book.id` does NOT start near 1 on prod (`MIN(id)=35519971, MAX(id)=133245735`) - an initial `id<=500000` filter silently matched zero books (writer/publisher/translator/category all *do* start near 1, so those were fine) - corrected to `id BETWEEN 35519971 AND 36019971`. Also found the "small tables" full-data dump was actually 4.1GB because it accidentally included a dozen `stg_*` bulk-import-staging tables (4M-37M rows each, pure historical scaffolding the live app never queries) plus the 42M-row `work` table - excluded all of them explicitly, bringing the real payload down to ~240MB compressed. Final local mirror: new `dklist_local` database (same MySQL instance as `dklist_shadow`, which stays as a schema-only fallback) with **500,001 real books, ~480K real writers, ~472K real publishers, real category links** - `.env.local` repointed at it, dev server restarted, verified real photographic covers rendering correctly alongside typeset jackets on `/kitaplar` with real production content.

**PRODUCTION INCIDENT (2026-08-24): `doctrine:schema:update --force` on v1's every-boot backend CMD silently dropped most of the ALTER-TABLE-based v2 migrations off the real production database.** Discovered when the maintainer hit a real runtime error on `/yazar/[slug]` (`Unknown column 'user.writer_id'`) - investigating found v1's `backend/Dockerfile` has run `doctrine:schema:update --force --no-interaction` on every single container boot since before v2 existed (comment in the file even correctly reasoned about the `--complete` flag's table-dropping risk, but missed that plain `--force` still drops *columns* Doctrine's own entity mappings don't know about). v2's hand-written migrations added real columns to several tables v1's Doctrine entities also map (`user`, `read`, `chat`, `marketplace_settings`) - the Caddy-cutover-triggered backend restart earlier this session was the first boot after those landed, and schema:update silently stripped them back out, only surfacing loudly when it later reached `book` and crashed trying to drop an FK-protected index it didn't recognize (`idx_book_work`) - that crash is *why* `advertisement.*`/`notice.book_id` (processed before the crash point) survived while `read`/`score`/`comment`/`user`/`chat`/`marketplace_settings`'s ALTER-added columns didn't. **This is exactly the failure class this entire rewrite project exists to get away from** (PLAN.md's own opening paragraph). Fixed in two parts: (1) re-applied every dropped `ALTER TABLE` (re-checked duplicate-row safety first, same as the original migration pass - still zero duplicates) to restore the real production schema, verified all 15 previously-missing columns/constraints are back and survived a second backend restart; (2) removed `doctrine:schema:update --force` from `backend/Dockerfile`'s boot `CMD` entirely - it cannot safely auto-sync a database two independent apps now share, full stop; any future v1-side schema need gets a hand-written, reviewed `ALTER TABLE`, never an automatic one. Propagated the same fix to `dklist_local` (built from a schema dump taken *during* the window the columns were missing, so it inherited the same gap). Real, if likely small, cost: any user data written into the now-recreated columns during the gap window is gone (column DROP is destructive) - acceptable given the real user count is 4 and the columns were only hours old, but a genuine lesson: **restarting a container that runs `doctrine:schema:update` is no longer a "safe, boring" operation now that v2 exists** - treat any future `backend` restart with the same caution as a schema migration, not routine ops, until/unless this class of coupling is removed entirely (e.g., v1 retirement).

**Feed card redesign + site-wide "yavaşlık" root cause fixed + Kurucu role lockout fixed (2026-08-31).** Three real, user-reported problems closed in one pass. (1) Feed card: the old side-column cover ("bomboş... orantısız bir kart") replaced with a compact inline "attachment chip" and a bordered action-toolbar strip - verified against the real live "Sefiller" comment card. (2) `/kitaplar` and the admin Kitaplar/Yazarlar/Yayınevleri lists had no cache at all and re-ran full scans/estimates over multi-million-row tables on every request - confirmed via a live prod round-trip (cold ~45-80s, warm ~70-80ms) and via `EXPLAIN` that the underlying query plan was already optimal (`idx_book_viewcount` backward index scan, zero filesort) - the cost is genuine cold-disk I/O on this host, not a bad query. Added `'use cache'`/`cacheTag`/`updateTag` to all of them (publisher-admin's per-row N+1 count also batched into one `GROUP BY`), then - since a container restart wipes the whole in-process cache and the next real visitor was still paying the full cold cost - added `scripts/warm-cache.sh` and made pre-warming every hot page a mandatory post-deploy step, not optional. (3) The real production "admin" account is typed `Kurucu` (the customer's founder role), not `Admin` - `hasRole()` never gave Kurucu SuperAdmin's blanket bypass (by design, to keep `isProtectedFromRoleChange()` meaningful), but every one of the ~30 admin-panel allow-lists only listed `Admin`, so the actual founder account was silently redirected away from nearly the entire panel - this, not performance, was the real cause of most "tıklayınca açılmıyor" reports. Fixed at the single source of truth: `hasRole()` now passes Kurucu wherever Admin is allowed, verified live across `/admin`, `/admin/bloglar`, `/admin/yazarlar`.

**Verified-reader (Doğrulanmış Okur) submission flow + point store seeded (2026-08-31).** `user.verified` previously had only a bare admin toggle with no request behind it - added the real process: `/profil/duzenle` lets a user upload an ID photo + note, landing in a new `identity_verification` queue (migration 0032); `/admin/dogrulama` (Admin/Mod, appears in the sidebar with a live pending-count badge) approves/rejects, approval flips the existing `user.verified` column, both outcomes notify the user. The document image is served through a genuinely access-controlled route (`/api/identity-document/[filename]`, Admin/Mod-only, `Cache-Control: private, no-store`) rather than the public-by-filename pattern every other upload route uses - real ID-document PII, not a book cover. Separately, the point store (rewards/redemption/frame-equip infra already existed, fully built) had zero seeded rewards on both DBs - seeded 8 tiered profile frames (30-800 points), verified live with the real "admin" account's 70-point balance.

**Askıda Kitap listing bug fixed + any-format image upload -> webp site-wide + book-topic AI moderation gate (2026-08-31).** Real reported bug: the create-listing form rejected images "even in an accepted format" and wiped the entire form on any validation error (a `redirect()`-based error path is a full page navigation, losing every field including already-picked images). Root-caused: `saveUploadedImage()` (shared by every upload feature site-wide) only accepted a fixed png/jpg/webp extension list + a hand-rolled magic-byte sniff - a phone's camera photo is very commonly HEIC (iPhone default), silently rejected even though it looks like "an obviously accepted format" to the user. Replaced with `sharp`/libvips: accepts genuinely any decodable image format (including HEIC/TIFF/GIF/BMP) and always normalizes to webp on save - every image `accept="..."` attribute site-wide widened to `image/*` to match. The listing form itself converted to a client component calling the (now result-returning, non-redirecting-on-error) `createStoreAction` directly, so a failure no longer navigates away. Also found and closed a real gap while wiring this up: `createStore()` had **no moderation at all** (every other free-text creation path already goes through `checkModerationOrThrow()`) - fixed, and extended `moderation.ts` with a new `isOffTopicFromBooks()` local-embedding check (same no-external-API approach as the existing abuse check) wired into standalone feed posts and Askıda Kitap listings, per the maintainer's "paylaşımlar hep kitap ile ilgili olmalı" ask - deliberately NOT applied to ordinary book/writer/translator comments (already anchored to a real entity; a short reply would false-positive against any topic classifier). Real image-content relevance filtering (checking an uploaded *photo* is book-related, not just its caption text) would need a separate vision/CLIP-style model - flagged as a genuine gap, not built this pass, rather than silently claimed done. **Windows-local dev note:** sharp's Windows binary fails to `dlopen` when loaded through Turbopack's runtime on this machine (reproduced in both `next dev` and a real `next start`; a `createRequire()` workaround made no difference - Turbopack intercepts that too) - confirmed this is Windows/Turbopack-specific, not a real bug: the same image built for the actual Linux VPS loads and converts images correctly (`docker run dklist-v2 node -e "require('sharp')..."` verified directly), and the full flow (reject-off-topic, accept-on-topic-with-image, webp filename in DB) was verified end-to-end against the live production site before shipping. If local Windows dev on image-upload routes needs to work again, that's a separate Turbopack/native-addon problem to pick up later.

**Admin badge/frame assignment wired up + Yazarhane self-service writer application (2026-08-31).** Two more "her türlü şeyin ataması yapılabilsin" gaps closed. (1) `updateUserBadges()` already existed in the query layer but had zero action/UI - a code comment even documented this as intentionally matching v1's "admin-defined-but-manually-assigned-outside-the-app" badge behavior. Built the actual UI: a "Rozet & Çerçeve" toggle on each `/admin/kullanicilar` row opens a lazily-loaded panel where an Admin/Kurucu can check/uncheck badges and force-set any profile frame regardless of whether the user redeemed it (a manual grant, deliberately not faking a `point_redemption` row). (2) The `Yazar` role already existed for Yazarhane authorship, but the only path to it was an Admin manually promoting someone with no request behind it - added a real self-service application (`/yazarhane` shows a form to any non-author member, landing in a new `writer_application` queue; `/admin/yazar-basvurulari` approves/rejects, approval sets `userType=Yazar` and optionally links the proposed catalog `writer` record). No new role type was needed - `Yazar` already meant "yazacak yazarlar". Both features share the same submit-queue-review-notify shape as the verified-reader flow built earlier this session. Verified via real build + live deploy; the writer-application form's own end-to-end submission (as a genuine non-author test account) wasn't exercised live - registration in the verification script got blocked by the custom Select component's hidden native `<select required>` for "Cinsiyet", a Playwright-script limitation, not a sign of an app bug - flagged here rather than silently claimed as fully tested.

**Customer-relayed live bugs closed (2026-09-01): avatar upload, dead homepage CTAs, unlabeled date fields, verification-email resilience.** (1) "Profil resmi ekleyemedim, telden bakayım dedim" - `avatar.ts` turned out to have its OWN duplicated fixed-extension/magic-byte validation, entirely separate from `saveUploadedImage()` - the site-wide any-format-to-webp fix earlier this session never actually reached avatar uploads. Routed through the shared upload path; a phone's HEIC camera photo now works. (2) Homepage's "Keşfetmeye Başla"/"Nasıl Çalışır?" hero buttons were plain `<Button>`s with no destination - given real ones (`/ara`, and a genuine new 3-step "Nasıl Çalışır?" section, since nothing on-site previously explained the site at all). (3) Birth-date inputs (register + profile edit) had no visible label, reported as looking "tanımsız" - added one. (4) Investigated "onay kodu gelmedi" (verification code never arrived) by sending a real SMTP test directly from the production container - Brevo genuinely accepts and queues the mail, and a live DNS check confirmed SPF/DKIM/DMARC are all correctly configured on the sending domain, so this is not a broken mail pipeline. Hardened it anyway: the email send was previously `await`ed serially in the same request as the DB insert + bcrypt hash + immediate credentials sign-in - real risk of the exact "internette sorun oldu" (network error) the customer hit on a slow connection - made it fire-and-forget (same pattern as the book-embedding compute), and did the same for both password-reset email paths. Added a rate-limited "Kodu Tekrar Gönder" resend action + button to `/dogrula`, since previously a genuinely-lost code left an account permanently stuck.

**Category pagination + Turkish-first sort + unbounded admin pagination bug + rating display order (2026-09-01) - large customer feedback batch.** (1) Category pages hardcoded a 40-book cap with no pagination at all - rebuilt `getBooksByCategory()` with real paging. (2) Restored a v1 behavior ("Kategorilerde Türkçe en ilk sırada listelenmişti") - a naive single `ORDER BY` reintroduced a catastrophic filesort on categories with 2M+ books (confirmed via EXPLAIN and a 2m20s live timing test), fixed by running two separate FORCE-INDEX queries (Turkish-only, then the rest) spliced across pages using a `days`-cached Turkish count. (3) Bigger bug found while investigating: the admin Yazarlar/Çevirmenler/Yayınevleri/Kategoriler/Kitaplar/Kullanıcılar lists rendered `Array.from({ length: lastPage })` unbounded - fine for small tables, but writer/translator have millions of rows so `lastPage` is in the hundreds of thousands there, freezing the page - this was the real cause of "sayfa sayılarının üzerine tıklanmıyor". New shared `pageWindow()`/`<PaginationNav>` (with a real "İlk Sayfa" jump link, another explicit ask) applied to every paginated list, admin and public. (4) Site-popup image was broken - its `image` column held a legacy full Cloudinary URL, not a bare filename; fixed with the same URL-detection pattern as `blogImageUrl()`. (5) Book rating display reordered per the customer's explicit reasoning: the pooled "ortak kitap puanı" (across every edition) is now the dominant/bold number, "bu baskının puanı" the secondary one; every rating display standardized to one decimal place (book/writer/translator pages had a stray `.toFixed(2)`). Deliberately NOT attempted this pass (documented, not silently dropped): vote-count/comment-count next to ratings, collapsed-then-"tümünü göster" reading-status lists on profiles, real avatar photos in the leaderboard/reader widgets, the writer-page book-list edition-grouping redesign, feed-image aspect-ratio normalization, showing the pooled score directly on feed book covers, and the Akış tab-content investigation - all real, reasonable asks, just larger design/verification work than fit in this pass.

**Continued the same feedback batch (2026-09-01): live header search, rating vote counts, profile shelf collapse, real avatars everywhere they were missing.** (1) Header search only ever submitted to `/ara` on Enter - new debounced live dropdown (reusing the existing cached `searchBooks()`) with a real cover thumbnail per result, verified live returning genuine matches ("Suç ve Ceza" etc. with real `/kapak/[id]` covers). (2) `ratingCount` was already being fetched on the book page but only ever fed into the invisible JSON-LD SEO block - now shown next to the score; added the equivalent `getWriterRatingCount()`/`getTranslatorRatingCount()` (didn't exist before) for those pages too. (3) New generic `<ExpandableShelf>` (collapses to 12 items, real "Tümünü Göster (N)" toggle) applied to every shelf-shaped profile section (reading-status groups, kitaplığım, beğenilen yazarlar/çevirmenler/yayınevleri) - previously rendered everything at once with no cap. (4) Real avatar photos were missing from `getFollowSuggestions()`, `getTopReaders()`, and `getBookReaders()` at the *query* level (never selected `user.image` at all, not just a component oversight) - fixed the queries and switched the homepage's three initials-only avatars over to the same `EntityAvatar` component already used elsewhere. Also verified (not a bug): the Akış tabs' `scope`/`mode` filtering is correctly wired end to end - the customer's "hangisine tıklasam aynı veriler geliyor" perception is real but is current low data-volume on a still-young site, not broken filtering. Comment/reply/blog-author/message avatars were found to have the same missing-image gap but were NOT fixed this pass (a genuinely bigger, separate sweep) - flagged, not silently left implied-done.

**Closed out the rest of that feedback batch (2026-09-01): the real avatar root cause, writer edition grouping, feed image cropping, feed book scores.** The "comment/reply/blog-author/message avatars" gap flagged as deferred above turned out to have a single, much bigger root cause: `EntityAvatar` (used everywhere site-wide) passed its `image` prop straight to `<AvatarImage src=...>` with zero URL resolution - a bare uploaded filename isn't a valid `<img>` src on its own, it needs the `/api/avatar/[filename]` proxy prefix. Every call site, including ones that predate this session (the main feed's own actor avatar), was silently falling back to initials this whole time. Fixed once at the source (new client-safe `avatarUrl()` in `image-urls.ts`, `EntityAvatar` now resolves its own prop) rather than requiring every future call site to remember to wrap it - verified live on `/akis`: real profile photos now render for `@admin`/`@gokhan` in feed avatars and the weekly-leaders widget. Also this pass: (1) new `<WriterBookGrid>` groups a writer's books by `originalBookId` (Sabahattin Ali's 610-row list collapses to real works, each with a "+N baskı" toggle for the rest) - the header book-count stat was fixed to match; genuinely un-linked duplicate catalog rows (no `original_book_id` set on either side) still show as separate cards, a known, documented, pre-existing data-quality gap (Phase 5's deferred dedup backfill), not something this grouping logic can fix on its own. (2) Feed post images were getting silently cropped on tall/portrait photos (object-cover + only a max-height, no fixed ratio) - fixed with a real fixed aspect ratio + object-contain, letterboxed, never cropped. (3) Feed book attachments now show the book's score in gold next to its name, per the customer's explicit ask to reinforce the ratings system visually in the feed.

**PRODUCTION INCIDENT (2026-09-01): real 502 - the v2 Node process crashed with a JavaScript heap OOM.** Customer hit a live Cloudflare "Bad gateway (Host: Error)" screen. `docker logs` showed a genuine V8 `FATAL ERROR: Ineffective mark-compacts near heap limit... JavaScript heap out of memory` right before Docker's restart policy silently recycled the container - the 502 was that crash-and-restart window. Confirmed Next.js 16's default `'use cache'` data-cache handler is itself bounded (`cacheMaxMemorySize` defaults to only 50MB), so this session's extensive caching work almost certainly wasn't the direct cause on its own - more likely this specific host's heavy synthetic load today (many rapid rebuilds/redeploys, repeated live Playwright test sessions, and cache-warming scripts all hitting production in the same stretch) pushed a genuinely memory-hungry app (mysql2 connections, the in-process embeddings/ONNX model for moderation, normal Next.js server-render memory) past V8's default heap ceiling for the first time (confirmed via `docker inspect`: `RestartCount: 1`, i.e. never happened before). Host has real headroom (11GB total RAM, other containers combined use <100MB) that wasn't being used at all. Fixed with two real safety nets rather than just restarting and hoping: (1) `NODE_OPTIONS=--max-old-space-size=4096` on the `v2` service in `docker-compose.yml`, giving V8 2x the heap ceiling it crashed at; (2) a `mem_limit: 5g` Docker-level cap, so a genuine future runaway kills and cleanly auto-restarts only this one container instead of risking host-wide memory pressure. Also installed a cron job (`*/10 * * * * warm-cache.sh`) on the VPS itself - previously cache-warming only ever happened when *I* remembered to run it right after a deploy, so any *unplanned* restart (a crash, a host reboot) left the site cold for whichever real visitor hit it first; now it self-heals within 10 minutes automatically. Verified live: site back to a real `200` in under a second post-fix, `sh warm-cache.sh` clean across every hot page.

**"Güzel bir preloader" (2026-09-01).** No route-level loading UI existed anywhere - a navigation to a page with real server work to do (fresh Suspense boundary, an uncached fetch) showed nothing until content was ready, reading as frozen rather than loading, compounding how a genuinely slow cold page already feels. New `src/app/loading.tsx` - the actual Next.js App Router mechanism for this (shown instantly on any navigation, swapped out once that route's own content resolves), styled to match the site's own editorial design language (a small animated book-jacket motif + the Fraunces serif wordmark) instead of a generic spinner.

**Urgent live-health check + "resumable slots" root cause fixed (2026-09-02, commit `a5070d9`, pushed and deployed).** Customer reported pages feeling frozen/not navigating. Live investigation: no deploy/build was running, container had been up 20h with no crash, and `docker logs` showed three real but low-frequency error types over that window - "Couldn't find all resumable slots by key/index during replaying" (10x), a `RangeError: Maximum call stack size exceeded` inside `buildQueryFromSourceParams` (2x - traced to `node_modules/drizzle-orm/sql/sql.js`, i.e. Drizzle's own SQL-builder recursing on something, most likely one call site passing an unusually large array into `inArray()`; not yet isolated to a specific query, worth revisiting if it recurs), and a stale "Server Reference ID did not match" (1x, consistent with a browser tab left open since before an earlier deploy - harmless, fixed by a hard refresh). Zero recurrences of any of the three in the prior 30 minutes, and every page manually checked returned fast `200`s - so nothing was actively broken *at that moment*, but the resumable-slots error had a real, fixable root cause: `AdSlot` reads `auth()` internally and its own doc comment requires callers to isolate it in its own `<Suspense>` boundary (same pattern as `AuthStatus`) so the prerendered static shell doesn't need viewer-dependent content - but `community-right-rail.tsx` and `blog/[slug]/page.tsx`'s `BlogSidebar` both rendered it as a bare sibling inside an already-dynamic boundary alongside other conditionally-rendered lists, which is exactly the shape that produces a prerender/resume tree mismatch. Fixed both call sites, verified via `tsc`/lint/85 tests/clean build, deployed to the VPS.

**Real finding from this deploy, likely the actual explanation for the earlier 502 incident**: after `docker compose up -d v2` recreates the container, Next.js 16's in-process `'use cache'` store is wiped, so every cached query goes cold at once. Running `warm-cache.sh` right after showed most pages reach sub-second immediately, but the **homepage cold-loaded in 195.9 seconds** - far beyond any previously-documented cold-query cost (the worst single query on record was ~2m20s for one category aggregate) - almost certainly because the homepage fires ~8 independent uncached queries (top books, top readers, weekly leaderboard, follow suggestions, recommendations, trending, book-of-month, recent activity) that all hit the same single HDD-backed MySQL instance at once, and disk-seek contention compounds rather than parallelizes. Cloudflare's origin timeout is ~100s - a cold homepage request in that window would 502 exactly like the screenshot from the original incident, independent of the OOM crash already fixed. **Mitigation already in place, now confirmed load-bearing, not just a nicety**: the `*/10 * * * * warm-cache.sh` cron means any *unplanned* restart self-heals within 10 minutes, and every *manual* deploy must run `warm-cache.sh` immediately after `docker compose up -d v2`, before considering the deploy done - this was already the documented pipeline step, but this incident is the first time its necessity was this visible. Not yet built: gating so a fresh container can't receive real traffic until warm - would need a blue-green or health-check-gated rollout, real infra scope, not attempted without the maintainer's go-ahead. Post-warm, homepage confirmed back to 0.1s; full live re-verification (`/`, `/kitaplar`, `/akis`, `/yazarlar`, `/bloglar`) all fast `200`s; `docker logs` clean of all three error types since redeploy (only the expected one stale-client Server Action error, from a pre-existing old tab).

**MAJOR PRODUCTION INCIDENT (2026-09-02): site-wide hang, root-caused via `SHOW FULL PROCESSLIST` to three related MySQL query-plan bugs, all the same underlying disease, found and fixed one at a time over several hours (commits `754ce60`, `8db11dd`, `1ff107b`).** Customer reported pages "dönüp duruyor" (spinning/not loading) shortly after the previous entry's fix was deployed. Live `curl` confirmed `/`, `/kitaplar`, `/akis` genuinely hanging (20s+ timeouts) with no deploy in progress - `SHOW FULL PROCESSLIST` on the VPS's MySQL showed 6+ queries stuck concurrently for 500-2100+ seconds each (one admin query stuck 39 minutes), all saturating the single HDD-backed instance.

*Bug 1 - `getSimilarBooks()` (book-detail.ts), large categories:* joined `book_category`→`book` and `ORDER BY book.score DESC` with no `STRAIGHT_JOIN`/`FORCE INDEX`. For a large category (confirmed on prod: category 31 has ~4.47M rows in `book_category`) `EXPLAIN` showed MySQL materializing the whole join into a temp table and filesorting it (`Using temporary; Using filesort`) instead of using `idx_book_score`. Made far worse by a caching bug: `bookId` (the book being *excluded* from results) was a parameter of the `'use cache'`-wrapped function even though the underlying candidate pool only depends on `categoryId` - every distinct book page produced a fresh cache miss, so this catastrophic query fired concurrently for every book viewed in a popular category. Fixed by splitting out `getCategoryCandidatePool(categoryId, poolSize)`, cached by `categoryId` alone, moving the "exclude current book" filter into JS after the cached fetch.

*Bug 2 - `getBooksByCategory()` (books.ts), small categories:* the *existing*, previously-verified-safe `STRAIGHT_JOIN b FORCE INDEX(idx_book_viewcount) WHERE EXISTS(...) ORDER BY view_count DESC LIMIT N` pattern (from the 2026-08-20 category-pagination work) turned out to only be safe for *large* categories. MySQL's plan walks `book` in view_count-descending order and stops once it collects `N` matches - for a small category (confirmed on prod: category 95900 has 15 books total) there aren't enough matches to ever satisfy the `LIMIT`, so MySQL scans nearly the entire ~98.5M-row `book` table before concluding there's nothing left (one real request stuck 27+ minutes). `EXPLAIN`'s `rows` estimate for this plan shape is *always* ≈`LIMIT` regardless of true selectivity - it gave zero warning of the problem. Fixed by checking the already-available subset count (`trCount`/`total-trCount`) against a 20k threshold and picking the join order accordingly: below it, start from `book_category` (index range scan on `category_id`) and filesort the small result instead - confirmed via `EXPLAIN`+timing that a filesort over a few thousand rows is a non-issue, only catastrophic at millions.

*Bug 3 - the SAME small-category disease, missed in Bug 1's own fix:* Bug 1's replacement `getCategoryCandidatePool()` was verified only against category 31 (large) before shipping - it has the identical "book-first FORCE INDEX plan degrades to full scan when the category is small" problem as Bug 2, just discovered *after* Bug 1 deployed, when several homepage recent-activity categories (16-290 books each) got stuck 6-12+ minutes in production. Fixed with the exact same size-aware join-order pattern as Bug 2.

**The real lesson, worth remembering for every future query of this shape**: a book-first `FORCE INDEX(...) WHERE EXISTS(...) ORDER BY ... LIMIT N` plan is fast *only* when the filtering condition matches a large-enough fraction of the table that the index scan finds `N` matches quickly - it silently degrades to a near-full-table-scan when the condition is rare, and `EXPLAIN`'s cost estimate does not warn about this (it reports `rows≈LIMIT` either way). **Every future query using this pattern must be tested via `EXPLAIN`+live timing against both a large real category AND a small/rare one before shipping** - testing only category 31 (a top-10 category) is exactly what let bugs 1 and 3 both ship with the same latent defect.

**Emergency mitigation while each fix was being prepared**: repeatedly used `KILL QUERY <id>` directly against MySQL (all read-only `SELECT`s, always safe) to relieve the live site between deploys, rather than leaving real visitors stuck behind a queue that could take hours to drain on its own.

**Final verification**: after the third deploy + `warm-cache.sh`, `SHOW FULL PROCESSLIST` showed all connections idle (`Sleep`) with nothing stuck; directly re-tested the 5 actual previously-stuck small categories live (`/kategori/christian-coalition-64819` etc.) - all returned in under 1.1s; `docker logs dklist-v2` clean of all errors since the final deploy.

**Deferred, unrelated, lower-priority**: an admin book-list query in `book-admin.ts` (`ORDER BY approve, id DESC LIMIT 20`, no `WHERE` clause, no index on `approve`) was observed stuck 7-39 minutes several times during this incident, contributing to the general disk contention but admin-only/not customer-facing. Not fixed this pass - needs its own investigation (likely either an index on `approve` or a rewrite avoiding the full sort over ~98.5M rows).

**Large customer feedback batch fixed (2026-09-02, commits `a0f0047`/`7d1ac44`, deployed and verified).** ~15 real bugs/gaps investigated against actual code (not assumed) and fixed: pooled rating averaged editions' own averages instead of raw votes (7-baskı-1-oy case showed ~1/10 instead of ~8/10, renamed "Ortak kitap puanı"→"Kitap puanı"); weekly leaderboard summed `point_store_redeem` (spending) into the activity total, producing real negative weekly scores; `AddToListButton` only ever offered "create a list" with zero lists, never after; `MyListRow` had no edit UI despite `updateReadingListAction` already existing; `getUserClubs()` existed with zero UI callers, so a private/unlisted club had no way back once its link was lost (added a "Kulüplerim" section to `/kulupler`); no page ever set `og:image` (book pages included) - Facebook's crawler partially covers for this, WhatsApp's doesn't, matching the exact reported split, fixed with real book covers + a default brand-icon fallback everywhere including the bare domain's own preview; comment/quote shares on book/writer/translator/club pages only ever shared the entity page's generic URL with zero trace of the actual quoted text in Facebook/WhatsApp previews (added an `alinti` query-param mechanism read by each page's `generateMetadata`); reading-goal percentage was hard-clamped at 100 so exceeding a goal looked "stuck" forever (now shows real % + a "Hedef aşıldı!" note + an actual progress bar); `getSimilarBooks()` never filtered by language at all (now tries same-language first, falls back if too few candidates) and book pages never showed which language a book is in at all (new `src/lib/languages.ts`); "Askıya Bırak" from a book page required re-searching the same book from scratch (added a `?bookId=` pre-fill); notifications/messages had no "tümünü sil" (v1 had both); profile shelves (okudum/kütüphanem/blog) had no search within a long list; `/ilanlarim` had no cover photos at all unlike the near-identical `/favorilerim` (same first-picture lookup added, plus status grouping); club's current-book widget was text-only, added a real cover.

**Investigated, not clearly bugs (documented, not guessed at)**: writer-image-upload error is most likely the Admin-only permission gate working as intended (asked which account hit it); feed-post image getting stuck at "Paylaşılıyor..." has no obvious code-level cause (`createFeedPostAction` has a proper try/catch, returns a real error result rather than hanging) - could be a slow local ONNX moderation pass or a network hiccup, needs a live repro to pin down; the club's internal "share with a note to your own profile" comment-reshare action (`shareCommentAction`→`shareEntityComment`) looks correctly wired end-to-end in code (`CommentTargetType` "bookClub" used consistently for write and read) - could not reproduce the reported no-op without a live session.

**Customer separately confirmed a "slowdown over time" perception (2026-09-02), distinct from the query-plan incidents already fixed.** Code review of the two most likely leak sources found nothing conclusive: the SSE/event-bus (`event-bus.ts`/`/api/events`) correctly unsubscribes and clears its heartbeat interval in the stream's `cancel()` callback, and `RealtimeRefresher` correctly closes its `EventSource` on unmount; the mysql2 pool is bounded (`connectionLimit: 10`) and stored on `globalThis` to avoid the Next.js multi-chunk duplicate-singleton trap already documented for the event bus. Rather than guess further, installed **actual measurement**: `/root/log-memory.sh` appends a timestamped `docker stats` sample for `dklist-v2` to `/root/v2-memory.log` every 10 minutes via cron (verified working) - enough real data in a few days to confirm or rule out a genuine multi-hour memory/GC-pressure creep, instead of treating today's 48-minutes-post-redeploy snapshot as evidence either way. Also installed a **safety net**, not a fix: `/root/nightly-restart.sh` restarts the `v2` container at 03:30 server time (a real low-traffic window) and immediately re-warms the cache, cron'd for daily 03:30. This bounds any *unconfirmed* slow degradation to at most 24h before a clean reset, at the cost of one brief (~10-30s) cold homepage overnight - remove this once `v2-memory.log` shows real multi-day stability without it.

**Third batch (2026-09-02): the six "needs a decision" items resolved with judgment calls rather than left waiting, per explicit maintainer instruction ("kanka yap işte ya").** Only two were genuinely un-doable without something only the maintainer can provide (Google AdSense needs a real registered account/site-verification; the AI photo-library-scan feature needs either a paid vision API or GPU-hosted model, which conflicts with the standing no-paid-services rule) - left as real external blockers, not decisions punted out of laziness. The other four:
- **"Akış" promoted to top-level header nav** (was buried inside the "Topluluk" dropdown, three clicks deep for a feature meant to feel alive/immediate) - kept "Keşfet" first since an anonymous first-time visitor still needs a discovery entry point before a social feed means anything, removed the now-duplicate entry from the dropdown.
- **Category/similar-books sort**: confirmed `getBooksByCategory` (books.ts) already sorts by `view_count` (popularity), matching the recommendation given - no code change needed there. Left `getSimilarBooks`'s score-based sort as-is deliberately - a *recommendation* engine should surface well-rated books, not just already-popular ones, which is the opposite reasoning from a *browse* listing.
- **Duplicate-merge extended to writer/translator/publisher** (`db/queries/merge.ts`, `/admin/merge` now has a type selector) - writer/translator are many-to-many (`writer_book`/`translator_book` + generic `score`/`comment`/`user_writer`-style tables), so a plain UPDATE could hit a real PK/unique conflict when a book (or rater, or liker) is already linked to both the duplicate and canonical record; used `UPDATE IGNORE` + a cleanup `DELETE` for exactly those rows rather than crashing the whole merge. Publisher is a plain 1:many FK (`book.publisherId`) - simpler, no join-table conflicts possible - and deliberately does NOT reuse `deletePublisher()`'s cascade (that ports v1's real behavior of *deleting* every book, correct for "remove this bad record", wrong for "these are the same publisher, keep the books").
- **"Satın al" (buy button / affiliate) plumbing built**, not a real integration (no partner terms exist yet - which retailers, revenue split, is a real business decision only the maintainer can make). New `book_purchase_link` table (migration `0034`, supports multiple retailers per book), admin-managed via a new section on `/admin/kitaplar/[id]`, shown as `rel="sponsored nofollow"` buttons on the book page only once a real link exists for that book. Zero effect until an admin actually adds a link - ships inert today, ready the moment a real deal exists.

Also added, smaller: a "mesajlar" ad placement (customer's minor suggestion), correctly isolated in its own `<Suspense>` boundary from the start (the exact mistake that caused the earlier "resumable slots" incident).

**Real Google AdSense integration built (2026-09-02, commit `ece90d3`, migration `0035`, deployed and verified).** Customer clarified the earlier AdSense answer - they didn't want a real Google account opened on their behalf, they wanted the actual ad-slot *infrastructure* built and ready, for either AdSense or their own direct ad sales. The direct/personal ad system (`advertisement` table) already covered the latter; this adds the former alongside it, not replacing it. `adsense_setting` (single-row: publisher id + a master enabled switch, defaults `enabled=0`/`publisherId=NULL`) and `adsense_placement` (per-placement AdSense ad-unit slot id) are both admin-managed from a new "Google AdSense" section on `/admin/reklamlar`. `AdSlot` checks AdSense config first and only renders Google's real `<ins class="adsbygoogle">` unit + loads Google's script when a placement has BOTH the master switch on AND its own slot id configured - otherwise it falls through to the existing personal-ad lookup exactly as before, so no existing placement's behavior changes until an admin deliberately opts a specific spot into AdSense. Verified post-deploy that the live homepage HTML has zero occurrences of "adsbygoogle" - confirmed genuinely inert, not just configured-off in a way that still leaks script tags to real visitors.

**Explained (not built) - the customer's own "görüntülü kitap tarama" (photo library-scan) idea**, since they asked for it to be described again: photograph a full bookshelf, have AI read every book spine in the photo, match each against the real catalog, and bulk-add them to the reader's library in one action instead of searching/adding one at a time. Confirmed this is real, useful, and NOT small - genuinely requires either a paid vision-AI API (per-image cost) or a GPU-hosted model this VPS almost certainly can't run, directly gated on the maintainer's own standing "no paid services, ever" rule (Phase 7's AI-features gate, documented earlier in this file) - not something to unilaterally spend the maintainer's money on. Waiting on an explicit decision + budget before any of this gets built.

**All 7 ad placements filled with real house ads (2026-09-02, direct production data - no code change, `advertisement` table only).** Customer's explicit ask: no placement should sit empty, and they should look genuinely designed, not like filler. Since there are no real advertisers yet, built self-promotional ads for real, already-working DKList features instead of leaving anything blank: homepage-banner→Premium, book-page→Puan Mağazası, kitaplar-listesi→Kulüpler, yazarlar-listesi→Yazarhane, akis→Askıda Kitap, akis-sidebar→Rozetler, mesajlar→Kulüp Kur (closing the placement added earlier this session). Each is a real designed banner (Playwright-rendered HTML/CSS using the site's own oklch design tokens and Fraunces serif, not a stock template) - generated at `1200x300` for the wide placements and `600x750` for the narrower `akis-sidebar` column, converted to webp via `sharp`, uploaded into the `v2_uploads_data` volume via `docker cp`, and inserted as real `active=1` rows with real internal `linkUrl`s. Verified live post-insert: curled all page types and confirmed each placement's exact expected image filename appears in the real HTML response (`/`, `/kitap/[slug]`, `/kitaplar`, `/yazarlar`, `/akis` - the last one showing both `akis` and `akis-sidebar` together, correctly). `/mesajlar` requires a signed-in session to render, so couldn't be curl-verified anonymously, but runs the identical `AdSlot` code path already confirmed working elsewhere.

**House ads redesigned to read as real ad units (2026-09-02, same-day follow-up, direct production data).** Customer's direct feedback on the first pass: they looked like elegant page hero sections, not advertisements. Redesigned with the actual visual grammar of a real ad: a "Reklam" disclosure tag in the corner (standard ad-network practice, e.g. Google's own "Ads" label), a bold icon badge per ad (real lucide icon paths extracted straight from `node_modules/lucide-react` - Sparkles/Gift/Users/PenTool/ShoppingBag/Award/MessagesSquare, matching the concept each ad promotes), a punchier high-saturation CTA button with a drop shadow instead of a pale blended pill, and a subtle outer border so the image reads as a distinct inserted card rather than page content. Same 7 placements, same destinations, same generation pipeline (Playwright HTML/CSS render → sharp webp conversion → docker cp into the uploads volume) - only the visual design changed. Old image files deleted after the DB rows were repointed, verified live via the same curl+grep check as before.

**Mobile ad text was near-illegible (2026-09-02, commit `68c7f5b`, migration `0036`, deployed and verified).** Customer report: "her şey aşırı küçük" on mobile for the ad sections. Real root cause: house-ad banners are flat images with text baked in, and `AdSlot` renders them `w-full` - the whole image scales down proportionally with its container, so a wide 4:1-proportioned desktop banner shrinks its baked-in 42px title to roughly 12px effective size once displayed in a ~340px-wide phone column. Real ad creative is normally produced in distinct desktop/mobile sizes for exactly this reason (IAB standard banner sizes include separate mobile formats). Added `advertisement.mobile_image` (nullable, fully additive - no existing ad's behavior changes without one); `AdSlot` renders a real `<picture>` element with a `media="(max-width: 640px)"` source swapping to it, falling back to the desktop image at every size otherwise. Generated proper mobile creatives for the 6 wide placements at a squarer `640×520` "medium rectangle"-adjacent format with much larger relative type (not just a smaller crop of the wide banner) - `akis-sidebar` deliberately skipped, confirmed via source read that its `<aside>` wrapper is `hidden` below the `xl` breakpoint, i.e. never shown on mobile at all. Admin UI (`create-ad-form`, `ad-admin-row`) can now upload/replace a mobile creative on any ad. Verified live: `<picture>` with the correct mobile source and desktop fallback confirmed present in the real homepage HTML.

**MAJOR INCIDENT, three passes to actually fix (2026-09-02): category+language selectivity for `getSimilarBooks`.** Discovered live while verifying the mobile-ad deploy - `SHOW FULL PROCESSLIST` showed several requests stuck 7-12+ minutes, all the same shape (`category_id = X AND b.lang = 'uk'/'it'`). Root cause: the language-aware similar-books fix shipped earlier the same day (`getCategoryCandidatePool`) decided its book-first-vs-bookCategory-first query plan using the category's TOTAL size regardless of the requested language - a large category with almost no books in a specific language (confirmed on prod: category 863 has only 2 Ukrainian books total) still got the "large category" book-first plan, degrading to the same near-full-table-scan already fixed for the language-agnostic case.

*First fix (commit `eca2ab4`)*: added `getCategoryLangCount(categoryId, lang)`, one `COUNT(*)` join per (category, language) pair, generalizing the existing `getCategoryTurkishCount()` pattern. Verified against the actual stuck case (category 863/uk): 0.171s vs 12+ minutes. Looked solid in isolation - but **this fix itself became a recurring contention source within minutes of deploying**, discovered live: category 31 (~2.2M rows) turned out to span 50+ distinct languages, and organic traffic touching several different languages close together each triggered a *fresh* multi-minute cold join for that specific language - several piled up concurrently fighting over the same disk, repeatedly, not just once as assumed. Had to `KILL QUERY` two separate pileup batches during triage.

*Second fix (commit `2865e3b`)*: replaced the per-language count with `getCategoryLangBreakdown(categoryId)` - a single `GROUP BY b.lang` query computing every language's count for a category in one pass, cached once per category regardless of how many distinct languages get requested against it. This is the actual fix - the expensive join now runs at most once ever per category. Verified live: category 863's full 51-language breakdown resolved in one 14.7s query; post-deploy monitoring over several minutes confirmed each category (31, 13, 2436, 689, etc.) triggers its breakdown query exactly once, never repeating for a category already computed.

*Known, accepted, NOT separately fixed*: the actual similar-books *data-fetch* query (not the count) for a genuinely rare-language-within-huge-category combination can still be expensive as a one-time cost (observed: category 31 + Hebrew took 125s) - `book_category` carries no `lang` column, so neither query strategy (book-first or bookCategory-first) can filter language before joining to `book`, and for a huge category this means touching a large fraction of its rows regardless of which side initiates the scan. Because `getCategoryCandidatePool` is `'use cache'`-keyed on `(categoryId, poolSize, lang)`, each distinct combination only ever pays this cost once (cached for hours) - confirmed via monitoring that already-computed combinations don't recur, only genuinely new ones incur cost. This is a real, inherent limitation of the current schema for the true long tail of rare category+language pairs, not a bug in the sense of an infinite loop or wrong plan choice - a future fix would need a covering/denormalized `(category_id, lang)` lookup, not attempted now given the cost is bounded and self-resolving rather than a repeating incident.

**Honest summary**: this took three iterations to actually understand and fix, not one clean pass - each fix was verified working before its own follow-on problem surfaced live. The recurring-pileup/contention bug (the actual site-wide-feeling-slow complaint) is now genuinely fixed. Individual rare category+language combinations may still take up to ~1-2 minutes the very first time they're ever requested, but this is bounded, cached afterward, and does not repeat or compound - a fundamentally different (and far less severe) class of behavior than the incident being fixed.

**House ads replaced with real animated HTML/CSS (2026-09-02, commit `6a519d9`, deployed and verified, no migration).** Customer's direct feedback: the flat-image house ads should be "hareketli" (moving/alive) real HTML, not static pictures. New `HouseAd` component - genuine DOM per placement (a real lucide icon in a badge, kicker/title/sub text, a CTA), with actual motion: a slowly drifting gradient background (`background-position` animated via `@keyframes`), a periodic diagonal light sweep across the card, a floating decorative ring, and a gently bobbing icon badge - all real CSS, `prefers-reduced-motion` respected. Incidentally this fully closes the earlier mobile-ad-text-scaling problem *for house ads specifically* at its root, not just mitigates it: real DOM text reflows and sizes normally at every viewport on its own, there is no image to scale down at all. (The `<picture>`/mobile-image mechanism from the earlier fix stays in place for real admin-uploaded advertiser images, which are still flat pictures by nature and still need it.)

`AdSlot`'s priority order is now: real AdSense (if admin-configured) → a real admin-uploaded advertiser image (the original direct/personal-ad system, unchanged) → `HouseAd` as the permanent floor when neither exists - so a real paid ad always outranks the house ad the moment one exists, and no placement is ever genuinely empty regardless of admin configuration. Real bug caught and fixed *before* shipping, not after: `getActiveAd()` returns `null` both for "premium viewer, ad-free" and "no ad configured for this placement" - harmless while "no ad" always meant an empty slot, but now that "no ad" falls back to a visible house ad, a premium user (whose one guaranteed benefit is ad-free) would have incorrectly seen it too. Fixed by checking premium status once in `AdSlot` itself, gating every ad type (AdSense, real ad, house ad) the same way, before any of the fallback logic runs.

The 7 placeholder flat-image ads created earlier the same day (and their webp files, desktop + mobile variants) were deleted from production so the new code-based component actually takes over, rather than the old rows continuing to win the lookup. Verified live: homepage HTML contains the `house-ad-drift` keyframe name and the real "Premium'u Keşfet" copy; `/kitaplar` shows "Kulüplere Katıl"; `/akis` shows both "Askıya Bak" (main feed placement) and "Rozetleri Gör" (sidebar placement) together, correctly; `docker logs` clean of any hydration or styling errors from the new component.

**Sticky skyscraper ads for the empty left/right viewport gutters (2026-09-02, commit `a794b57`, deployed and verified with a real screenshot).** Customer's ask, with a real screenshot attached showing it directly: on a wide monitor the header spans up to `max-w-[100rem]` (1600px) but nearly every page's own content maxes out well before that (`max-w-6xl`/`4xl`/`3xl`), leaving large, genuinely empty gutters on both sides site-wide, not just on one page - "sitenin neredeyse her yerinde sağ sol paneller boş duruyor". Explicit ask for sticky placement too.

New `SkyscraperAds` component wired once in the root layout (inside `PublicChrome`, so hidden on `/admin` like the other site-wide chrome) instead of per-page - two `position: fixed` `AdSlot`s anchored to the left/right viewport edges. Used `fixed`, not `sticky`: a `sticky` element still scrolls away once a short page's own content ends, which would defeat the ask on any page shorter than the ad's height - `fixed` stays visible for the whole session regardless of page length, matching "sticky de olabilir" in spirit more literally than the CSS property of the same name would. Shown only at the `2xl` breakpoint (1536px+) via `hidden 2xl:block`, since that's the point where there's reliably enough real gutter width left for a legible ad without colliding with actual page content - deliberately does not attempt to appear on ordinary laptop/tablet/phone viewports. Each side is its own `<Suspense>` boundary around its own `<AdSlot>` - repeating the shared-boundary mistake that caused the earlier "resumable slots" incident this same session would have been a real, avoidable regression.

Two new placements (`skyscraper-left`→Premium, `skyscraper-right`→Kulüpler) plus a new narrow/tall rendering branch in `HouseAd` (a ~160px-wide column: REKLAM tag, icon badge, kicker, 2-line title, short subtext, compact CTA - shorter copy than the horizontal placements need, tuned for the tight width), reusing the same animated gradient/sweep/float treatment already shipped. Verified two ways: `curl` confirmed both placements' real copy ("Reklamsız Oku", "Birlikte Oku") and the `2xl:block` class present in the server-rendered HTML (the hiding is pure CSS, so the markup itself is always present regardless of viewport), and a real Playwright screenshot at 1920×1080 visually confirmed both ads rendering cleanly in the gutters, correctly positioned below the header with no overlap into page content.

Known, accepted limitation, not silently left: `AdSlot`'s AdSense and real-admin-uploaded-image branches still use the horizontal `max-w-3xl` wrapper regardless of placement - only `HouseAd`'s new skyscraper branch has the narrow wrapper logic. Harmless today since no real ad can target these two placements yet (nothing to configure them with), but a future real ad aimed at `skyscraper-left`/`right` would need that wrapper logic extended first - not attempted now since it's speculative work with no real ad to test against.

**Same-day follow-up fix (commit `09e5318`, deployed and verified with two comparison screenshots): the skyscraper ads overlapped real page content.** Customer sent a screenshot showing it directly. Real root cause: the stock Tailwind `2xl` breakpoint (1536px) was picked on a "usually enough gutter" assumption instead of actual math against this site's real container. The homepage (and most pages) center content in `max-w-[100rem]` (1600px) with `px-6` padding inside that box - the box doesn't even start shrinking below full-bleed until the viewport exceeds 1600px, so at 1536-1900px there is close to zero real gutter. On an ordinary 1920px monitor the ad's right edge landed well inside the content box's own edge, visibly overlapping the hero text.

Fixed with an actual computed threshold: content edge sits at `(viewport-1600)/2` from the viewport edge; solving for that to clear the ad (offset 16px + 160px width) with a safety margin gives viewport ≈ 2050px+, rounded to a custom `min-[2100px]` Tailwind variant instead of a guessed stock breakpoint. This deliberately makes these ads ultra-wide/4K-adjacent-monitor-only, not "wide laptop" - a real consequence of the site's own generously wide content columns, not a smaller ad awkwardly squeezed into a smaller gutter. Also increased the card height per the customer's explicit "daha uzun" (longer) ask, from a fixed `600px` to `min(80vh, 850px)` - fills more of the available height on genuinely tall wide monitors while staying capped so it never overflows a shorter browser window.

Verified with two real Playwright screenshots this time, not just one: at 1920×1080 (below the new threshold) the ads correctly do not render at all, confirming the overlap is fully gone rather than just less-severe; at 2200×1200 (above the threshold) both ads render, visibly taller than before, with a clean, comfortable gutter gap before the actual hero content starts on both sides - no overlap.

**Ad frame redesign (commit `6556bad`, deployed and verified with clean screenshots on two pages/themes).** Customer's direct, blunt feedback: "çerçeveleri mükemmel hale getirmen lazım. Kötü görünüyor" (the frames need to be perfect, they look bad). Real root cause: the outer frame was a bare 1px border color-mixed from each ad's own `fg` color at 20% opacity - too faint to read as a deliberate edge against several of the busier gradients, and "flat bordered rectangle" rather than a polished, elevated card.

Replaced with the actual visual language real premium ad units use - a "glass card" edge: a bright inset top highlight (a lit edge, not just an outline), a subtle all-around inset hairline for corner definition, and a two-layer outer drop shadow (soft+wide, tight+dark) for genuine depth/separation from the page. Deliberately colorless (white-based, not per-ad-color) so it reads identically crisp across all nine very different gradient palettes instead of needing hand-tuning per ad. Applied consistently to the outer card, the icon badge, and the CTA pill on both the horizontal and skyscraper layout branches, so the whole card reads as one designed piece, not just the outer edge. Also bumped border-radius from `rounded-lg` to `rounded-xl` to match this site's own established card convention (`components/ui/card.tsx` already uses `rounded-xl` site-wide).

First screenshot attempt was blocked by the real site announcement popup (`SitePopupModal`) covering/blurring the whole page in a fresh Playwright session with no cookies - found and used its real `aria-label="Kapat"` close button (not a guessed selector) to get a clean, unobstructed view. Verified on two different pages/themes: the homepage skyscraper ads at 2200×1200 in **light mode** (confirms the redesign holds up outside the dark-mode screenshots used throughout this ad work so far) and `/akis`'s horizontal + sidebar house ads at 1440×900 - both clearly show the lit top edge and real shadow depth versus the previous flat look, and the icon badges/CTA buttons read as polished rather than plain flat-colored shapes.

**House ads redesigned again - dark "membership card" surface replacing the colored-stripe look (2026-09-03, commit `8b0f1be`, deployed and verified with two live screenshots, no migration).** Customer rejected the previous frame pass outright: "çerçeveleri yalnızca kalınlaştırmışsın... saçma renkli şeritlere değil, gerçekten havalı ve ayrıcalıklı görünen çerçevelere ihtiyacımız var" (you just made the borders thicker - we need frames that look genuinely cool/exclusive, not silly colored stripes). Correctly diagnosed: the glass-card shadow fix improved edge definition, but never addressed the actual complaint - every card's entire surface was a saturated per-ad gradient flood, which reads as a cheap banner stripe no matter how good its shadow/border is.

Redesigned at the root rather than tuning the shadow further: every one of the 9 house ads now sits on one shared dark graphite surface (`CARD_SURFACE`, a near-black two-stop gradient, deliberately identical across all placements) - a real design decision, not a missing feature, since visual *uniformity* across ads is what reads as "one deliberate premium product line" rather than nine unrelated banner colors. Each ad's own hue is now used only as a restrained accent: a thin animated "foil" gradient confined to the 1px outer border (previously the full card background), a soft blurred corner glow, the icon's ring-badge stroke (an outlined circle, not a filled colorful blob), the kicker label, and an outlined/ghost CTA button that fills solid on hover via CSS custom properties (`--accent`/`--on-accent` set inline per ad, referenced in a shared `:hover` rule - avoids needing 9 separate hover style blocks). Added a small top-row "DKList" wordmark + "Reklam" disclosure chip, mirroring how real premium card products (transit passes, membership tiers, metal credit cards) lay out an issuer mark and fine print - reinforcing "this is a real, considered product" rather than "this is an ad slot filled with a gradient."

Real bug caught while doing the color rework: `book-page`'s original spec had its light/dark accent pair inverted relative to the other 8 ads (it was designed against the old light-background scheme, where the "accent" badge color was meant to be dark with a light icon on top) - reusing it as-is under the new dark-surface scheme would have made its icon ring/kicker/border render as a near-invisible dark-on-dark accent. Fixed by defining a proper bright gold accent (`oklch(0.8 0.13 85)`) for it specifically, verified visually. Dropped the now-unused `fg` field entirely (title/body text is uniformly white/graphite-toned across every ad under the new scheme, no more per-ad text-color tuning needed) and renamed `bg`→`edge`/`badgeIcon`→`onAccent` to reflect what these fields actually control now, rather than leaving misleading names from the old design.

Verified live on two real pages after a full deploy (build/lint/85 tests/`tsc` all clean beforehand): the homepage's two skyscraper ads (light mode) and `/akis`'s horizontal + tall-sidebar house ads, both via a real Playwright screenshot with the site's own announcement popup closed via its real `aria-label="Kapat"` button first. Both screenshots show the intended result clearly - dark graphite cards, a thin lit accent ring around each icon, a slim shimmering border instead of a flooded background, and outlined pill CTAs - a materially different, more restrained/premium look than either of the two previous passes, not just a shadow or border-width tweak.

**Purchasable profile frames redesigned too - same complaint, different feature (2026-09-03, commits `9987dd9`/`a602ea5`, deployed and verified with real zoomed screenshots of two live production profiles).** Customer clarified their frame complaint wasn't about the ads at all: "çerçeve değil la profillerdeki. Puan ile satın alınabilen" - the Puan Mağazası's purchasable profile frames (`profile_frame` rewards: Bronz/Gümüş/Zümrüt/Ametist/Yakut/Safir/Altın/Elmas, confirmed live on prod - one real redemption, two accounts with a frame equipped). These were rendered as a completely flat single-color outline: `boxShadow: 0 0 0 3px <color>` on the profile page, `border-4` swatches in the store list and admin panel - a plain colored line gives zero reason to grind points for one.

New shared `ProfileFrameRing` (`src/components/dklist/profile-frame-ring.tsx`) derives a real medal-style ring from each reward's one stored color rather than requiring a hand-authored design per tier (which would break the moment an admin adds a 9th color): a slowly rotating conic gradient built as base-color-dominant with one genuine dark band and one bright near-white glint (like a single light source catching a physical polished ring), plus a soft matching outer glow. Wired into all three places the flat swatch appeared: the equipped avatar frame on the profile page, the store's reward-list preview (so people judge the real look before spending points), and the admin reward-list preview. No schema/reward-data change - `rewardValue` still means exactly one CSS color, so the existing real redemption and both already-equipped test frames kept working unchanged through the redesign.

Caught and fixed a real dilution bug via a zoomed verification screenshot, not by eye at normal size: the Avatar's own default `ring-4 ring-card` (a white outline) sits directly between the avatar image and the new frame ring, physically overlapping it and washing the whole band toward pale white - first-pass screenshots looked like a soft white halo instead of a distinct gem color. Fixed by giving framed avatars a thin `ring-2 ring-black/10` hairline instead (unframed avatars keep the original `ring-4 ring-card`), and widened the ring 5px->7px so the banding has room to actually show. Verified again with tight zoomed screenshots (`deviceScaleFactor: 3`, clipped to just the ring element) of two real production profiles: `admin` (Elmas/Diamond, `#7fd8e8`) now shows a clear cyan/teal metallic band with a distinct dark and bright arc, and `gokhanoral` (Yakut/Ruby, `#9b2226`) shows a genuine deep-red polished-ring look with visible depth and a glint - both a dramatic, confirmed improvement over the flat line, not just a claimed one.

**Frames tiered by point cost - the cheap and expensive ones now genuinely differ (2026-09-03, commit `2ebdc00`, deployed and verified across all four tiers via real screenshots).** Customer's direct follow-up: "Gelişme var ama yine de yeterli değil. Hepsi bir renk aq. Adam gibi yüksek olanları mükemmel iyi yapsana" (there's improvement but still not enough - they're all just one color - make the expensive ones properly excellent). Correct complaint: the previous pass fixed the *ring quality* but every reward from 30-point Bronz to 800-point Elmas got the exact same treatment, just recolored - no reason to actually chase the expensive ones.

New `frameTierFromPointCost()` (`src/lib/profile-frame-tier.ts`) buckets a 1-4 tier straight from each reward's existing point cost - no new schema field, so price alone decides the tier and any future reward (at any cost) slots in automatically. Real thresholds checked against the live 8-reward set: Bronz 30/Gümüş 80 -> tier 1, Zümrüt 150/Ametist 200/Yakut 250 -> tier 2, Safir 300/Altın 450 -> tier 3, Elmas 800 -> tier 4 (currently the sole showpiece). `ProfileFrameRing` escalates strictly by tier rather than just recoloring: tier 2 adds a slim bright inner accent line (double-ring); tier 3 upgrades the single-glint gradient to a multi-facet "cut gem" pattern (several alternating light/dark bands instead of one) plus a slow breathing glow-pulse animation layered on top of the existing rotation; tier 4 adds three independently-timed twinkling sparkle marks orbiting the ring plus a wider band, on top of everything tier 3 has.

New `getFramePointCost()` query resolves the cost for a frame stored only as a raw color on `user.profile_frame` (the profile page's situation) by matching it back to its `point_reward` row; the store and admin previews already had `pointCost` on hand directly from the reward row, so those needed no extra query.

Verified live across all four tiers using the two real equipped test frames plus two temporary same-account color swaps for full coverage (admin's account, restored to its real Elmas frame afterward) - each change required a container restart to actually see it, a genuine finding worth noting: `getProfileByUsername` is `'use cache'`-tagged per-username with a "minutes" `cacheLife`, and a raw SQL `UPDATE` (used here purely for fast verification, not through the app) doesn't trigger any `revalidatePath`/`revalidateTag`, so it silently kept serving the previously-cached ring. This is NOT a real bug in the actual feature - the app's own `setActiveProfileFrameAction`/`redeemRewardAction` both call `revalidatePath` on the profile route already - it's a verification-methodology quirk of bypassing the app layer with direct SQL, documented here so it isn't mistaken for a caching bug later. Screenshots confirmed a real, dramatic escalation: Bronz (tier 1) - a modest single-glint bronze band; Altın (tier 3) - a visibly multi-faceted gold ring with an inner accent line and richer glow; Elmas (tier 4) - the same faceted ring plus two clearly visible twinkling sparkle stars caught mid-animation, unmistakably the most spectacular of the four.

**Frames given a genuinely different SHAPE per tier, not just more color (2026-09-03, commit `2cae504`, deployed and verified with a local static-HTML preview before touching production).** Customer's third round, more pointed: "sadece renkleri farklı çerçeveler var... yükseldikçe şekilli heybetli şekilde olsa" (they're still just differently-colored rings - going up in tier should make the shape itself grander/more imposing). Correct - tiers 2/3/4 so far only varied color/glow/facet-count on the same plain circular silhouette.

Researched how real reward-frame systems actually escalate (Mobile Legends/PUBG Mobile avatar frames, Steam rare profile frames, Discord avatar decorations, in-game rank medallions): low tiers stay a plain ring, high tiers literally break out of the circle - a radiant "sunburst"/medallion halo, an emblem perched on top, visible layered depth. Rebuilt around that: tier 2 adds four small diamond-cut gem studs at the compass points (bezel-set look, still circular); tier 3 adds a real generated scalloped/gear polygon (`sunburstClipPath()` - alternating outer/inner-radius conic points, computed in JS, not an image asset) rotating independently behind the ring, the silhouette genuinely extending past the circle for the first time; tier 4 gets a bigger/sharper sunburst plus a small lucide `Crown` icon perched above the ring with its own glow/bob animation, on top of the existing sparkles.

Verified locally first, deliberately not iterating against production: wrote a standalone static HTML/JS file reimplementing the exact same clip-path math and gradients with zero DB dependency, screenshotted all four tiers side by side via Playwright against the local file. The escalation read immediately at a glance - plain ring -> jeweled ring -> gear-shaped halo -> spiky halo with a crown and sparkles - before ever touching the live site.

**Frames and a person's highest badge now show everywhere, not just the profile page (2026-09-03, commit `8f1a151`, deployed and verified live on /akis).** Customer's next ask: "Çerçeveler akış yorum tartışma mesaj vs gibi heryerde gözüksün. Kişinin sahip olduğu en yüksek rozet de gözüksün onu da hallet." Both the purchasable frame and a real ranking of the person's own badges needed to show wherever their avatar renders site-wide.

New `src/db/queries/user-decorations.ts`: `getUserDecorations(userIds)` - one batch lookup resolving each user's frame tier (via the existing point_reward cost lookup) and their single highest-ranked badge. "Highest" only has a well-defined meaning for the auto-awarded lifetime-point milestone badges (`POINT_MILESTONES`, real order 25/100/250/500) - matched back by name; admin-manually-assigned badges (the older v1 `badges`/`user_badges` tables) have no inherent rank anywhere in the system, so they're deliberately excluded from "highest" rather than inventing a fake ordering. `EntityAvatar` - the one shared small-avatar component already used at every real user-avatar call site across the app - gained optional `profileFrame`/`frameTier`/`highestBadge` props: wraps the avatar in `ProfileFrameRing` when framed, and adds a small colored medal-icon corner mark (bronze/silver/gold/magenta by milestone tier, with the badge name as a hover title) when a badge is held. `ProfileFrameRing` also gained an automatic "compact" mode below 60px - the full sunburst/crown/sparkle spectacle from the previous pass stays reserved for the one prominent 96px profile-page avatar; every dense inline context gets the same color/facet/glow ring scaled down, without a spiky halo or crown colliding with neighboring text at comment-thread density.

Wired at every genuine user-avatar site found via a full codebase grep sweep: comments and their reply threads (book/writer/translator/club pages all funnel through `comments.ts`'s `getEntityComments`/`getRepliesForComments`, enriched once at the query layer itself so zero page-level changes were needed there or in `feed-posts.ts`'s equivalent for feed-post replies), the feed composer's own avatar, `/akis` itself (post/comment author avatars, the community sidebar's own-profile row, the right rail's follow-suggestions and weekly-leaders widgets), messages (conversation list, message requests, the open thread's header), the homepage's top-readers/weekly-leaders/follow-suggestions widgets, yazarhane's member grid and post list, blog post owners (list and detail pages), and a book's "Bu Kitabı Okuyan Üyeler" list. Deliberately left un-enriched: writer/translator/publisher entity avatars (no user, so no frame/badge concept applies at all) and a signed-in viewer's own optimistic comment/reply shown for the instant before the next refresh (falls back to no decoration rather than threading yet another prop through every optimistic-update call site - self-corrects on refresh, a real but minor and disclosed gap).

Verified live on the real production site post-deploy: a Playwright screenshot of `/akis` shows colored ring frames and medal badge marks on `@admin` and `@gokhanoral`'s avatars in both feed posts and comments, and all three entries in the right rail's "Bu Haftanın Liderleri" widget - confirming the decoration data flows correctly through the batch lookup into every one of these distinct rendering contexts, not just the ones tested in isolation.

## PR backlog re-audit #3 (2026-09-03, cloud sandbox) - still zero merged, the previous audit itself never landed either

**This is the third consecutive cloud-agent audit of the same unaddressed backlog** (`v2-cloud/pr-backlog-followup-0831` on 08-31, `v2-cloud/pr-backlog-audit-0902` on 09-02, this one on 09-03). Nothing has changed in the interim: **still zero `v2-cloud/*` branches merged since 2026-08-23**, 27 open PRs against `v2-rewrite` (#7 through #33), 28 total open branches. The 09-02 audit's fix for "why didn't the 08-31 warning get seen" - write it straight into `PLAN.md` on a branch - repeated the exact same mistake it was diagnosing: **`v2-cloud/pr-backlog-audit-0902` was never merged into `v2-rewrite`, and no PR was ever even opened for it**, so this session found it only by manually diffing every `v2-cloud/*` branch against `v2-rewrite`, not by reading this file. This entry supersedes both prior ones and, this time, is being opened as an actual PR (not just pushed to a branch) specifically to break that pattern.

**No new branch opened for feature/test work this pass**, for the same reason as 09-02: every DB-free pure-logic file still has an existing open branch claiming it, and adding a 29th unreviewed branch would compound the real problem rather than help it. `v2-cloud/pr-backlog-audit-0902` can be deleted once this PR is merged - its content is fully carried forward here.

**The actual blocker is a batch human-review pass, not more cloud-agent output.** The risk-tiered inventory from the 09-02 audit still holds (re-verified the branch list is otherwise unchanged - no new commits landed on `v2-rewrite` between the two audits' branch-count baselines other than product-feature work, no `v2-cloud/*` branch was merged or closed): `ci-workflow` first (unblocks CI for everything after it), then the ~20 independently-reviewable additive/extraction branches, then the four-branch `duplicate-detection.ts` cluster as one sitting, with a staleness re-read (not just a mechanical rebase) for the ~8 branches now 80-90+ commits behind `v2-rewrite`. See the 09-02 entry above for the full per-branch breakdown - it is still accurate as of this writing.

**If this entry is also still sitting unmerged the next time a cloud session reads this file: that itself is the finding to act on.** A recurring cloud-agent note that never reaches a human is a process gap worth fixing directly (e.g. a standing reminder/notification outside this repo), not something a fourth or fifth re-audit will resolve on its own.
