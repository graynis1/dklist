# Backend API base URL (Symfony backend), including the /api prefix
REACT_APP_API_BASE_URL=https://your-backend.onrender.com/api

# Backend base URL without the /api prefix (used for static files like the CSV import template)
REACT_APP_BASE_URL=https://your-backend.onrender.com
